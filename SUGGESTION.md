# SUGGESTION.md - Assistant Recommendations

Last updated: 2026-07-08 15:13 (Asia/Jakarta)

## Purpose
This file is exclusively for Codex/assistant suggestions. It is where I record improvement ideas I notice while working on the project: product value, UX, reliability, performance, security, data quality, operations, migration safety, and developer workflow.

User decisions remain authoritative. This file is not a replacement for `AGENTS.md`, `CODEBASE.md`, or implementation plans; it is a proactive recommendation backlog.

## How To Use
- Status values: `Suggested`, `Accepted`, `In Progress`, `Done`, `Deferred`.
- Keep each suggestion actionable: include why it matters and the next concrete step.
- Promote accepted suggestions into the relevant plan or implementation document before large work begins.
- Add or update entries whenever a project improvement becomes clear during implementation.
- Remove or mark stale suggestions when project direction changes.

## Recommendation Backlog

| ID | Area | Status | Suggestion | Why It Matters | Next Step / Result |
| ---: | --- | --- | --- | --- | --- |
| 1 | Auth QA | Suggested | Create a production auth/account-linking QA checklist. | Recent auth work connects custom Clerk UI, D1 roles, `/login`, `/daftar`, `/profil`, Google SSO, password recovery, and profile edits; identity bugs are high-impact. | Cover email/password registration, Google registration, same-email account linking, forgot password/email, role selection, protected `/daftar`, `/profil` role tabs, admin/staff auth inspection, and logout. |
| 2 | Auth architecture | Done | Split `js/auth-clerk.js` into smaller modules. | The auth runtime had too many responsibilities, making changes risky. | Implemented `js/auth-clerk-ui.js` and `js/auth-clerk-core.js`; `js/auth-clerk.js` now focuses on auth page/event behavior while preserving `window.FranchiseAuth`. |
| 3 | Media uploads | Done | Build the R2 media upload path. | Franchisors need first-party logo, cover, and proposal uploads instead of fragile URL fields. | Implemented `/profile-upload`, R2 binding config, `franchise_assets` writes, listing media updates, and `/profil` upload controls. Production R2 domain is now configured as `https://assets.franchisee.id`. |
| 4 | Franchisee value | Done | Make saved opportunities cross-device. | Local-only saves are weak after login and across devices. | Implemented `franchise_saved_opportunities`, profile API save/remove, local-save migration, profile rendering, and public save buttons. |
| 5 | Franchisor value | Done | Add a franchisor lead inbox. | Franchisors need a clear place to see and respond to leads for owned listings. | Implemented `/profil` Leads tab with incoming lead cards, contact shortcuts, listing links, and owner-checked lead status updates. |
| 6 | Publish operations | Suggested | Finish static publish automation verification. | Approved listing edits may not appear publicly if the dirty-record-to-rebuild cycle is not fully verified. | Verify Cloudflare Pages build command, required secrets, deploy hook, GitHub scheduler, D1 queue records, and one controlled edit-to-rebuild cycle. |
| 7 | Contact data | Done | Normalize franchise contact data. | Imported phone/contact fields are messy; structured contacts improve editing, validation, WhatsApp links, outreach, and quality checks. | Added `franchise_contacts`, contact normalization helpers, and a dashboard refresh action that migrates high-confidence values from existing listing/profile fields. |
| 8 | Data quality | Done | Persist dashboard data-quality checks. | Read-time-only checks are hard to audit, trend, assign, and resolve. | Added `franchise_quality_checks`, refresh logic for missing media/contact/description/category, all-caps, suspicious contacts, stale listings, and invalid URLs; dashboard reads persisted checks with a live fallback. |
| 9 | Dashboard review UX | Done | Convert staff listing review from JSON to guided fields. | Guided field-level inputs reduce mistakes and make staff/admin review faster. | Replaced the JSON textarea with field rows from shared editable-field definitions and changed pending reviews to show old/new values by field. |
| 10 | Shared validation | Done | Share Zod schemas across forms, functions, and imports. | Validation can drift when each endpoint, form, and importer defines payload rules separately. | Added `functions/_shared-schemas.js` for dashboard actions, form submissions, `/get-franchises` query params, and shared normalizers. Added `src/lib/shared-schemas.ts` for CSV import rows and D1 static snapshot rows used by import/build/Astro paths. |
| 11 | CSV reliability | Done | Replace remaining naive CSV parsing. | CSV parsing bugs can silently shift franchise fields. | Added `scripts/shared-csv.cjs` and moved legacy listing/sitemap CSV fallback reads onto the shared quote-aware parser. |
| 12 | Ranking signals | Done | Add product analytics for ranking signals. | Popularity and recommendations should eventually use real behavior instead of mostly static listing quality. | Added privacy-safe product event tables/API/client hooks for listing view, save, unsave, inquiry, and contact click; profile recommendations now include recent event counts in scoring when available. |
| 13 | Operations telemetry | Done | Strengthen operations telemetry. | Auth failures, webhook failures, publish queue failures, broken media, and API errors should not hide. | Added operation telemetry tables/helper, logged key API/webhook failures, and expanded dashboard health with operation counts, webhook summaries, recent audit events, rebuild state, and activity counts. |
| 14 | Multi-site controls | Done | Design multi-site publishing controls. | The shared franchise network will need clear controls for where listings appear. | Added admin dashboard publication status controls across `network_sites` and franchisor-facing distribution status in `/profil`; publication changes enqueue rebuild requests for the affected site. |
| 15 | Copy quality | Done | Improve public copy consistency. | Public-facing copy should avoid internal terms and give clear next actions. | Completed a public-copy pass for touched Premium/Profile membership flows: stale payment details moved behind the profile action, Membership unavailable copy now gives a clear retry step, renewal copy is user-facing, and "Kedaluwarsa" spelling is standardized. |
| 16 | Premium conversion | Done | Add premium conversion analytics events. | `/premium` and `/profil` membership actions are a revenue funnel; without events we cannot see where franchisors drop off. | Added `premium_funnel_events`, `/premium-event`, `/premium` CTA tracking, profile/dashboard event writes, and dashboard funnel summary. |
| 17 | Payment operations | Done | Move payment settings into admin-managed data. | BCA account details and future QRIS/e-wallet instructions should not require code edits every time payment methods change. | Added `payment_methods`, fallback BCA method, profile payment instructions from managed data, and dashboard payment-method editor. |
| 18 | Premium trust | Done | Add a premium readiness checklist for listing completeness. | Activating Premium before a listing has logo, proposal, contact, package, and description can make the paid product feel weak. | Added readiness checks to `/profil` Membership and pending dashboard Premium payment review. |
| 19 | Premium operations | Done | Add owner/admin notifications for premium payment events. | Manual payment review depends on admins noticing confirmations and owners knowing whether payment was approved or rejected. | Added `premium_notifications`, owner profile notifications, admin dashboard notifications, and review/confirmation notification writes. |
| 20 | Maintainability | Done | Split the profile API and profile client after the premium MVP settles. | `functions/profile-data.js` and `js/profile-page.js` now handle account, role, franchisee, franchisor, listing, lead, saved opportunity, and premium workflows; future changes will be slower and riskier if they keep growing. | Extracted premium backend logic into `functions/_profile-premium.js`, shared operations into `functions/_premium-ops.js`, and public premium tracking into `js/premium-page.js`. |
| 21 | Payment safety | Done | Add proof-of-payment attachment support with file size/type checks. | The current manual confirmation stores text fields only; screenshots or PDFs can make admin review faster and reduce back-and-forth. | Added `/premium-receipt-upload`, R2 storage, image/PDF validation, confirmation `proof_asset_id`, profile upload flow, and dashboard proof links. |
| 22 | Premium renewal | Done | Support renewal before Premium expires. | A paid franchisor should not wait until the subscription ends before preparing the next payment. | Added a 30-day renewal window, renewal CTA in `/profil`, backend renewal order allowance, and approval logic that starts renewed subscriptions after the current term ends. |
| 23 | Premium operations | Done | Show upcoming Premium expiries in dashboard operations. | Admins need early visibility before paid listings lapse. | Added expiring subscription reads and a dashboard operations list for Premium terms ending soon. |
| 24 | Notification delivery | Done | Queue important Premium emails before adding an email provider. | Owner/admin in-app notifications are useful, but email delivery needs a durable queue before provider integration. | Added and remotely applied `notification_email_queue`, helper writes for payment submitted/approved/rejected events, and dashboard queue summary visibility. |
| 25 | Profile maintainability | Done | Extract Premium profile helper logic from the large profile client. | `js/profile-page.js` is still large; small pure helper extraction reduces future edit risk without changing the page architecture. | Added `js/profile-premium.js` for Premium status labels, active subscription selection, expiry formatting, and renewal-window checks. |
| 26 | Premium payment copy | Done | Keep public payment copy aligned with managed payment settings. | Hardcoding bank/account details on `/premium` can drift from dashboard-managed payment methods. | `/premium` now points users to `/profil/?tab=membership` to generate the current nominal and payment instructions for their selected listing. |
| 27 | Notification delivery | Done | Connect `notification_email_queue` to an outbound email worker. | The queue now records important payment messages, but users/admins still will not receive email until a provider and sender policy exist. | Added protected `/premium-email-worker`, Resend delivery support, sent/failed/provider metadata, retry backoff, and a GitHub Actions cron trigger. Manual setup still needs Resend/API/DNS secrets before real delivery starts. |
| 28 | Premium lifecycle | Done | Add scheduled Premium expiry and renewal reminders. | Dashboard visibility helps admins, but owners should receive reminders before expiry and the system should avoid silent lapses. | Added scheduled expiry marking plus 30/14/7/1-day renewal reminder queuing through `notification_email_queue`, with `premium_subscription_reminders` preventing duplicate reminders. |
| 29 | Payment operations | Done | Add admin retry/cancel controls for queued payment emails. | Queued emails can fail or become stale; admins need a simple recovery path without editing D1 manually. | Premium Operations now shows recent queued email rows with icon-only retry/cancel controls for admin and recent error text. |
| 30 | Premium data integrity | Done | Add renewal/order guardrails at the database layer. | Current duplicate prevention is mostly application-level; race conditions or manual retries could still create confusing duplicate active renewal orders. | Added and remotely applied `0012_premium_email_worker_guardrails.sql` with delivery metadata, reminder idempotency, one active pending order per listing/user/plan, and unique source-order subscriptions. |
| 31 | Premium lifecycle | Done | Define public downgrade behavior when Premium expires. | Subscriptions now expire automatically, but public `verification_tier`, network publication, and rebuild behavior should follow a clear business rule before changing live visibility. | Implemented configurable 3-day default grace period, daily grace emails, Free downgrade after grace, Premium network publication hiding, and rebuild queue writes. |
| 32 | Dashboard maintainability | Done | Split `js/dashboard-admin.js` into dashboard modules. | The dashboard client now owns too many panels and workflows in one file; future Premium, review, or operations edits will be easier and safer after modular extraction. | Extracted shared action/render utilities into `js/dashboard-utils.js` and Premium Operations rendering/actions into `js/dashboard-premium-operations.js`; `js/dashboard-admin.js` now keeps core boot, tabs, and non-Premium panels. |
| 33 | Dashboard maintainability | Done | Continue dashboard module extraction for Review and Outreach panels. | `js/dashboard-admin.js` was smaller but still owned outreach, publication controls, leads, and auth debug in one file. | Added `js/dashboard-operations.js` for outreach, Premium payment review, publication controls, lead summary, and health rendering/actions; `js/dashboard-admin.js` is now a 279-line boot/tabs/auth facade. |
| 34 | Profile maintainability | Done | Split the profile client into role-focused modules. | `js/profile-page.js` was the largest runtime client file and mixed account, franchisee, franchisor, membership, uploads, and leads UI. | Added `js/profile-franchisee.js` and `js/profile-franchisor.js` on top of existing profile modules; `js/profile-page.js` is now a 727-line controller facade. |
| 35 | Profile API maintainability | Done | Continue splitting `/profile-data` write workflows after the current refactor settles. | The API facade was below the long-file threshold, but account, role, franchisee, saved opportunity, and inquiry write workflows still lived in one endpoint file. | Added `functions/_profile-account.js` and `functions/_profile-franchisee-actions.js`; `/profile-data` is now a 582-line facade delegating account/role/franchisee actions. |
| 36 | Profile API maintainability | Done | Split remaining franchisor/listing/lead write workflows from `/profile-data`. | The largest remaining workflow block in `functions/profile-data.js` was franchisor profile, owner listing edit, and lead status handling. | Added `functions/_profile-franchisor-actions.js` for franchisor profile edits, owner listing edit throttling/rebuild queueing, publication distribution reads, and lead status updates; `/profile-data` is now a 346-line facade. |
| 37 | Profile API maintainability | Done | Split the `/profile-data` read model after production verification. | The endpoint was small, but GET response composition still combined user/profile/listing/claims/recommendation/saved/lead/premium reads in one facade. | Added `functions/_profile-read-model.js` for GET composition and franchisee action loader callbacks; `/profile-data` is now a 92-line endpoint facade. |
| 38 | Profile reliability | Suggested | Add focused API handler smoke tests for `/profile-data` module boundaries. | The profile API is now well split, but behavior depends on callback wiring across read model, account, franchisee, franchisor, and premium modules. | Add lightweight tests with mocked D1 statements for action dispatch and unchanged GET response keys before the next profile feature batch. |
| 39 | Premium media reliability | Done | Migrate legacy Blogspot proposal images into the owned media bucket and generate download PDFs server-side from allowlisted image hosts. | Browser PDF creation from R2 images can still fail when the asset host does not return CORS headers for canvas rasterization. | Image migration is done for 34 franchises / 491 proposal images: R2 objects, D1 `franchise_assets`, first-party `proposal_url`, and rebuild queue writes were applied. PDF download now posts image URLs to `/proposal-download`, which fetches allowlisted proposal hosts server-side and adds the Franchisee.id watermark only to the returned PDF. |
| 40 | Static renderer maintainability | Done | Split `src/lib/franchise-static-assets.ts` into directory assets and detail assets. | Premium detail CSS/JS pushed the generated asset helper above 900 lines; separating directory filtering assets from detail tab/proposal assets reduces future edit risk. | Extracted `injectDetailAssets()` and proposal PDF helpers into `src/lib/franchise-detail-assets.ts`, leaving placeholders and directory controls in the current asset module. |
| 41 | Media migration operations | Suggested | Add a faster R2 bulk media migration path using R2 S3-compatible credentials or a small batch uploader. | The one-file-at-a-time Wrangler path works and is safe for small backfills, but large media migrations are slow, timeout-prone, and harder to resume cleanly. | Before the next large asset migration, create scoped R2 access keys and a script that uploads with bounded concurrency, checksum/size verification, and a resumable manifest. |
| 42 | Local discovery SEO | Done | Add city/region landing pages after we have reliable location fields. | User intent often includes city names, but the current listing data has limited clean city coverage. Thin city pages would be risky unless backed by normalized city/service-area data. | Added static `/peluang-usaha/kota/` and city pages generated from listing city/address/outlet/service-area text only when a city has enough matching listings, plus internal links from the directory and buyer tools. |
| 43 | Buyer qualification | Done | Store comparison and calculator intent as optional lead context. | Budget matcher and BEP calculator can make leads more useful for franchisors, but the current tools are anonymous/local only. | Budget matcher, BEP calculator, and comparison selections now write optional browser lead context, and public/profile inquiry submissions attach that context to the lead payload. |
| 44 | Promo measurement | Done | Track promo ribbon impressions and CTA clicks. | Admin-managed campaigns are now configurable, but we need campaign performance before deciding future discount/bonus policy. | Public promo ribbon views and CTA clicks now write coarse `premium_funnel_events` with safe campaign metadata. |
| 45 | Local SEO data quality | Done | Normalize location/service-area data into a structured table before scaling city pages aggressively. | The new city pages work from available text fields, but better city coverage needs cleaner city, province, service-area, and outlet fields instead of relying on text matching. | Added generated location metadata, a repeatable location sync script, remote backfill for 45 cities and 715 franchise-location rows, and static build support for `structured_locations` with text fallback. |
| 46 | Location management UX | Done | Add admin/owner UI to review and edit structured franchise locations. | Backfilled city/service-area data is useful, but franchisors/admins should correct service areas, outlet cities, and head office locations directly instead of waiting for another inferred sync. | Added `/profil` owner Area Listing editor and `/dashboard` admin Area Listing editor backed by `locations` and `franchise_locations`; manual rows override generated rows in static builds and enqueue public rebuilds. |
| 47 | Location write maintainability | Done | Extract shared location-write helpers for profile and dashboard actions. | Suggestion 46 intentionally kept the profile and dashboard changes scoped, but both actions normalized city rows and wrote manual `franchise_locations` rows with near-identical behavior. A shared helper reduces drift before adding more location types or bulk edits. | Added `functions/_location-writes.js` for manual location normalization, deterministic ids, D1 statement creation, and audit summaries; profile and dashboard location actions now call it. |
| 48 | Premium maintainability | Done | Split `_premium-ops.js` into focused Premium operation modules. | The file combined settings, pricing, promo, notifications, email queue, lifecycle expiry, annual reports, reminders, and readiness; that size increased risk for future Premium changes. | Added `_premium-settings.js`, `_premium-notifications.js`, `_premium-lifecycle.js`, `_premium-readiness.js`, and `_premium-ops-utils.js`; `_premium-ops.js` now remains as a compatibility facade. |
| 49 | Profile CSS maintainability | Done | Split `css/profile.css` by profile surface after the backend Premium split lands. | Profile CSS was the largest maintained file and mixed shell, Premium, franchisor listing/location/media, analytics, roles, and lead styles. Splitting it reduces CSS regression risk during future profile UI work. | Planned in `AUDIT.md`, then added `css/profile-premium.css`, `css/profile-franchisor.css`, and `css/profile-analytics.css`; `/profil` now loads those after the base profile stylesheet. |
| 50 | Refactor safety | Suggested | Add focused smoke tests for the newly split auth, form-submit, D1 builder, and CSV importer boundaries. | The large files are now modular, but future edits need quick proof that facade-to-helper wiring still preserves route/script behavior. | Add mocked-D1 tests for `/form-submit` dispatch and fixture-based dry-run output checks for the D1 builder/importer; include browser load-order checks for auth script globals. |
| 51 | Traffic conversion UX | In Progress | Strengthen the homepage-to-free-member-to-Premium funnel before paid traffic. | The code path exists, but homepage brand/CTA hierarchy, `/profil` next-step prominence, and Premium discoverability need to be strong enough before serious acquisition spend. | Implemented benefit-led homepage/Premium copy, free-brand/Premium CTA hierarchy, `/profil` next-best-action panel, `/profil` brand retheme, homepage schema/RSS/config cleanup, and directory/detail franchisor CTA modules. Remaining: run one production QA pass through free member creation and Premium activation. |
| 52 | Contact operations | Done | Confirm the public support mailbox for Franchisee.id before scaling traffic. | Paid traffic should not depend on an ambiguous support address or old-domain mailbox. | User confirmed `email@franchisee.id`; updated support-facing legacy pages, homepage footer, generated templates, and renderer fallback to use that mailbox. |
| 53 | Profile QA | Done | Add a focused profile client regression check for undefined helper calls. | The `/profil` admin next-action panel broke on a stale `selectedFranchise()` helper name that normal build checks did not catch. | Added `pnpm run profile:check`, which syntax-checks profile client files and fails if the stale undefined helper call reappears. |
| 54 | Authenticated UI QA | Suggested | Add a lightweight visual QA checklist for protected app shells after each profile/dashboard style pass. | `/profil` and `/dashboard` are now visually aligned, but authenticated states still need manual viewport checks because navbar account replacement, role badges, tabs, and admin-only panels depend on live session data. | Add checklist items for `/profil` logged-out redirect, franchisee/franchisor/admin nav states, tab hover/active states, `/dashboard` locked/login/ready states, mobile layout, and dark-header contrast after the next deploy. |
| 55 | Dashboard reliability | Suggested | Add a high-volume `/dashboard-data` read-model smoke test. | The dashboard failed only when enough editable listing IDs were loaded to exceed D1's SQL variable limit; normal build/type checks did not exercise that data volume. | Add a mocked-D1 or fixture-based test that loads more than 100 editable listings with structured locations and asserts dashboard GET composition succeeds without oversized bind lists. |
| 56 | Dashboard QA | Suggested | Add a browser-level dashboard interaction smoke test for Data Quality quick edit. | The pencil action was wired but seeded a hidden Review form, so it appeared to do nothing and only manual interaction exposed the bug. | Add a DOM/browser test that clicks a Data Quality quick-edit button, asserts the Review tab becomes active, the listing/reason/field controls are seeded, and admin copy says direct edit rather than suggestion. |
| 57 | Contact data QA | Done | Add regression coverage for imported contact parser edge cases. | Data Quality false positives came from real imported contact formats: regional country codes, multiple mobile numbers, hyphenated groups, landlines, and call centers. These can regress silently without focused checks. | Added `scripts/check-contact-parser.mjs` and `pnpm run contact:check` covering Malaysia, Singapore, China, Hong Kong, Taiwan, Vietnam, Indonesian multi-number, landline, and call-center cases. |
| 58 | International franchisor readiness | Done | Add explicit target-market and home-country fields for franchisors. | Phone parsing and country codes now allow regional franchisors to register, but admin review and buyer trust improves when listings can show “asal brand” and target market intentionally instead of inferring from phone/contact data. | Added optional `brand_country` / `target_market` fields to D1, `/daftar`, `/form-submit`, `/profil`, dashboard guided edits, D1 static snapshot validation, and public listing/detail fact rendering. Migration `0017` was applied remotely. |
| 59 | Data enrichment | Done | Backfill brand origin and target market for existing imported listings from clear phone-country signals. | User confirmed Indonesian phone numbers are acceptable signals for Indonesian origin/market defaults, while recognizable regional country codes can set non-Indonesia origin and Indonesian target market as a practical editable default. | Added a migration-style D1 backfill for legacy rows with clear phone/contact country signals; Indonesian listings default to Indonesia/Indonesia, while recognized non-Indonesia listings default to detected origin/Indonesia. |
| 60 | Country metadata maintainability | Done | Centralize country metadata used by forms, contact parsing, backend defaults, and public flags. | Country names, dial codes, aliases, and flag emoji appeared in several runtime layers. Keeping these lists duplicated increased drift risk as more countries are supported. | Added canonical `data/country-metadata.json`, public `json/country-metadata.json`, shared Functions/TypeScript metadata helpers, browser form metadata loading, and `pnpm run country:check` to catch drift. |
| 61 | Detail page QA | Suggested | Add an automated generated detail visual smoke check. | Recent listing-detail improvements depend on layout relationships that type/build checks cannot fully prove: centered H1, H2-row actions, side-by-side logo/social/facts, hidden legacy dummy blocks, and mobile stacking. | Add a small Playwright/static-HTML smoke that builds one fixture detail page and asserts key selectors plus screenshot dimensions for desktop and mobile before future traffic-facing detail-page UI changes. |
| 61 | Traffic operations | Done | Add a lightweight Cloudflare Pages Function usage review before traffic campaigns. | Client throttling reduces avoidable public Function invocations, but campaign readiness still needs route-level confirmation from Cloudflare analytics so `/product-event`, `/premium-promo`, `/auth-config`, or bot traffic can be distinguished quickly. | Added a `/dashboard` local Traffic Guardrail panel, added the Cloudflare route-metric review checklist in `docs/ux/LISTING_DETAIL_UX_AUDIT.md`, removed promo loading from generated low-intent pages, kept promo on `/premium` and `/profil`, and added a dashboard-configured per-day promo view cap. |
| 62 | Brochure data enrichment | Done | Add a resumable OCR backfill for image-only proposal pages already stored in R2. | Digital PDF text extraction now handles new selectable-text uploads, but the migrated image-page archive and scanned PDFs require OCR. Running large OCR synchronously during upload would risk timeouts and Worker CPU limits. | Added admin-triggered OCR jobs, provider adapters, content-hash cache, attempt/usage logging, local quota counters, bounded provider failover, and persistence through the existing admin-reviewed proposal knowledge path. |
| 63 | Credential security | Done | Encrypt D1-stored OCR credentials with a root key held outside D1. | Dashboard masking prevents browser disclosure, but application-readable credentials stored only in D1 cannot be meaningfully protected from database disclosure without an external root key. | Added AES-GCM encrypted credential envelopes using the Cloudflare Pages secret `OCR_KEY`; dashboard saves/enables now fail closed without the external key, and existing plaintext values are re-encrypted on next save. |
| 64 | Listing detail maintainability | Done | Split `src/lib/franchise-detail-assets.ts` before the next major detail-page feature. | The file mixed generated CSS, injected browser scripts, brochure reader behavior, contact-tab logic, and compare controls; that size increased review and truncation risk. | Extracted generated styles to `src/lib/franchise-detail-styles.ts`, injected browser behavior to `src/lib/franchise-detail-scripts.ts`, reduced `src/lib/franchise-detail-assets.ts` to an injector facade, and added `pnpm run detail-assets:check`. |
| 65 | OCR maintainability | Done | Centralize OCR provider field requirements before adding provider adapters. | The dashboard had a provider-specific field matrix, while the backend had provider-specific activation checks. Adapter code would make this drift-prone unless the requirements came from one shared provider metadata contract. | Added `src/lib/ocr-provider-metadata.js` to drive dashboard field visibility, backend activation validation, dashboard metadata injection, and OCR regression checks. |
| 66 | OCR operations | Done | Add a one-asset production OCR dry-run and provider-health review before a broad backfill. | The queue/failover layer prevents unbounded sending, but real provider behavior, response shape, OCR quality, and quota counters should be verified against one low-risk brochure page before processing many assets. | Added admin-only `run_ocr_dry_run` plus a `/dashboard` “Dry run 1 aset” button. It requires `OCR_KEY` and one enabled provider, prepares at most one proposal-image job, processes only that job, and then surfaces the result in the existing OCR job status/recent-job panel. |
| 67 | OCR operations | Done | Add a controlled OCR worker/cron for large backfills after provider quality is verified. | The dashboard batch button intentionally processes only a few jobs per click to avoid request timeouts and accidental quota burn. Once OCR.Space/other provider quality is confirmed, a scheduled worker can process a safe quota-aware amount without manual clicking. | Added protected `/ocr-worker`, optional `.github/workflows/ocr-worker.yaml`, `OCR_SECRET` trigger auth, manual workflow runs, scheduled-run gate via `OCR_WORKER_ENABLED=true`, daily usage cap checks, and operation-event summaries. |
| 68 | OCR operations | Done | Add provider-specific request-rate metadata and cooldown scheduling. | The OCR runner already guards daily/monthly/trial quota and avoids duplicate OCR through content hashes, but manual/worker batches also need a local short-window guard so provider calls are not fired too tightly. | Added migration `0022_ocr_provider_rate_limits.sql`, seeded conservative local rate windows, added `cooldown_until`, made the OCR runner skip providers whose local window is exhausted, and exposed rate/cooldown metadata in `/dashboard`. |
| 69 | OCR QA | Done | Add a focused dashboard OCR client regression check. | Provider activation, no-provider disabled state, and retry controls are browser-module behavior that can regress without backend/type failures. | Added `scripts/check-dashboard-ocr-client.mjs` and `pnpm run dashboard:ocr:check` to syntax-check the browser module and assert the provider toggle, retry actions, and no-provider disabled-state wiring remain present. |
| 70 | Dashboard maintainability | Done | Extract dashboard feature CSS from the large base dashboard stylesheet. | `css/dashboard.css` had grown past 1,190 lines and was mixing shell layout with auth, OCR, review, operations, and Premium feature styles, making active dashboard UX changes harder to review. | Added `css/dashboard-auth.css`, `css/dashboard-review.css`, `css/dashboard-operations.css`, `css/dashboard-premium.css`, and `css/dashboard-ocr.css`; kept `css/dashboard.css` as the base shell/layout stylesheet; updated `/dashboard` load order and `AUDIT.md`. |
