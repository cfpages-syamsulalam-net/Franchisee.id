# Franchisee.id Codebase Map

Last updated: 2026-07-14 01:26 (Asia/Jakarta)

## Purpose
`CODEBASE.md` is the living map of project-owned logic. Keep it current whenever relevant files, functions, data contracts, routes, generated assets, or backend responsibilities change. The large WordPress-exported HTML files are mostly static surface; this document focuses on the runtime, builders, data files, templates, workflows, and integration points that define application behavior.

## Current Shape
Franchisee.id is a WordPress-to-static conversion hosted on Cloudflare Pages. Most pages are static HTML/CSS/JS exported from WordPress/Astra/Elementor. The interactive layer is custom JavaScript in `/js`, Cloudflare Pages Functions in `/functions`, generated/static data in `/json` and `/csv`, TypeScript D1 import/public-page generation pipelines in `/scripts`, and GitHub Actions that trigger D1-backed publish/email jobs.

The legacy data source is Google Sheets:
- `FRANCHISOR`: claimed/listed franchise brands.
- `UNCLAIMED`: scraped or imported potential brands that owners can claim.
- `FRANCHISEE`: prospective buyers/leads.

CSV files in `/csv` are local fallback/source snapshots and the current import source for D1 seeding. JSON files in `/json` are frontend runtime data assets, especially the sanitized claim-search list.

The first CSV snapshot has been imported into remote Cloudflare D1 `franchise_db`:
- `franchises`: 197
- `franchise_packages`: 190
- `franchise_site_publications`: 197
- `franchisee_profiles`: 2
- `franchisor_profiles`: 1
- `legacy_source_rows`: 199

## Target Upgrade Direction
The current Sheets/CSV/functions implementation is a transition layer. The project direction is:
- Astro on Cloudflare for new public directory and application pages.
- Cloudflare D1 `franchise_db` as the shared source of truth for users, franchisees, franchisors, franchises, claims, leads, packages, locations, assets metadata, network-site publication, premium orders/payment confirmations, subscriptions, and audit events.
- Cloudflare R2 for franchise media and proposal assets.
- Clerk for login/register, identity/session handling, and protected franchisee/franchisor/admin route entry.
- TypeScript by default for new app/backend/importer/schema work.
- Zod for runtime validation of forms, API inputs, imports, Clerk webhooks, env/config, and D1 write inputs.
- Source-controlled SQL migrations for all D1 schema changes.
- D1-authoritative roles: `franchisee`, `franchisor`, `staff`, and `admin`; Clerk metadata is a UI hint only.
- Drizzle can be added when D1-backed route handlers need shared typed queries, but initial schema migrations should remain explicit and reviewable.
- Existing CSS and form field names remain compatibility contracts until `FORM_SCHEMA.md`, `AUDIT.md`, and this file are updated together.

## Project-Owned Logic Files

| File | Role | Connected To |
| --- | --- | --- |
| `js/build-listing.js` | Legacy static-site builder for `/peluang-usaha/index.html`; fetches `FRANCHISOR` and `UNCLAIMED`, merges/sorts tiers, renders listing cards with a neutral `Info Franchise` detail CTA, and regenerates `json/unclaimed-brands.json`. | `templates/peluang-usaha-tpl.html`, `csv/franchisors.csv`, `csv/unclaimed.csv`, Google Sheets, `json/unclaimed-brands.json` |
| `js/build-details.js` | Static-site builder for individual `/peluang-usaha/{slug}/index.html` pages; injects brand fields, JSON-LD, breadcrumbs, unclaimed disclaimers, tabs, and sticky claim CTA. | `templates/detail-franchise-tpl.html`, Google Sheets, `/daftar?claim={slug}` |
| `js/build-sitemap.js` | Generates `sitemap-complete.xml` from franchisor and unclaimed brands. | Google Sheets, CSV fallback, generated detail URLs |
| `functions/get-franchises.js` | Cloudflare Function API for directory/claim-search reads. D1-first through the `franchise_db` binding, validates query params through the shared Function schema module, preserves `purpose=claim-search` sanitization, and keeps a Sheets read fallback only when D1 is unavailable or explicitly requested. | Cloudflare D1 `franchise_db`, `functions/_shared-schemas.js`, `js/form-02-claim-workflow.js`, legacy Sheets read fallback |
| `functions/form-submit.js` | Cloudflare Function API router for franchisee, franchisor, claim, and dev test-data submissions. Keeps the endpoint D1 binding check, base payload validation, Clerk/D1 role authorization, telemetry handling, and dispatch; write workflows live in form-submit helper modules. | `js/form-06-submit-validation.js`, `functions/_shared-schemas.js`, `functions/_form-submit-franchisee.js`, `functions/_form-submit-franchisor.js`, `functions/_form-submit-test-actions.js`, `functions/_form-submit-utils.js`, `functions/_clerk-auth.js`, `functions/_telemetry.js`, Cloudflare D1 `franchise_db` |
| `functions/_form-submit-franchisee.js` | Franchisee form-submit workflow. Checks duplicates and writes franchisee profiles, legacy source rows, and actor-aware audit events. | `functions/form-submit.js`, `functions/_form-submit-utils.js`, D1 `franchisee_profiles`, `legacy_source_rows`, `audit_events` |
| `functions/_form-submit-franchisor.js` | Franchisor and claim form-submit workflows. Writes/updates franchisor profiles, franchise listings, optional progressive form canonical fields for area/staff/setup/working-capital/BEP/omzet/profit ranges, packages, publications, claim records, legacy source rows, audit events, and static rebuild queue requests. | `functions/form-submit.js`, `functions/_form-submit-utils.js`, `functions/_site-publish-queue.js`, D1 `franchisor_profiles`, `franchises`, `franchise_packages`, `franchise_site_publications`, `franchise_claims`, `legacy_source_rows`, `audit_events`, `site_rebuild_requests`, `site_publish_state` |
| `functions/_form-submit-test-actions.js` | Staff/admin dev test-data workflows for creating unclaimed rows and clearing generated test rows without restoring anonymous or Sheets writes. | `functions/form-submit.js`, `functions/_form-submit-utils.js`, `functions/_site-publish-queue.js`, D1 public listing tables |
| `functions/_country-metadata.js` | Shared Functions country metadata adapter generated from the same supported-country contract used by forms and static rendering. Exposes country-code options, country-name normalization/display, dial-code lookup, WhatsApp digit ranges, and international-mobile formatting helpers. | `data/country-metadata.json`, `functions/_form-submit-utils.js`, `functions/_dashboard-utils.js`, `scripts/check-contact-parser.mjs`, `scripts/check-country-metadata.mjs` |
| `functions/_form-submit-utils.js` | Shared form-submit utilities for validation/duplicate/json responses, payload cleaning, text/number/money/WhatsApp normalization, deterministic ids, Jakarta timestamps, audit/legacy statements, claim source lookup, unique slug creation, and common franchise bind values. Franchisor listing writes default missing brand origin/target market to Indonesia/Indonesia, with non-Indonesia contact country codes prefilling brand origin through shared country metadata when explicit metadata is absent, and now maps progressive optional canonical fields into D1. | `functions/_form-submit-franchisee.js`, `functions/_form-submit-franchisor.js`, `functions/_form-submit-test-actions.js`, `functions/_shared-schemas.js`, `functions/_country-metadata.js`, D1 |
| `functions/profile-data.js` | Protected profile center API facade for `/profil`. GET requires Clerk/D1 auth then delegates profile response composition to `functions/_profile-read-model.js`. POST dispatches validated account/profile/listing/location edits, public role additions, saved-opportunity save/remove, franchisee inquiry creation, franchisor lead status updates, premium order creation/renewal, and premium payment confirmation. Account/role, franchisee actions, franchisor/listing/location/lead actions, read model, schemas, utilities, recommendations, listing patch construction, and Premium workflows are delegated to profile helper modules. | `src/pages/profil/index.astro`, `js/profile-page.js`, `js/profile-premium.js`, `functions/_profile-read-model.js`, `functions/_profile-account.js`, `functions/_profile-franchisee-actions.js`, `functions/_profile-franchisor-actions.js`, `functions/_profile-schemas.js`, `functions/_profile-utils.js`, `functions/_profile-recommendations.js`, `functions/_profile-listing-patch.js`, `functions/_profile-premium.js`, `functions/_clerk-auth.js`, `functions/_telemetry.js`, D1 `users`, `user_roles`, `franchisee_profiles`, `franchisor_profiles`, `franchises`, `franchise_locations`, `locations`, `franchise_claims`, `franchise_leads`, `franchise_saved_opportunities`, `premium_orders`, `premium_payment_confirmations`, `franchise_subscriptions`, `franchise_product_events`, `operation_events`, `audit_events`, `site_rebuild_requests` |
| `functions/_profile-schemas.js` | Zod mutation schemas for `/profile-data`, including account, franchisee/franchisor profile, owner listing, owner listing locations, public role add, inquiry, saved opportunity, lead status, and Premium order/confirmation actions. | `functions/profile-data.js`, `functions/_profile-premium.js`, Zod |
| `functions/_profile-utils.js` | Shared profile API utilities for D1 binding checks, JSON/validation responses, audit statements, text/number/WhatsApp normalization, id generation, missing saved-table detection, and Clerk name/email helpers. | `functions/profile-data.js`, `functions/_profile-listing-patch.js`, `functions/_profile-recommendations.js`, `functions/_site-publish-queue.js` |
| `functions/_profile-recommendations.js` | Franchisee recommendation helper for `/profil`: queries published listings, merges privacy-safe product event counts, scores by budget/category/verification/event activity, and returns reason labels. | `functions/profile-data.js`, `functions/_analytics.js`, `functions/_profile-utils.js`, `functions/_site-publish-queue.js`, D1 `franchises`, `franchise_site_publications`, `franchise_product_events` |
| `functions/_profile-listing-patch.js` | Owner listing mutation helper for `/profile-data`: extracts allowed listing fields, normalizes money/number/text values, and builds the SQL update statement used by rate-limited owner edits. | `functions/profile-data.js`, `functions/_profile-utils.js`, D1 `franchises` |
| `functions/_location-writes.js` | Shared manual location write helper for profile and dashboard actions. Normalizes owner/admin city rows, deduplicates by location type and city slug, preserves deterministic `locations`/`franchise_locations` ids for `owner_profile` rows, builds replace-and-insert D1 statements, and exposes compact audit summaries. | `functions/_profile-franchisor-actions.js`, `functions/_dashboard-actions.js`, D1 `locations`, D1 `franchise_locations` |
| `functions/_google-contacts.js` | Dashboard outreach Google Contacts helper. Builds People API contact payloads from the current unclaimed outreach queue, retrieves the staff member's linked Google OAuth access token from Clerk, checks existing contacts through Google People API `people:searchContacts`, skips duplicate phone numbers, calls `people:batchCreateContacts` for remaining contacts, and returns setup-required errors with `/dashboard/#google-contacts-setup` documentation links when Google Contacts scope/token is missing. | `functions/dashboard-data.js`, `functions/_dashboard-queries.js`, `js/dashboard-operations.js`, Clerk Google OAuth, Google People API |
| `functions/_profile-account.js` | Profile account/role action module. Updates Clerk identity and D1 account/profile identity fields for account edits, syncs Clerk metadata from D1 roles, and lets public users add self-service `franchisee` or `franchisor` roles only. | `functions/profile-data.js`, `functions/_clerk-auth.js`, `functions/_profile-utils.js`, `functions/_site-publish-queue.js`, Clerk, D1 `users`, `user_roles`, `franchisee_profiles`, `franchisor_profiles`, `audit_events` |
| `functions/_profile-franchisee-actions.js` | Profile franchisee action module. Updates franchisee profile preferences, creates one-click franchise inquiries with optional comparison/calculator buyer context, saves/removes D1 saved opportunities, returns missing-profile CTA metadata, and records product events. | `functions/profile-data.js`, `functions/_analytics.js`, `functions/_profile-utils.js`, `functions/_site-publish-queue.js`, D1 `franchisee_profiles`, `franchise_leads`, `franchise_saved_opportunities`, `franchise_product_events`, `audit_events` |
| `functions/_profile-franchisor-actions.js` | Profile franchisor action module. Updates franchisor profile fields, enforces owner listing edit throttling, writes owner listing location overrides through shared location-write helpers, queues public rebuilds for owner listing/location edits, loads publication distribution and location rows for owned listings with chunked ID queries to stay under D1 bind limits, and updates owner lead status. | `functions/profile-data.js`, `functions/_profile-listing-patch.js`, `functions/_profile-utils.js`, `functions/_location-writes.js`, `functions/_site-publish-queue.js`, D1 `franchisor_profiles`, `franchises`, `franchise_locations`, `locations`, `franchise_site_publications`, `network_sites`, `franchise_leads`, `audit_events`, `site_rebuild_requests` |
| `functions/_profile-read-model.js` | Profile GET/read-model module. Builds the `/profile-data` response payload, including current user summary, completion flags, franchisee/franchisor profiles, owned listings with publication distribution/structured locations/edit lock state, claims, recommendations, saved opportunities, inquiry history, franchisor leads, premium membership state, and owner analytics. Also exports franchisee loader callbacks used by franchisee action handlers. | `functions/profile-data.js`, `functions/_profile-franchisee-actions.js`, `functions/_profile-franchisor-actions.js`, `functions/_profile-premium.js`, `functions/_profile-owner-analytics.js`, `functions/_profile-recommendations.js`, `functions/_profile-utils.js`, `functions/_site-publish-queue.js`, D1 profile/listing/location/lead/saved opportunity/premium tables |
| `functions/_profile-owner-analytics.js` | Owner analytics helper for `/profil`. Aggregates `franchise_product_events` for owned listings into total and 30-day views, saves, inquiries, contact clicks, and view-to-inquiry conversion rates, returning empty metrics if the analytics table is unavailable. | `functions/_profile-read-model.js`, D1 `franchise_product_events`, `franchises` |
| `functions/_premium.js` | Shared premium membership helper for plan constants, yearly price, renewal/expiry windows, included network site ids/domains, fallback BCA payment instructions, optional QRIS image instructions, unique transfer code generation, premium order/confirmation/subscription/publication ids, submitted amount normalization, canonical URL generation, and discount-aware payable amount calculation. | `functions/_profile-premium.js`, `functions/_dashboard-actions.js`, D1 `premium_orders`, `premium_payment_confirmations`, `franchise_subscriptions`, `franchise_site_publications` |
| `functions/_premium-ops.js` | Compatibility facade that re-exports focused Premium operation modules so existing callers keep their import path. | `functions/_premium-settings.js`, `functions/_premium-notifications.js`, `functions/_premium-lifecycle.js`, `functions/_premium-readiness.js`, `functions/_premium-ops-utils.js` |
| `functions/_premium-settings.js` | Premium settings/pricing module. Reads admin-managed payment methods/settings, provides the manual BCA fallback, computes multi-brand discounts, and returns date-gated public promo ribbon settings including the dashboard-configured per-day view cap. | `functions/_premium-ops.js`, `functions/_profile-premium.js`, `functions/_dashboard-queries.js`, `functions/_dashboard-actions.js`, `functions/premium-promo.js`, D1 `payment_methods`, `premium_settings` |
| `functions/_premium-notifications.js` | Premium event and notification module. Records funnel events, owner/admin in-app notifications, queued payment/status emails, queue summaries/recent rows, and notification feeds. | `functions/_premium-ops.js`, `functions/_profile-premium.js`, `functions/_dashboard-queries.js`, `functions/_dashboard-actions.js`, `functions/premium-event.js`, D1 `premium_funnel_events`, `premium_notifications`, `notification_email_queue`, `users`, `user_roles` |
| `functions/_premium-lifecycle.js` | Premium lifecycle module. Loads upcoming expiries and annual reports, queues renewal reminders, queues annual/grace emails, expires subscriptions after grace, hides Premium network publications when needed, writes audit events, and queues static rebuilds. | `functions/_premium-ops.js`, `functions/_premium-email-worker.js`, `functions/_premium-settings.js`, `functions/_premium-notifications.js`, `functions/_site-publish-queue.js`, D1 `franchise_subscriptions`, `premium_subscription_reminders`, `premium_grace_notifications`, `premium_annual_reports`, `notification_email_queue`, `franchise_product_events`, `franchise_leads`, `franchise_site_publications` |
| `functions/_premium-readiness.js` | Premium readiness scoring helper for listing completeness checks shown to owners/admins. | `functions/_premium-ops.js`, `functions/_profile-premium.js`, `functions/_dashboard-queries.js` |
| `functions/_premium-ops-utils.js` | Small shared utility module for Premium operation text normalization, ids, JSON safety, dates, clamping, safe D1 reads, and email HTML escaping. | `functions/_premium-settings.js`, `functions/_premium-notifications.js`, `functions/_premium-lifecycle.js`, `functions/_premium-readiness.js`, `functions/_premium-email-worker.js` |
| `functions/_premium-email-worker.js` | Premium email worker service. Runs Premium lifecycle jobs, queues renewal reminders, sends due queued emails through Resend when `RESEND_API_KEY` is configured, stores provider message ids, and marks failed rows with retry backoff. | `functions/premium-email-worker.js`, `functions/_premium-ops.js`, Cloudflare env `RESEND_API_KEY`, `PREMIUM_EMAIL_FROM`, `PREMIUM_EMAIL_REPLY_TO`, D1 `notification_email_queue`, `premium_subscription_reminders`, `premium_grace_notifications`, `premium_annual_reports`, `franchise_subscriptions` |
| `functions/_profile-premium.js` | Profile-owned premium workflow module. Validates premium order/confirmation payloads, creates unique-code transfer orders and 30-day renewal orders with admin-managed multi-brand discounts when enabled, confirms manual payments with optional receipt asset ids, loads membership state with payment instructions/readiness/notifications using chunked owned-listing queries, records Premium funnel events, and creates owner/admin in-app notifications plus queued email notifications. | `functions/profile-data.js`, `functions/_premium.js`, `functions/_premium-ops.js`, `functions/_dashboard-utils.js`, D1 `premium_orders`, `premium_payment_confirmations`, `franchise_subscriptions`, `franchise_assets`, `notification_email_queue` |
| `functions/premium-event.js` | Public coarse Premium funnel endpoint for `/premium` page views, CTA clicks, promo ribbon views, and promo ribbon clicks. Stores no IP/user-agent data and degrades silently if analytics storage is unavailable. | `js/premium-page.js`, `js/site-promo-bar.js`, `functions/_premium-ops.js`, D1 `premium_funnel_events` |
| `functions/premium-promo.js` | Public Premium promo endpoint. Reads active dashboard-managed promo/event settings, date-gates the campaign, returns only safe ribbon data plus the per-day view cap for public pages, and sends cache headers so public promo checks do not over-invoke Pages Functions. | `js/site-promo-bar.js`, `functions/_premium-ops.js`, D1 `premium_settings` |
| `functions/premium-email-worker.js` | Protected scheduled Premium email endpoint. Requires `PREMIUM_EMAIL_WORKER_SECRET`, calls the worker service, logs operation events, and is intended to be triggered by GitHub Actions or a manual authenticated POST. | `.github/workflows/premium-email-worker.yaml`, `functions/_premium-email-worker.js`, `functions/_telemetry.js`, Cloudflare env `PREMIUM_EMAIL_WORKER_SECRET` |
| `functions/premium-receipt-upload.js` | Protected proof-of-payment upload API for Premium confirmations. Requires Clerk/D1 auth, verifies the order belongs to the user, accepts JPG/PNG/WebP/PDF up to 6 MB, stores the file in R2, records `franchise_assets`, and writes an audit event. | `js/profile-page.js`, `functions/_clerk-auth.js`, R2 `FRANCHISE_ASSETS`, D1 `premium_orders`, `franchise_assets`, `audit_events` |
| `functions/payment-method-upload.js` | Admin-only QRIS image upload API for dashboard payment settings. Requires D1 `admin`, accepts JPG/PNG/WebP up to 3 MB, stores the image in R2 under `payment-methods/{code}/`, returns the public URL, and writes an audit event. | `js/dashboard-premium-operations.js`, `functions/_clerk-auth.js`, R2 `FRANCHISE_ASSETS`, D1 `audit_events`, `payment_methods` |
| `js/profile-premium.js` | Browser helper/renderer for `/profil` Premium UI state: chooses the current active subscription when renewal rows exist, formats expiry dates, labels Premium order statuses, exposes the 30-day renewal-window check, and renders the Membership tab payment/readiness/notification UI including bank details, exact payable amount, optional QRIS image, proof upload, and confirmation form used by `js/profile-page.js`. | `src/pages/profil/index.astro`, `js/profile-page.js`, `css/profile-premium.css` |
| `functions/profile-upload.js` | Protected owner media upload API for `/profil` listing assets. Requires Clerk/D1 auth, verifies listing ownership, validates logo/cover image and proposal PDF file types/sizes, writes files to R2, records assets, updates the public media field, queues static rebuilds, and schedules digital-PDF knowledge extraction without delaying the upload response. | `js/profile-page.js`, `functions/_proposal-knowledge.js`, `functions/_clerk-auth.js`, `functions/_site-publish-queue.js`, R2 `FRANCHISE_ASSETS`, D1 `franchise_assets`, `franchise_asset_knowledge`, `listing_edit_suggestions`, `franchises`, `site_rebuild_requests` |
| `functions/_proposal-knowledge.js` | Worker-compatible digital PDF/OCR knowledge extractor. Uses UnPDF to preserve selectable proposal text, sanitizes known source-noise watermarks before storage/display, deterministically identifies outlet/location, area/staff/setup, investment/fees, BEP, omzet/profit, royalty, target-market, and support candidates, filters out fields that already have canonical values, stores auditable extraction data, and creates pending admin-review suggestions instead of overwriting listings when a valid D1 user actor is available. It treats franchise fee/license fee/biaya lisensi/biaya franchise/biaya kemitraan/biaya join/joining fee/investasi kemitraan as one `fee_license_idr` concept, does not map ambiguous `paket kemitraan` to that field, and only extracts `total_investment_idr` from explicit total/nilai investasi wording to avoid false-positive totals from single fee rows. | `functions/profile-upload.js`, `functions/_ocr-job-runner.js`, `scripts/enrich-ocr-structured-data.ts`, `migrations/0019_proposal_knowledge.sql`, D1 `franchise_asset_knowledge`, `listing_edit_suggestions`, UnPDF |
| `functions/_ocr-provider-config.js` | Admin-only OCR provider configuration module. Returns metadata plus boolean key/secret presence without selecting credential values on dashboard reads, validates activation requirements from shared OCR provider metadata, preserves blank password fields, supports explicit credential clearing, encrypts saved credentials through `_ocr-credential-crypto.js` with external secret `OCR_KEY`, updates admin-controlled priority/credential settings while preserving seeded quota/free-limit/rate-limit metadata, exposes cooldown status and source-linked provider limit details, provides an explicit enable/disable toggle action separate from credential autosave, and audits only non-secret state. | `functions/dashboard-data.js`, `functions/_dashboard-schemas.js`, `functions/_ocr-credential-crypto.js`, `src/lib/ocr-provider-metadata.js`, `migrations/0020_ocr_provider_configs.sql`, `migrations/0022_ocr_provider_rate_limits.sql`, `migrations/0029_ocr_provider_actual_limits.sql`, D1 `ocr_provider_configs`, Cloudflare Pages `OCR_KEY`, `audit_events` |
| `functions/_ocr-scheduler-config.js` | Admin-only OCR scheduler configuration, preflight, and dispatch module. Returns masked third-party scheduler provider state, stores scheduler API tokens encrypted with `OCR_KEY`, preflights Upstash QStash with a harmless `/ocr-worker` `{ preflight: true }` message before large batch creation, enables QStash delayed HTTP delivery to `/ocr-worker`, writes structured scheduler trigger status/ETA fields into `ocr_batch_runs`, and leaves cron-job.org/Inngest/Trigger.dev credentials configurable for external trigger workflows without putting provider keys in code or env. | `functions/dashboard-data.js`, `functions/ocr-worker.js`, `functions/_ocr-credential-crypto.js`, `migrations/0023_ocr_batch_scheduler.sql`, `migrations/0025_ocr_batch_scheduler_timing.sql`, D1 `ocr_scheduler_configs`, `ocr_batch_runs`, Cloudflare Pages `OCR_KEY`, `OCR_SECRET` |
| `functions/_ocr-credential-crypto.js` | OCR credential envelope helper. Uses AES-GCM with provider/field additional authenticated data and a root key held outside D1 (`OCR_KEY`), encrypts new dashboard OCR provider/scheduler credential values, preserves existing encrypted values, clears explicit removals, and re-encrypts legacy plaintext values on next save. | `functions/_ocr-provider-config.js`, `functions/_ocr-scheduler-config.js`, `scripts/check-ocr-provider-config.ts`, Cloudflare Pages `OCR_KEY` |
| `functions/_ocr-provider-adapters.js` | OCR provider adapter boundary. Decrypts provider credentials with `OCR_KEY`, normalizes provider responses into plain text, and implements adapters for OCR.Space, Azure Vision, Cloudflare Workers AI, Google Vision, Groq Vision, AWS Textract, Veryfi, Mindee, PDF.co, and API4AI without logging secrets. | `functions/_ocr-job-runner.js`, `functions/_ocr-credential-crypto.js`, D1 `ocr_provider_configs`, external OCR provider APIs |
| `functions/_ocr-quota-policy.js` | OCR quota/cap policy module. Computes dashboard/worker combined quota from active providers with known free limits, reports account-specific providers separately, applies optional `OCR_WORKER_DAILY_CAP` as a safety ceiling, determines when exhausted providers can re-enter after reset, resets per-provider counters for daily/monthly/compute periods, prepares provider quota eligibility before assignment, and builds quota increment statements after successful OCR calls. | `functions/_ocr-job-runner.js`, `functions/ocr-worker.js`, `scripts/check-ocr-job-runner.ts`, D1 `ocr_provider_configs`, D1 `ocr_provider_usage_events`, optional env `OCR_WORKER_DAILY_CAP` |
| `functions/_ocr-run-lease.js` | Short-lived dashboard continuous-OCR lease module. Stores one active `dashboard_continuous` lease in D1, lets admins acquire/heartbeat/release it, masks active ownership for dashboard display, and validates lease ownership before multi-job dashboard OCR chunks run. | `functions/dashboard-data.js`, `functions/_ocr-job-runner.js`, `migrations/0027_ocr_run_leases.sql`, D1 `ocr_run_leases`, `audit_events` |
| `functions/_ocr-batch-runs.js` | OCR persisted batch-run module. Runs scheduler preflight before creating large batches, creates up to 100-job OCR batches only after preflight succeeds, assigns `batch_id` to pending jobs, refreshes batch progress including `paused_rate_limit`/`paused_quota` and final `no_text` rows, marks scheduler-overdue queued/running batches as failed with an actionable retry/dashboard-run message, excludes only providers whose cooldown window is still active from runnable-provider checks, keeps succeeded jobs untouched on batch retry while returning failed and stale running jobs to pending before scheduler redispatch, masks structured scheduler trigger timing fields for dashboard output, and delegates third-party trigger dispatch to `_ocr-scheduler-config.js`. | `functions/dashboard-data.js`, `functions/_ocr-job-runner.js`, `functions/_ocr-scheduler-config.js`, `migrations/0023_ocr_batch_scheduler.sql`, `migrations/0024_ocr_batch_pause_statuses.sql`, `migrations/0025_ocr_batch_scheduler_timing.sql`, `migrations/0026_ocr_no_text_status.sql`, D1 `ocr_batch_runs`, `ocr_jobs`, `ocr_provider_configs` |
| `functions/_ocr-job-actions.js` | Admin-only OCR dashboard job action module. Owns direct retry/run for a single failed/needs-review/no-text job, manual no-text resolution after image review, and batch retry of failed OCR jobs, while delegating actual OCR execution back to `_ocr-job-runner.js` and shared pending-state writes to `_ocr-job-claiming.js`. | `functions/dashboard-data.js`, `functions/_ocr-job-runner.js`, `functions/_ocr-job-claiming.js`, `functions/_dashboard-utils.js`, D1 `ocr_jobs`, `audit_events` |
| `functions/_ocr-job-claiming.js` | OCR pending-job claiming and retry-state helper. Selects pending jobs in franchise-first order with optional `job_id`/`batch_id` scopes, marks claimed jobs running, releases unprocessed claimed jobs back to pending when rate/quota pauses happen, provides the shared retry statement, and centralizes small OCR job text/error helpers. | `functions/_ocr-job-runner.js`, `functions/_ocr-job-actions.js`, D1 `ocr_jobs`, `franchise_assets` |
| `functions/_ocr-job-runner.js` | OCR queue processor. Enqueues active image-based proposal assets, delegates bounded pending-job claiming/release/retry-state writes to `_ocr-job-claiming.js`, requires the D1 continuous-run lease for multi-job dashboard chunks without a batch id, carries `requested_by_user_id` into worker-claimed jobs so scheduled OCR writes do not use synthetic user IDs, prioritizes all pending proposal pages for one franchise before moving to another franchise, supports a one-asset dry-run action before broad backfills, runs one-job retry requests initiated by `_ocr-job-actions.js`, marks provider text-too-short outcomes as `needs_review` instead of provider failure, refreshes active batch progress and active run lease state during dashboard reads, delegates combined quota visibility, provider quota eligibility, reset-aware provider re-entry, and quota increments to `_ocr-quota-policy.js`, computes content hashes, reuses `ocr_content_cache`, applies provider short-window rate-limit/cooldown checks, pauses rate-limited/quota-limited batches by returning the active job to `pending` and releasing unprocessed claimed jobs, writes provider-quota exhaustion messages that suggest activating another provider or waiting for reset, rotates enabled providers by priority and can process bounded concurrent waves across multiple active providers, records attempts/usage, stores successful OCR text through the proposal-knowledge review contract, sanitizes OCR preview text defensively before dashboard output, returns expanded OCR result previews/links plus refreshed batch progress and the grouped OCR enrichment queue for the dashboard/worker, exposes server-side OCR result history search/filter with limit/offset pagination plus exact `asset_id` lookup for dashboard deep-links, and exposes server-side OCR job status filtering/pagination including unqueued proposal assets with a default/max page size of 120. | `functions/dashboard-data.js`, `functions/ocr-worker.js`, `functions/_ocr-job-actions.js`, `functions/_ocr-job-claiming.js`, `functions/_ocr-provider-adapters.js`, `functions/_ocr-batch-runs.js`, `functions/_ocr-run-lease.js`, `functions/_ocr-quota-policy.js`, `functions/_ocr-enrichment-review.js`, `functions/_proposal-knowledge.js`, `migrations/0021_ocr_job_queue.sql`, `migrations/0022_ocr_provider_rate_limits.sql`, `migrations/0023_ocr_batch_scheduler.sql`, `migrations/0024_ocr_batch_pause_statuses.sql`, `migrations/0026_ocr_no_text_status.sql`, `migrations/0027_ocr_run_leases.sql`, `migrations/0029_ocr_provider_actual_limits.sql`, D1 `ocr_jobs`, `ocr_batch_runs`, `ocr_run_leases`, `ocr_attempts`, `ocr_content_cache`, `ocr_provider_usage_events`, `franchise_asset_knowledge`, `listing_edit_suggestions` |
| `functions/_ocr-enrichment-review.js` | Grouped OCR enrichment review helper. Reads structured proposal candidates from `franchise_asset_knowledge`, filters them against current canonical listing values and editable-field rules, deduplicates candidate conflicts per franchise, normalizes short text candidates such as outlet type to consistent title case, returns source-linked queue rows for the OCR Results dashboard, and creates one pending `ocr_enrichment_bundle` edit suggestion per franchise for admin review. New OCR bundles store sanitized per-field source page/excerpt/image evidence under `old_value.__ocr_evidence`, while `suggested_value` remains the normal canonical-field payload used by approval. | `functions/dashboard-data.js`, `functions/_ocr-job-runner.js`, `functions/_dashboard-ocr-schemas.js`, `functions/_shared-schemas.js`, `functions/_proposal-evidence.js`, `js/dashboard-ocr-results.js`, D1 `franchise_asset_knowledge`, D1 `listing_edit_suggestions` |
| `functions/ocr-worker.js` | Protected OCR backfill endpoint. Requires `OCR_SECRET`, accepts harmless `{ preflight: true }` scheduler checks without running OCR, uses `_ocr-quota-policy.js` combined active-provider remaining quota for real runs instead of a hardcoded 100-job cap, treats optional `OCR_WORKER_DAILY_CAP` only as a safety ceiling, accepts optional `batch_id`, drains only small pending job chunks, asks `_ocr-scheduler-config.js` to schedule the next Upstash QStash delayed trigger only while the batch is still actively queued/running, marks scoped batches `paused_quota` instead of `failed` when combined provider quota/safety cap is reached, stops automatic redispatch for paused rate-limit/quota batches, and logs run summaries/failures to `operation_events`. Intended for third-party scheduler/queue providers after proposal assets are already queued. | `functions/_ocr-job-runner.js`, `functions/_ocr-quota-policy.js`, `functions/_ocr-scheduler-config.js`, `functions/_ocr-batch-runs.js`, `functions/_dashboard-utils.js`, `functions/_telemetry.js`, Cloudflare env `OCR_SECRET`, optional `OCR_WORKER_DAILY_CAP`, D1 `ocr_jobs`, `ocr_batch_runs`, `ocr_provider_configs`, `ocr_provider_usage_events`, `operation_events` |
| `functions/_proposal-pdf.js` | Server-side proposal PDF builder. Validates allowlisted proposal image hosts, fetches JPEG pages server-side so browser canvas CORS is not involved, builds a PDF attachment, and applies the Franchisee.id watermark only to the returned PDF. | `functions/proposal-download.js`, `scripts/check-proposal-download.ts`, public brochure download buttons |
| `functions/proposal-download.js` | Public proposal PDF download endpoint. POST creates a watermarked PDF from submitted proposal image URLs through `_proposal-pdf.js`; GET returns 405 JSON for direct route visits. | `functions/_proposal-pdf.js`, generated listing detail pages |
| `functions/_site-publish-queue.js` | Shared Pages Functions helper for public-page dirty queue writes. Exposes `SITE_FRANCHISEE_ID` and `siteRebuildStatements()` so D1 mutations can enqueue rebuild requests and update per-site publish state in the same batch. | `functions/form-submit.js`, D1 `site_rebuild_requests`, D1 `site_publish_state` |
| `functions/_clerk-auth.js` | Shared Clerk/D1 auth helper for Pages Functions. Verifies Clerk bearer tokens, fetches Clerk user data when full sync is needed, upserts D1 `users` by Clerk id or the same verified primary email, applies pending `email_role_grants`, assigns only self-assignable roles, checks D1 roles before writes, lets `admin` satisfy protected-role checks while keeping `staff` limited to staff-level access, pushes D1 role snapshots to Clerk metadata, and exposes a fast read-only path for already-synced dashboard reads that verifies the token then reads D1 user/roles without a Clerk user fetch or metadata write. | `@clerk/backend`, `functions/auth-sync.js`, `functions/form-submit.js`, `functions/dashboard-data.js`, `functions/clerk-webhook.js`, D1 `users`, `user_roles`, `email_role_grants` |
| `functions/auth-config.js` | Public same-origin config endpoint for Clerk publishable key. Keeps static HTML environment-neutral and accepts Astro-style `PUBLIC_CLERK_PUBLISHABLE_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, and `CLERK_PUBLISHABLE_KEY`, plus a committed public live-key fallback because Clerk publishable keys are browser-safe. | `js/auth-clerk.js`, Cloudflare env |
| `functions/auth-sync.js` | Authenticated Clerk-to-D1 user sync endpoint. Maps the active Clerk user into D1 and self-assigns `franchisee` or `franchisor` when requested. | `functions/_clerk-auth.js`, `js/auth-clerk.js`, D1 `users`, `user_roles` |
| `functions/clerk-webhook.js` | Clerk-to-D1 lifecycle sync endpoint. Verifies Clerk webhooks, upserts/deletes D1 users from Clerk `user.created`, `user.updated`, and `user.deleted` events, and records operation telemetry for webhook success/failure. | `@clerk/backend/webhooks`, `functions/_telemetry.js`, Cloudflare env `CLERK_WEBHOOK_SIGNING_SECRET`, D1 `users`, D1 `operation_events` |
| `functions/dashboard-data.js` | Thin protected Franchisee.id dashboard API router. GET uses the fast already-synced D1 auth path for lower-latency dashboard refreshes, falling back to full Clerk-to-D1 sync when the D1 user is missing/inactive or the required role is not yet visible. POST actions still use full auth sync before mutations. Requires D1 role `staff` or elevated `admin`, composes the dashboard read model, and dispatches validated listing/review/operations/Premium/OCR provider configuration, OCR scheduler configuration, provider toggles, dry-run, retry, no-text resolution, OCR result search, OCR enrichment bundle creation, persisted batch-run start, and OCR job actions. OCR provider/scheduler metadata is returned only to admin and never includes stored key/secret values; external OCR only runs through explicit admin dry-run/batch actions or the protected worker. | `functions/_clerk-auth.js`, `functions/_dashboard-schemas.js`, `functions/_dashboard-queries.js`, `functions/_dashboard-actions.js`, `functions/_ocr-provider-config.js`, `functions/_ocr-scheduler-config.js`, `functions/_ocr-enrichment-review.js`, `functions/_ocr-job-actions.js`, `functions/_ocr-job-runner.js`, `functions/_dashboard-utils.js`, `functions/_telemetry.js`, `src/pages/dashboard/index.astro`, dashboard client modules |
| `functions/_analytics.js` | Shared product-event helper for privacy-safe listing interaction events and recent aggregate counts used by profile recommendation scoring. | `functions/product-event.js`, `functions/profile-data.js`, D1 `franchise_product_events` |
| `functions/_telemetry.js` | Shared operation-event helper that logs API/webhook/upload failures without breaking the request if the telemetry table is not available yet. | `functions/dashboard-data.js`, `functions/profile-data.js`, `functions/profile-upload.js`, `functions/form-submit.js`, `functions/clerk-webhook.js`, D1 `operation_events` |
| `functions/product-event.js` | Public product-event endpoint for coarse listing interactions such as detail views and contact clicks. It does not store IP/user-agent and silently degrades if analytics storage is unavailable. | `js/product-events.js`, `functions/_analytics.js`, D1 `franchise_product_events` |
| `functions/_shared-schemas.js` | Shared Zod/schema constants for roles, source/form/test action enums, dashboard review decisions, editable listing fields, contact types, quality-check types, form submission payloads, `/get-franchises` query params, and listing/package/status normalization used by Pages Functions. The editable `fee_license_idr` label is `Biaya kemitraan awal` so franchise fee, license fee, biaya join, and partnership-fee wording map to one canonical field. | `functions/_dashboard-schemas.js`, `functions/form-submit.js`, `functions/get-franchises.js`, Zod |
| `functions/_dashboard-schemas.js` | Dashboard action schema facade, site constants, Premium/payment/email validation, shared editable-listing field definitions, field normalization, granular `approved_fields` validation for review approvals, and update statement builder for dashboard listing edits. OCR-specific dashboard action schemas are imported from `_dashboard-ocr-schemas.js` so the generic dashboard contract does not keep growing with OCR operations. | `functions/dashboard-data.js`, `functions/_dashboard-actions.js`, `functions/_dashboard-ocr-schemas.js`, `functions/_shared-schemas.js`, Zod |
| `functions/_dashboard-ocr-schemas.js` | OCR-specific dashboard Zod schemas. Validates OCR provider config/toggle payloads, scheduler config/toggle payloads, dry-run/enqueue/run batch bounds, persisted batch start/retry payloads, failed/needs-review retry payloads, manual no-text resolution payloads, OCR result search including exact `asset_id` lookup, OCR enrichment review bundle creation, and OCR job search statuses including final `no_text` while sharing OCR provider/scheduler key enums with `_dashboard-schemas.js`. | `functions/_dashboard-schemas.js`, `functions/_ocr-provider-config.js`, `functions/_ocr-scheduler-config.js`, `functions/_ocr-batch-runs.js`, `functions/_ocr-enrichment-review.js`, `functions/_ocr-job-runner.js`, Zod |
| `functions/_contact-normalization.js` | Converts existing listing/profile contact fields into normalized contact rows for phone, WhatsApp, email, website, address, and social links, and detects invalid public contact URLs. Phone normalization accepts Indonesian mobile multi-number strings, regional international mobile numbers for supported SEA/nearby Asia markets, Indonesian landlines, and call-center short numbers while keeping WhatsApp-only rows filtered to WhatsApp-capable numbers. | `functions/_quality-checks.js`, `functions/_dashboard-utils.js`, `functions/_country-metadata.js`, D1 `franchise_contacts` |
| `functions/_quality-checks.js` | Computes and refreshes persistent dashboard quality checks, upserts normalized contacts, resolves stale checks, and writes a refresh audit event. | `functions/_dashboard-actions.js`, `functions/_dashboard-queries.js`, `functions/_contact-normalization.js`, D1 `franchise_contacts`, D1 `franchise_quality_checks` |
| `functions/_dashboard-queries.js` | Dashboard read model for overview metrics, persisted data-quality warnings with live fallback, publish state, multi-site publication controls, unclaimed outreach queue plus outreach summary counts, pending claims, pending premium payment confirmations with proof/readiness, Premium funnel/payment/settings/report/notification/email queue operations, full editable listing snapshots with structured locations loaded through chunked ID queries, recent outreach, edit suggestions with document evidence attached by `_dashboard-review-evidence.js`, lead summary, product-event aggregates, operation telemetry, recent audit events, system health, and a local Cloudflare Free-plan traffic guardrail summary. | `functions/dashboard-data.js`, `functions/_dashboard-utils.js`, `functions/_dashboard-review-evidence.js`, `functions/_quality-checks.js`, `functions/_premium-ops.js`, D1 |
| `functions/_dashboard-review-evidence.js` | Dashboard review evidence helper. Enriches pending `proposal_extraction` and `ocr_enrichment_bundle` suggestions that lack complete embedded evidence by reading `franchise_asset_knowledge`, matching suggested fields back to structured proposal/OCR rows, and attaching `old_value.__ocr_evidence` with sanitized source excerpts, field-specific `basis` snippets, and source page/image URLs for Review OCR hover proof. | `functions/_dashboard-queries.js`, `functions/_proposal-evidence.js`, D1 `franchise_asset_knowledge`, D1 `franchise_assets` |
| `functions/_proposal-evidence.js` | Shared proposal/OCR evidence helper. Sanitizes proposal/OCR text, finds field-aware basis snippets for money, staff, area, royalty, and related candidates, returns excerpt plus exact basis text for dashboard highlighting, and intentionally leaves weak numeric matches without a basis so admins know to inspect the image before approval. | `functions/_dashboard-review-evidence.js`, `functions/_ocr-enrichment-review.js`, `functions/_proposal-knowledge.js` |
| `functions/_dashboard-actions.js` | Dashboard write workflows for manually confirmed WhatsApp outreach logging, guided listing edit suggestions, data-quality refresh, granular admin edit suggestion review through optional `approved_fields`, admin claim review, admin publication status updates, admin listing location updates through shared location-write helpers, admin payment-method updates, admin Premium settings updates, admin queued-email retry/cancel, and admin premium payment approval/rejection. Premium approval activates a one-year subscription, marks the listing premium, creates missing publication rows for the included network sites, publishes them, writes audit/events/notifications, and queues static rebuilds for public-page-affecting changes. | `functions/dashboard-data.js`, `functions/_dashboard-schemas.js`, `functions/_dashboard-queries.js`, `functions/_dashboard-utils.js`, `functions/_location-writes.js`, `functions/_quality-checks.js`, `functions/_site-publish-queue.js`, `functions/_premium.js`, `functions/_premium-ops.js`, D1 |
| `functions/_dashboard-utils.js` | Shared dashboard helper functions for JSON responses, audit statements, admin checks, JSON parsing, contact parsing/link generation, grouped counts, likely all-caps checks, and ids. Contact parsing distinguishes structured phone contacts from WhatsApp-capable outreach contacts. | `functions/_dashboard-queries.js`, `functions/_dashboard-actions.js`, `functions/_contact-normalization.js` |
| `functions/user-role.js` | Admin-only D1 role mutation endpoint. Assigns/removes D1 roles and then pushes the D1 role snapshot to Clerk metadata. | `functions/_clerk-auth.js`, D1 `user_roles`, Clerk `users.updateUserMetadata` |
| `functions/sync-clerk-metadata.js` | Admin-only repair/backfill endpoint for pushing D1 user/role state into Clerk metadata after manual SQL changes. | `functions/_clerk-auth.js`, D1 `users`, `user_roles`, Clerk metadata |
| `js/auth-clerk-debug.js` | Masked Clerk/auth diagnostics module. Owns persisted debug events, `window.FranchiseAuth.getDebugSnapshot()` backing helpers, Clerk/session summaries, token/key/session hints, browser storage/cookie key inspection, and debug value sanitization. Must load before `js/auth-clerk.js` on auth-enabled pages. | `js/auth-clerk.js`, `/dashboard`, `/sso-callback`, `/login`, `/register`, `/daftar` |
| `js/auth-clerk-ui.js` | Auth page renderer for custom login/register/recovery/verification panels. Owns the custom auth HTML template and Google icon markup so `js/auth-clerk.js` can focus on page/event behavior. | `js/auth-clerk.js`, `css/auth-clerk.css`, `/login`, `/dashboard`, `/sso-callback`, `/register`, `/daftar`, public templates |
| `js/auth-clerk-core.js` | Custom ClerkJS core runtime. Owns publishable-key config/fallbacks, script loading, Clerk instance initialization, shared created-session activation, OAuth redirect callback finalization, pending role/next storage, `/auth-sync`, token/header helpers, signed-in guards, and URL cleanup while preserving the `window.FranchiseAuth` API consumed by protected scripts. | `js/auth-clerk.js`, `js/auth-clerk-debug.js`, `/auth-config`, `/auth-sync`, `/clerk/clerk.browser.js`, `/login`, `/dashboard`, `/sso-callback`, `/register`, `/daftar`, public templates |
| `js/auth-clerk.js` | Custom auth page facade. Delegates diagnostics to `js/auth-clerk-debug.js`, rendering to `js/auth-clerk-ui.js`, and Clerk/session primitives to `js/auth-clerk-core.js`; owns login/register/recovery form mounting, auth panel messages, tab switching, staff dashboard login-only variant, public role selection, Google OAuth button coordination, email/password flows, and session-state rendering. Email/password, registration, and reset-password success paths activate the created Clerk session through the shared core helper before redirecting. Dashboard can mark its auth root with `data-auth-defer="true"` so the hidden login form does not mount or render a redundant logged-in state before the dashboard controller decides login is needed. | `js/auth-clerk-debug.js`, `js/auth-clerk-ui.js`, `js/auth-clerk-core.js`, `/login`, `/dashboard`, `/sso-callback`, `/daftar`, `js/form-06-submit-validation.js` |
| `js/auth-navbar.js` | Public navbar auth-state controller. On pages with legacy HFE nav menus, normalizes logged-out `Login`/`Daftar` links to `Masuk` and `Daftar Mitra` pointing at protected `/daftar/`, then replaces them after Clerk/D1 sync with Font Awesome account/logout icons, account name, D1 role badge, `/profil/` account link, and red icon-only logout. | `js/auth-clerk.js`, `css/auth-clerk.css`, `/login`, `/daftar`, `/profil`, franchise directory/detail templates |
| `js/shared-tooltip.js` | Shared custom tooltip runtime. Uses `data-fr-tooltip`, renders one body-level fixed tooltip so parent overflow cannot clip hints, shows on hover/focus instantly, upgrades supported legacy interactive `title` attributes by removing the browser tooltip, and observes dynamically inserted UI. | `css/shared-tooltip.css`, `/login`, `/daftar`, `/profil`, `/dashboard`, `/sso-callback`, franchise directory/detail templates, `js/profile-page.js`, `js/auth-navbar.js`, `src/lib/franchise-static.ts` |
| `css/auth-clerk.css` | Custom auth UI styling built on existing Astra/global colors and Font Awesome icon classes. Explicitly preserves `[hidden]` tab/panel behavior against legacy display rules, constrains inputs/buttons inside the auth panel under legacy CSS, and styles animated segmented auth tabs with readable active contrast, inline label/input rows, centered actions/notes/help links, CTA-backed auth messages, role-first registration controls, Google OAuth buttons, and compact public navbar account/logout controls. | `/login`, `/dashboard`, `/daftar`, franchise directory/detail templates |
| `css/shared-tooltip.css` | Shared tooltip styling for the body-level `fr-tooltip` layer and `data-fr-tooltip` triggers. Keeps tooltip hints instant, high-z-index, viewport-clamped, and independent from parent overflow. | `js/shared-tooltip.js`, auth/profile/dashboard/public generated pages |
| `css/opportunity-save.css` | Public save-opportunity styling for generated directory cards and detail pages. Directory cards use icon-only image-corner controls with shared tooltips; detail pages keep the larger save action and both surfaces can show CTA-backed feedback. | `js/opportunity-save.js`, `src/lib/franchise-static.ts`, franchise templates |
| `css/profile.css` | Base protected profile page styling. Owns the branded `/profil` yellow/black/cream shell, dark header, scoped readable auth-navbar account overrides, hero, next-best-action card, rounded side-tab layout/active states, panels, granular account/security rows, profile forms, identity lock notes, role add-on cards/modal, franchisee opportunity cards, budget-fit badges, shared chips/lists/notices/buttons, and base responsive collapse behavior while reusing the public site color language and Font Awesome cues. | `src/pages/profil/index.astro`, `js/profile-page.js`, `js/auth-navbar.js`, profile CSS modules |
| `css/profile-premium.css` | Profile Premium stylesheet module. Owns Membership tab card, Premium benefit/price grid, payment instruction rows, optional QRIS display, readiness checklist, Premium notifications, and related responsive stacking. | `src/pages/profil/index.astro`, `js/profile-premium.js`, `css/profile.css` |
| `css/profile-analytics.css` | Profile analytics stylesheet module. Owns owner analytics summary icon treatment, per-listing analytics cards, metric grid, and metric typography. | `src/pages/profil/index.astro`, `js/profile-analytics.js`, `css/profile.css` |
| `css/profile-franchisor.css` | Profile franchisor stylesheet module. Owns publication distribution chips, owner Area Listing editor, listing selector, owner media upload controls, franchisor lead inbox cards/status controls, and related responsive stacking. | `src/pages/profil/index.astro`, `js/profile-franchisor.js`, `js/profile-leads.js`, `css/profile.css` |
| `css/dashboard.css` | Dashboard base stylesheet for the app shell, sticky header, metric cards, tabs, panels, tables, forms, generic icon actions, shared text-pill actions, debug panel, and shared responsive behavior. Feature-heavy dashboard surfaces now live in dedicated CSS modules. | `src/pages/dashboard/index.astro`, `js/dashboard-admin.js`, `js/dashboard-utils.js`, dashboard feature CSS modules |
| `css/dashboard-auth.css` | Dashboard auth-state stylesheet. Owns the loading skeleton shown while Clerk/session/dashboard authorization is processing before the login form is shown. | `src/pages/dashboard/index.astro`, `js/dashboard-admin.js` |
| `css/dashboard-review.css` | Dashboard Review/Data Quality stylesheet. Owns guided edit field rows, full-width pending review tables, vertically stacked brand/actor/source cells, per-field approval checkbox pills, icon-led old-to-new field diff rows, OCR/general review source badges, OCR evidence excerpt/image-preview rows, reason wrapping, and the Area Listing editor responsive layout. | `src/pages/dashboard/index.astro`, `src/components/dashboard/DashboardReviewPanel.astro`, `js/dashboard-review.js` |
| `css/dashboard-operations.css` | Dashboard Operations/admin helper stylesheet. Owns full-width operation panels, publication status grids/cells, and compact checkbox rows. | `src/pages/dashboard/index.astro`, `src/components/dashboard/DashboardOperationsPanel.astro`, `js/dashboard-operations.js` |
| `css/dashboard-premium.css` | Dashboard Premium Operations stylesheet. Owns Premium operations grid layout, payment/settings forms, QRIS preview, and Premium-specific responsive rules. | `src/pages/dashboard/index.astro`, `src/components/dashboard/DashboardOperationsPanel.astro`, `js/dashboard-premium-operations.js` |
| `css/dashboard-ocr.css` | Dashboard OCR shell stylesheet kept as the stable base include for the OCR tab. Detailed OCR feature styling now lives in focused settings, execution, and results CSS modules loaded after it. | `src/pages/dashboard/index.astro`, `src/components/dashboard/DashboardOcrPanel.astro`, OCR CSS modules |
| `css/dashboard-ocr-settings.css` | Dashboard OCR settings stylesheet. Owns OCR guide-card navigation for Pengaturan, Eksekusi Job, Hasil OCR, and Review OCR, provider/scheduler credential form layout, provider metadata, stored credential badges, provider toggle/error/copy controls, and settings responsive behavior. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr-providers.js`, `js/dashboard-ocr-schedulers.js` |
| `css/dashboard-ocr-execution.css` | Dashboard OCR execution stylesheet. Owns execution toolbar/status, combined quota chip, job filters, compact icon-led page-size control, grouped job cards, compact icon-only job message pills, job status/actions, batch progress/countdown/actions, viewport-aware lazy hover image-preview overlay, and execution responsive behavior. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `js/dashboard-ocr-jobs.js`, `js/dashboard-ocr-batches.js`, `js/dashboard-ocr-worker.js` |
| `css/dashboard-ocr-results.css` | Dashboard OCR results stylesheet. Owns Hasil OCR filter controls, icon-only search/reset/load-more actions, the grouped OCR enrichment queue, franchise-card pagination, compact franchise result cards, per-page pager controls, OCR text preview blocks, result action links, and results responsive behavior. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr-results.js`, `js/dashboard-ocr.js` |
| `css/premium.css` | Public premium sales page styling. Owns `/premium` responsive hero, benefit grids, price/payment block, comparison rows, and CTA bands using the existing site color language and Font Awesome cues. | `src/pages/premium/index.astro` |
| `js/form-01-state-helpers.js` | Shared `window.FranchiseForm` state, constants, sanitizers, claim persistence, and franchisor draft persistence. Includes fallback country metadata plus helpers for dial-code-to-country and WhatsApp digit ranges when the public metadata JSON has not loaded yet. | All form modules, `localStorage`, claim search, autosave |
| `js/form-02-claim-workflow.js` | Claim-search data loading, autocomplete UI, claim-mode prefill, and claim-mode exit. | `json/unclaimed-brands.json`, `/get-franchises`, `window.openTab`, franchisor form |
| `js/form-03-navigation-steps.js` | Registration tab switching, animated tab-indicator positioning, and franchisor 5-step navigation/validation. Step-2 zero-modal confirmation now respects the simple `min_capital` baseline so optional investment breakdown fields do not create false warnings. `?admin` and legacy `mode=preview` bypass required-field step validation for admin QA navigation only. | `/daftar/index.html` onclick handlers, `form-utils.js`, `localStorage` |
| `js/form-04-calculation-city.js` | Investment/BEP calculation and city autocomplete loader. | `/json/data-kota-id.json` if present, remote city JSON fallback, form fields |
| `js/form-05-country-whatsapp.js` | Country-code dropdown rendering, flag fallback, public country metadata loading, and WhatsApp normalization. Supports international numbers entered with `+`, `00`, selected country code, or local national digits. | `json/country-metadata.json`, submit pipeline |
| `js/form-06-submit-validation.js` | Live validation binding, unified submit pipeline to `/form-submit`, Clerk bearer-token attachment through `window.FranchiseAuth`, inline/SweetAlert submit feedback with CTA links, safe `next` redirect handling after successful completion, and aggressive franchisor autosave triggers. | `functions/form-submit.js`, `js/auth-clerk.js`, `form-utils.js`, `form-01-state-helpers.js` |
| `js/form-07-init.js` | DOMContentLoaded bootstrap for form modules, progressive franchisor field initialization, HAKI conditional display, collapsed international-origin fields, state restoration, role deep-link tab selection, Clerk login enforcement for `/daftar`, Clerk/D1 identity locking for franchisee/franchisor name/email/PIC fields using shared custom tooltip hints, completed-profile redirect to `/profil/`, and compatibility globals. | All form modules, `/daftar/index.html`, `js/auth-clerk.js`, `/profile-data`, `js/shared-tooltip.js` |
| `js/form-10-progressive-franchisor.js` | Progressive franchisor form helper. Auto-opens follow-up groups when related baseline fields are filled, calculates `total_investment_value` from either simple `min_capital` or optional fee breakdown fields, and syncs support-system checkbox chips plus free-text notes into the existing `support_system` submit field. | `/daftar/index.html`, `js/form-07-init.js`, `js/form-06-submit-validation.js`, `css/form-franchise/03-form-core.css` |
| `js/form-08-franchisee-steps.js` | Separate 2-step franchisee form navigation and validation. `?admin` and legacy `mode=preview` bypass required-field step validation for admin QA navigation only. | `franchiseeForm`, `localStorage` key `franchisee_form_step` |
| `js/form-09-test-data-generator.js` | Dev-only `?dev=1` form filler and test data cleanup UI. | `/form-submit`, form modules, Google Sheets test-data helpers |
| `js/form-utils.js` | Shared formatting, validation, UI helpers, and progress helpers exposed as globals. WhatsApp validation reads country-specific national-number lengths from shared form country metadata so supported SEA/nearby Asia franchisor numbers are accepted. | Form modules, `js/form-01-state-helpers.js`, and `/daftar/index.html` |
| `js/form-franchise.js` | Legacy non-executing shim. Do not add runtime logic here. | Historical compatibility only |
| `js/profile-page.js` | Client controller facade for `/profil`. Requires Clerk session, fetches `/profile-data`, controls side tabs, renders summary, syncs old local saved opportunities after login, creates one-click franchise inquiries, submits account/profile/listing/location mutations, adds missing public roles, creates premium transfer orders, uploads receipt files through `/premium-receipt-upload`, submits payment confirmations, uploads owner listing media through `/profile-upload`, updates franchisor lead status, and exposes admin/staff dashboard shortcut only when D1 roles allow it. Account, role, franchisee/franchisor, lead, analytics, Premium membership, saved-opportunity storage, and shared UI helpers are delegated to profile modules. | `src/pages/profil/index.astro`, `functions/profile-data.js`, `functions/profile-upload.js`, `functions/premium-receipt-upload.js`, `functions/_profile-premium.js`, `js/profile-ui-utils.js`, `js/profile-account.js`, `js/profile-roles.js`, `js/profile-leads.js`, `js/profile-analytics.js`, `js/profile-franchisee.js`, `js/profile-franchisor.js`, `js/profile-opportunities.js`, `js/profile-premium.js`, `js/auth-clerk.js`, `css/profile.css`, `css/profile-premium.css`, `css/profile-analytics.css`, `css/profile-franchisor.css`, ClerkJS user password APIs |
| `js/profile-ui-utils.js` | Shared `/profil` browser UI helpers for HTML/attribute escaping, profile field markup, empty states, Rupiah formatting, status labels, Clerk account helper copy, and form busy/message handling. | `js/profile-page.js`, `js/profile-account.js`, `js/profile-roles.js`, `js/profile-leads.js`, `js/profile-premium.js` |
| `js/profile-account.js` | `/profil` Account tab renderer for granular name/email edit rows and password add/change UI. Submit behavior remains in `js/profile-page.js`. | `js/profile-page.js`, `js/profile-ui-utils.js`, ClerkJS user password APIs |
| `js/profile-roles.js` | `/profil` missing-role CTA and confirmation modal renderer for additive franchisee/franchisor access. Submit behavior remains in `js/profile-page.js`. | `js/profile-page.js`, `functions/profile-data.js` |
| `js/profile-leads.js` | `/profil` franchisor lead list/card renderer with email, WhatsApp, listing shortcuts, and status select markup. Status update behavior remains in `js/profile-page.js`. | `js/profile-page.js`, `js/profile-ui-utils.js` |
| `js/profile-analytics.js` | `/profil` owner analytics renderer for franchisor/admin/staff users. Renders 30-day summary cards and per-listing views, saves, inquiries, contact clicks, total counts, and conversion rate from `owner_analytics`. | `js/profile-page.js`, `functions/_profile-owner-analytics.js`, `css/profile.css`, `css/profile-analytics.css` |
| `js/profile-opportunities.js` | `/profil` saved-opportunity browser helper for localStorage keying, local/server merge, lookup, and inquiry-history checks. Server sync and save/remove POST behavior remains in `js/profile-page.js`. | `js/profile-page.js`, `/profile-data`, browser `localStorage` |
| `js/profile-franchisee.js` | `/profil` franchisee renderer for Minat Usaha and Peluang Saya, including recommendation cards, saved opportunity cards, and inquiry history. Submit/save/inquiry behavior remains in `js/profile-page.js`. | `js/profile-page.js`, `js/profile-ui-utils.js`, `js/profile-opportunities.js` |
| `js/profile-franchisor.js` | `/profil` franchisor renderer for Data Brand, Listing Brand, publication distribution chips, owner Area Listing editor, owner media upload controls, and claim history. Submit/upload behavior remains in `js/profile-page.js`. | `js/profile-page.js`, `js/profile-ui-utils.js`, `functions/_profile-franchisor-actions.js`, `css/profile-franchisor.css` |
| `js/opportunity-save.js` | Public save-opportunity and premium inquiry controller for generated franchise directory cards and detail pages. On click, ensures a Clerk session, redirects anonymous users to `/login?next=...`, posts D1 saved-opportunity save/remove and premium detail inquiry actions to `/profile-data`, updates icon-only/card/detail button states, records successful save/remove/inquiry events, and renders CTA-backed feedback when the API returns next-action metadata. | `css/opportunity-save.css`, `src/lib/franchise-static.ts`, `src/lib/franchise-premium-detail.ts`, `functions/profile-data.js`, `js/product-events.js`, public franchise templates |
| `js/product-events.js` | Public privacy-safe product-event client. Records sampled/deduped detail page views and deduped contact clicks through `/product-event`, applies a per-browser daily public-event budget to protect the Workers Free request limit, and exposes `window.FranchiseProductEvents.record()` so successful save/remove actions can be counted. | `functions/product-event.js`, `js/opportunity-save.js`, franchise directory/detail templates |
| `scripts/import-csv-to-d1.ts` | TypeScript/Zod importer facade that validates `csv/franchisors.csv`, `csv/unclaimed.csv`, and `csv/franchisee.csv`, maps rows into the D1 import plan, preserves strict `UNCLAIMED` claim-search sanitization, and prints/applies import stats. Reusable CSV parsing, SQL serialization, id/slug/hash, package/publication/legacy source, and remote apply helpers live in `scripts/import-csv-utils.ts`. | `scripts/import-csv-utils.ts`, `src/lib/shared-schemas.ts`, `migrations/0001_initial_network_schema.sql`, `/csv`, `.context/d1-import-franchise-data.sql`, Cloudflare D1 `franchise_db` |
| `scripts/import-csv-utils.ts` | Shared TypeScript utility module for the CSV-to-D1 importer: quote-aware CSV loading/parsing, SQL value serialization/builders, deterministic ids/hashes, slug/text/number normalization, package/publication/legacy source builders, stats helpers, claim-search row filters, and remote cfman/wrangler apply execution. | `scripts/import-csv-to-d1.ts`, `/csv`, Cloudflare D1 `franchise_db` |
| `scripts/shared-csv.cjs` | Shared quote-aware CSV parser for legacy Node builders so quoted commas/newlines do not shift columns. | `js/build-listing.js`, `js/build-sitemap.js`, `/csv` |
| `scripts/build-d1-franchise-pages.ts` | TypeScript/Zod D1-backed public page bridge facade. Reads and validates the full public listing fact contract—including outlet/location/cost/contract/area/staff/setup/working-capital/BEP range/omzet range/profit range/royalty fields—handles Cloudflare D1 HTTP/Wrangler/JSON loading, writes the Astro snapshot and claim-search JSON by default, and keeps legacy root bridge page writing/pruning behind explicit `--write-bridge-pages` so normal builds do not create mass generated source diffs. Dry runs still render bridge pages in memory for validation. | `scripts/d1-page-renderer.ts`, `src/lib/shared-schemas.ts`, `templates/peluang-usaha-tpl.html`, `templates/detail-franchise-tpl.html`, `json/d1-franchise-static-data.json`, `json/d1-generated-pages-manifest.json`, `json/unclaimed-brands.json`, `/peluang-usaha`, Cloudflare D1 HTTP API, Wrangler |
| `scripts/enrich-ocr-structured-data.ts` | Replayable remote D1 OCR enrichment script. Fetches extracted `franchise_asset_knowledge` rows plus current canonical listing fields, reruns the shared proposal sanitizer/extractor, writes idempotent SQL updates for sanitized `source_text` and missing-field `structured_data`, records an operation event only when rows change, and can apply the SQL through cfman/Wrangler. | `functions/_proposal-knowledge.js`, `.context/ocr-structured-enrichment-fetch.sql`, `.context/ocr-structured-enrichment.sql`, `package.json` `ocr:enrich:structured`, D1 `franchise_asset_knowledge`, D1 `operation_events` |
| `scripts/d1-page-renderer.ts` | Pure TypeScript renderer/helper module for the D1 public-page bridge. Builds listing card HTML, unclaimed claim-search items, detail pages, contact/social markup, JSON-LD/breadcrumb/disclaimer/tab/sticky sections, deterministic sorting, stable JSON hashes, generated HTML cleanup, and legacy link canonicalization. Shared detail quick-facts come from `src/lib/franchise-detail-summary.ts`; connected buyer-intent tab entries/shell come from `src/lib/franchise-detail-tabs.ts`; generated detail CSS/JS injection is reused from `src/lib/franchise-static-assets.ts` so flat bridge pages match Astro output. | `scripts/build-d1-franchise-pages.ts`, `src/lib/franchise-detail-summary.ts`, `src/lib/franchise-detail-tabs.ts`, `src/lib/franchise-static-assets.ts`, public franchise templates |
| `scripts/sync-franchise-locations.ts` | Repeatable location backfill script. Reads the D1 static franchise snapshot, infers supported city/service-area/head-office/outlet matches, writes SQL for `locations` and generated `franchise_locations` rows, and can apply it remotely through cfman/wrangler. | `src/lib/franchise-location-normalization.ts`, `src/lib/shared-schemas.ts`, `json/d1-franchise-static-data.json`, `.context/franchise-location-sync.sql`, D1 `locations`, D1 `franchise_locations`, `package.json` `locations:sync` |
| `scripts/migrate-blogspot-proposals-to-r2.mjs` | Operational migration script for legacy proposal images. Reads `proposal_url` image lists from the generated D1 static snapshot, extracts Blogspot/Blogger/googleusercontent image URLs, downloads them with brand/slug mapping, uploads each image to R2 through sequential `cfman wrangler r2 object put --remote`, writes idempotent D1 SQL for `franchise_assets`, updates `franchises.proposal_url` to first-party public URLs, and queues rebuild requests. Generated working files live under ignored `.context/proposal-r2-migration/`. | `json/d1-franchise-static-data.json`, Cloudflare R2 `franchise-assets`, D1 `franchise_assets`, D1 `franchises`, `site_rebuild_requests`, `package.json` `migrate:proposal-r2` |
| `scripts/copy-legacy-static.mjs` | Post-Astro build copier for the hybrid deployment. Copies legacy static HTML/assets/directories into `dist` without overwriting existing Astro output, copies `@clerk/clerk-js/dist` to `dist/clerk` for local browser auth loading, skips legacy duplicate directory/category routes, rewrites copied legacy HTML links from old archive URLs to canonical `/peluang-usaha` query URLs, and strips unused WordPress runtime snippets that call AnalyticsWP, emoji loading, LatePoint, or `/wp-admin/admin-ajax.php`. | `package.json` `copy:legacy-static`, Cloudflare Pages output `dist`, legacy root static export, `node_modules/@clerk/clerk-js/dist` |
| `scripts/check-profile-client.mjs` | Focused profile client regression check. Runs `node --check` on `/profil` browser modules and fails when stale undefined helper calls such as `selectedFranchise()` reappear. | `package.json` `profile:check`, `js/profile-page.js`, `js/profile-premium.js`, `js/profile-franchisee.js`, `js/profile-franchisor.js` |
| `scripts/check-contact-parser.mjs` | Focused contact parser regression check. Exercises imported contact edge cases for Malaysia, Singapore, China, Hong Kong, Taiwan, Vietnam, multiple Indonesian mobile numbers, hyphenated zero-prefixed groups, landlines, and call centers. | `package.json` `contact:check`, `functions/_dashboard-utils.js` |
| `scripts/check-franchise-detail-assets.ts` | Focused generated-detail asset regression check. Verifies `injectDetailAssets()` is idempotent and still injects detail tabs, proposal overlay/download/navigation hooks including side-specific pointer states, contact floats, compare behavior, and no legacy WordPress runtime snippets. | `package.json` `detail-assets:check`, `src/lib/franchise-detail-assets.ts`, `src/lib/franchise-detail-styles.ts`, `src/lib/franchise-detail-scripts.ts` |
| `scripts/check-dashboard-ocr-client.mjs` | Focused dashboard OCR browser-module regression check. Syntax-checks the split OCR browser modules plus shared dashboard utils/review modules touched by OCR Review, and asserts state/provider/worker renderer modules, provider toggle, retry actions, continuous-run lease wiring, job filter/pagination wiring, OCR enrichment queue/review-subtab wiring, shared pill action helpers, hover image-preview wiring, retry button wiring, and no-active-provider disabled-state fragments remain present. | `package.json` `dashboard:ocr:check`, `js/dashboard-utils.js`, `js/dashboard-review.js`, `js/dashboard-ocr.js`, `js/dashboard-ocr-schedulers.js`, `js/dashboard-ocr-state.js`, `js/dashboard-ocr-providers.js`, `js/dashboard-ocr-worker.js`, `js/dashboard-ocr-jobs.js`, `js/dashboard-ocr-batches.js`, `js/dashboard-ocr-results.js` |
| `scripts/d1-static-publish-poller.mjs` | Dependency-free Node poller for GitHub Actions. Calls the Cloudflare D1 HTTP API to expire stale queued requests, check pending rebuild work, enforce per-site guardrails, call the Cloudflare Pages Deploy Hook in the default mode, and mark direct-deploy fallback status. | `.github/workflows/d1-static-publish.yaml`, D1 `site_rebuild_requests`, D1 `site_publish_state`, GitHub secrets `CLOUDFLARE_API_TOKEN` and `PAGES_DEPLOY_HOOK_FRANCHISEE_ID` |
| `src/lib/shared-schemas.ts` | Shared TypeScript Zod schemas and normalizers for CSV import rows, D1 franchise static snapshot rows including progressive canonical fields, listing status/tier/package field normalization, and import/build/Astro validation paths. | `scripts/import-csv-to-d1.ts`, `scripts/build-d1-franchise-pages.ts`, `src/lib/franchise-static.ts`, Zod |
| `src/lib/country-metadata.ts` | Shared static-build country metadata adapter. Normalizes country aliases, renders flag labels, and provides international mobile matching/formatting for generated franchise pages. | `data/country-metadata.json`, `src/lib/franchise-static.ts`, `src/lib/franchise-contact.ts`, `scripts/check-country-metadata.mjs` |
| `src/lib/ocr-provider-metadata.js` | Shared OCR provider metadata contract. Defines provider-specific visible fields, labels/help copy, activation requirements, official/source-linked free-limit details, rate-limit guards, file-limit notes, and account-specific caveats so dashboard UI, server-side validation, provider config reads, and regression checks use one source of truth. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `js/dashboard-ocr-providers.js`, `functions/_ocr-provider-config.js`, `scripts/check-ocr-provider-config.ts`, `docs/architecture/OCR_PROVIDER_STRATEGY.md` |
| `src/lib/franchise-static.ts` | Astro-side renderer facade that validates the D1 franchise static snapshot and composes listing/detail HTML through focused helper modules. It keeps route-facing exports, template orchestration, neutral `Info Franchise` directory CTAs, franchisor free-brand/Premium CTA modules, icon-only save controls, information-section detail actions, shared tooltip markup, legacy-template public branding/support-mailbox cleanup, cleaned detail metadata, breadcrumbs/chips, non-Indonesia origin/target facts with flag labels, and SEO/contact/social placeholder mapping. Text cleanup, country display, detail quick-facts, connected buyer-intent detail tabs, Premium CTA/media/brochure entries, contact parsing, ranking, category/capital/city routes, and generated assets are delegated to focused modules. | `src/lib/shared-schemas.ts`, `src/lib/country-metadata.ts`, `json/d1-franchise-static-data.json`, `templates/peluang-usaha-tpl.html`, `templates/detail-franchise-tpl.html`, `src/lib/franchise-static-assets.ts`, `src/lib/franchise-detail-assets.ts`, `src/lib/franchise-detail-summary.ts`, `src/lib/franchise-detail-tabs.ts`, `src/lib/franchise-text.ts`, `src/lib/franchise-premium-detail.ts`, `src/lib/franchise-contact.ts`, `src/lib/franchise-ranking.ts`, `src/lib/franchise-category.ts`, `src/lib/franchise-capital.ts`, `src/lib/franchise-city.ts`, Astro routes |
| `src/lib/franchise-text.ts` | Shared TypeScript helper for generated franchise pages. Owns display text normalization, company/brand casing, mostly-uppercase description cleanup, URL normalization, HTML/attribute escaping, paragraph/truncation helpers, Rupiah display, thumbnail URL shaping, `slugify`, generated HTML whitespace cleanup, legacy WordPress runtime sanitization, and legacy directory/category link canonicalization. | `src/lib/franchise-static.ts`, `scripts/check-proposal-download.ts` |
| `src/lib/franchise-premium-detail.ts` | Premium detail-page renderer helper. Detects premium listings, parses proposal/media URL values, renders Premium/Galeri/Brosur/FAQ entries, and emits one-page-at-a-time brochure reader controls with overlay download/status/navigation markup. | `src/lib/franchise-detail-tabs.ts`, `src/lib/franchise-static.ts`, `src/lib/franchise-text.ts`, `src/lib/shared-schemas.ts`, `functions/proposal-download.js` |
| `src/lib/franchise-detail-summary.ts` | Shared generated detail summary renderer. Builds complementary quick-fact chips and the brand profile card with logo, official social links, a category browsing link, actionable `Tanya Admin` / `Hubungi Admin` contact-tab openers, shared-tooltip explanations for lay readers, and icon-enriched D1/form facts including area/staff/setup and projection ranges used inside the `Profil` tab so Astro output and the D1 bridge do not duplicate summary markup. Fee/license facts use the public label `Biaya Kemitraan Awal`. | `src/lib/franchise-detail-tabs.ts`, `src/lib/franchise-static.ts`, `scripts/d1-page-renderer.ts`, `src/lib/franchise-text.ts`, `src/lib/country-metadata.ts`, `src/lib/franchise-static-assets.ts` |
| `src/lib/franchise-detail-tabs.ts` | Shared generated detail tab composer. Builds buyer-intent tab entries (`Profil`, `Detail`, `Investasi`, optional `Support`, optional Premium/media/Brosur/FAQ, and `Kontak`) and renders the connected tab shell used by both Astro detail pages and the D1 bridge. Keeps investment cards, optional fee breakdown, working-capital/additional-cost notes, projection ranges, `Biaya Kemitraan Awal`, and missing-info contact buttons in one place. | `src/lib/franchise-static.ts`, `scripts/d1-page-renderer.ts`, `src/lib/franchise-detail-summary.ts`, `src/lib/franchise-premium-detail.ts`, `src/lib/franchise-text.ts` |
| `src/lib/franchise-contact.ts` | Generated detail contact renderer helper. Parses messy imported regional phone/WhatsApp text, renders contact rows, and replaces legacy hard-coded Alam floating buttons with the listing's parsed WhatsApp/phone and PIC label while hiding unavailable channels. | `src/lib/franchise-static.ts`, `scripts/d1-page-renderer.ts`, `src/lib/franchise-text.ts` |
| `src/lib/franchise-ranking.ts` | Directory ranking helper for deterministic recommendation, popular, and alphabetical ordering. Keeps completeness/quality scoring and row comparison outside the route renderer while preserving `franchise-static.ts` compatibility re-exports. | `src/lib/franchise-static.ts`, `src/lib/franchise-text.ts` |
| `src/lib/franchise-category.ts` | Category route/helper module for canonical category slugs, aliases, summaries, and `/peluang-usaha?kategori=...` href generation. | `src/lib/franchise-static.ts`, `src/lib/franchise-text.ts` |
| `src/lib/franchise-location-normalization.ts` | Shared location normalization helper. Owns supported Indonesian city aliases, city/province slugs, text inference from `city_origin`, `office_address`, `outlets_location`, and `location_requirement`, plus parsing of structured location JSON from D1. | `src/lib/franchise-city.ts`, `scripts/sync-franchise-locations.ts`, `src/lib/franchise-buyer-tools.ts` |
| `src/lib/franchise-city.ts` | City discovery helper for static `/peluang-usaha/kota/` pages. Reads normalized `structured_locations` from the snapshot when present, falls back to supported city text inference, builds only cities with enough listings, and exposes city summaries for buyer-tool quick links. | `src/lib/franchise-static.ts`, `src/lib/franchise-location-normalization.ts`, `src/lib/franchise-buyer-tools.ts`, `json/d1-franchise-static-data.json` |
| `src/lib/franchise-static-assets.ts` | Generated directory asset helper for Astro franchise pages. Owns CSS-only missing-image placeholders, injected directory search/filter/sort CSS/JS, and the dark/yellow owner CTA styling used to send franchisors to free brand creation plus Premium education; directory badge tooltips override legacy card overflow so hover text can render above neighboring cards. Generated directory pages intentionally do not load the Premium promo ribbon to protect Function quota on low-intent SEO traffic. Re-exports `injectDetailAssets()` for compatibility. | `src/lib/franchise-static.ts`, `src/lib/franchise-detail-assets.ts` |
| `src/lib/franchise-detail-assets.ts` | Generated detail-page asset injector facade. It keeps the public `injectDetailAssets()` contract stable and delegates the large generated CSS/JS strings to focused style and script modules. | `src/lib/franchise-static-assets.ts`, `src/lib/franchise-detail-styles.ts`, `src/lib/franchise-detail-scripts.ts` |
| `src/lib/franchise-detail-styles.ts` | Generated detail-page CSS module. Owns border-light tab/profile styling, investment note styling, Premium gallery/brochure/FAQ styling, dynamic brand-contact floats, brochure side-specific transparent-gradient previous/next hit areas, motion-triggered proposal overlay controls, yellow-flash suppression for proposal navigation buttons, and responsive rules. | `src/lib/franchise-detail-assets.ts`, `src/lib/franchise-premium-detail.ts`, `src/lib/franchise-detail-summary.ts`, `src/lib/franchise-detail-tabs.ts` |
| `src/lib/franchise-detail-scripts.ts` | Generated detail-page browser script module. Owns tab activation, proposal reader page navigation, side-specific pointer state for brochure navigation overlays, pointer-motion auto-hide with pinned hover/focus behavior on prev/next controls, keyboard arrows, contact-tab shortcuts, compare button behavior, and the client call to `/proposal-download`. Server-side image fetch/PDF/watermark work remains in the proposal download Function. | `src/lib/franchise-detail-assets.ts`, `functions/proposal-download.js` |
| `src/pages/peluang-usaha/index.astro` | Astro static listing route for canonical `/peluang-usaha/`. Loads the D1 snapshot and renders the existing listing template with manual search, sort, category, status, comparison, category, modal, and buyer-tool links during build. | `src/lib/franchise-static.ts`, `json/d1-franchise-static-data.json` |
| `src/pages/peluang-usaha/[slug].astro` | Astro static detail route that physically builds `/peluang-usaha/{slug}.html` under `build.format: "preserve"` while public links stay `/peluang-usaha/{slug}`. Uses `getStaticPaths` from the D1 snapshot so each franchise keeps an individually indexable HTML page. | `src/lib/franchise-static.ts`, `json/d1-franchise-static-data.json`, `astro.config.mjs` |
| `src/pages/peluang-usaha/kategori/index.astro` and `src/pages/peluang-usaha/kategori/[slug].astro` | Astro static category directory routes generated from the D1 franchise snapshot. Each category landing page has useful intro copy, SEO metadata, directory controls, and filtered listing cards. | `src/lib/franchise-static.ts`, `src/lib/franchise-category.ts`, `json/d1-franchise-static-data.json` |
| `src/pages/peluang-usaha/modal/index.astro` and `src/pages/peluang-usaha/modal/[slug].astro` | Astro static capital-range directory routes generated from listing investment data. Ranges include under 10 juta through above 500 juta and refresh during the normal public build. | `src/lib/franchise-static.ts`, `src/lib/franchise-capital.ts`, `json/d1-franchise-static-data.json` |
| `src/pages/peluang-usaha/kota/index.astro` and `src/pages/peluang-usaha/kota/[slug].astro` | Astro static city directory routes generated from structured D1 location rows with text inference fallback. Each page has SEO metadata, explanatory copy, directory controls, and filtered listing cards. | `src/lib/franchise-static.ts`, `src/lib/franchise-city.ts`, `json/d1-franchise-static-data.json` |
| `src/pages/bandingkan/index.astro` | Static buyer comparison page. Embeds a safe compact listing payload so buyers can compare selected brands by modal, category, BEP, royalty, city, and detail link. | `src/lib/franchise-buyer-tools.ts`, `js/franchise-compare.js`, `css/franchise-buyer-tools.css`, `json/d1-franchise-static-data.json` |
| `src/pages/alat-franchise/index.astro` | Static buyer tools page. Provides a budget matcher, BEP calculator, and quick links into modal/category/city directories from the D1-backed listing snapshot. Tool usage can be stored as optional buyer context for later lead submission. | `src/lib/franchise-buyer-tools.ts`, `js/franchise-buyer-tools.js`, `css/franchise-buyer-tools.css`, `json/d1-franchise-static-data.json` |
| `js/dashboard-utils.js` | Browser utility namespace for dashboard modules. Provides HTML/attribute escaping, Indonesian currency formatting, form-value writes, icon-only action toolbar/button/link HTML, and shared text-pill action button/link HTML with custom tooltip attributes. | `js/dashboard-admin.js`, `js/dashboard-premium-operations.js`, `js/dashboard-review.js`, `js/dashboard-ocr-results.js`, `src/pages/dashboard/index.astro`, `css/dashboard.css`, `js/shared-tooltip.js` |
| `js/dashboard-premium-operations.js` | Premium Operations browser module. Renders Premium funnel metrics, payment-method form values including optional QRIS preview/upload, Premium lifecycle/settings/promo ribbon form values including the per-day visitor display cap, notifications, upcoming expiries, annual report rows, queued email rows, and handles admin payment-method/settings submissions plus email retry/cancel actions. | `js/dashboard-admin.js`, `functions/dashboard-data.js`, `functions/payment-method-upload.js`, `src/pages/dashboard/index.astro`, `js/dashboard-utils.js` |
| `js/franchise-compare.js` | Browser comparison tool for `/bandingkan`. Reads selected listing ids from URL/localStorage, lets buyers add/remove brands, renders a comparison table without server-side state, and stores selected brands as optional buyer context. | `src/pages/bandingkan/index.astro`, `src/lib/franchise-buyer-tools.ts` |
| `js/franchise-buyer-tools.js` | Browser runtime for `/alat-franchise`. Runs budget matching against embedded listing data, calculates a simple BEP simulation from user-entered numbers, and stores the latest tool intent as optional buyer context. | `src/pages/alat-franchise/index.astro`, `src/lib/franchise-buyer-tools.ts` |
| `js/site-promo-bar.js` | Shared public promo ribbon loader for high-intent Premium surfaces. Fetches `/premium-promo` with a 30-minute browser cache, injects its own compact top-bar CSS, inserts the top bar only when a dashboard-managed campaign is active and the visitor has not reached the configured per-day view cap, records deduped ribbon impressions/CTA clicks as coarse Premium funnel events, and keeps promo changes dynamic without rebuilding static pages while reducing Pages Function invocations. | `functions/premium-promo.js`, `functions/premium-event.js`, `src/pages/premium/index.astro`, `src/pages/profil/index.astro` |
| `js/dashboard-review.js` | Dashboard Review/Data Quality module. Renders persisted quality warnings, lets Data Quality quick-edit actions seed the Review form and switch to the Review tab, splits manual/staff edit suggestions into Pending Edit Review while document-derived `proposal_extraction` and `ocr_enrichment_bundle` suggestions render in Review OCR, renders icon-led old-to-new field diffs plus wrapped reasons, formats `_idr` values as Rupiah with Indonesian thousand separators, normalizes short text display casing, shows document source excerpts with highlighted basis snippets plus brochure image-preview actions from `old_value.__ocr_evidence`, renders admin-only per-field approval checkboxes and sends selected fields as `approved_fields`, renders claim rows and the admin Area Listing editor from editable listing location rows, posts edit suggestions/location updates, refreshes quality checks, and handles admin edit/claim review actions. | `js/dashboard-admin.js`, `functions/dashboard-data.js`, `src/pages/dashboard/index.astro`, `js/dashboard-utils.js`, `js/dashboard-ocr-jobs.js` |
| `js/dashboard-operations.js` | Dashboard Operations browser module. Renders outreach rows/logging, bulk Google Contacts save for the current outreach queue, pending Premium payment review controls, publication status controls, lead summary, system health rows, and local traffic guardrails for Cloudflare Free-plan limit/throttle visibility, using injected dashboard action/reload/status callbacks. Google Contacts setup-required errors render a safe status link to `/dashboard/#google-contacts-setup`. | `js/dashboard-admin.js`, `functions/dashboard-data.js`, `functions/_google-contacts.js`, `src/pages/dashboard/index.astro`, `js/dashboard-utils.js` |
| `js/dashboard-ocr-schedulers.js` | Dashboard OCR scheduler browser helper. Owns scheduler provider metadata, active-scheduler counting, and first-active-scheduler selection so `dashboard-ocr.js` does not hardcode every scheduler field/copy rule. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js` |
| `js/dashboard-ocr-state.js` | Dashboard OCR state factory. Creates the initial coordinator state for provider/scheduler admin flags, autosave timers, active job/result filters, OCR job page size defaulting to 120, grouped result page indexes, result franchise-card pagination, polling, countdown/foreground-refresh timers, and the current continuous-run lease id so the OCR facade does not keep its state contract inline. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js` |
| `js/dashboard-ocr-providers.js` | Dashboard OCR provider/scheduler renderer. Renders the provider priority list, credential badges, provider-specific field visibility/help, read-only quota/rate-limit metadata, per-provider remaining quota, source-linked official limit notes, scheduler selector, scheduler credential badges, and encrypted-credential status copy through injected metadata/helpers. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `js/dashboard-ocr-schedulers.js`, `src/lib/ocr-provider-metadata.js`, `css/dashboard-ocr-settings.css` |
| `js/dashboard-ocr-jobs.js` | Dashboard OCR job renderer. Renders clickable OCR job status chips including default `Semua`, paginated server-filtered job headings/pagers, compact franchise-grouped job cards, icon-only full-message pills for long OCR error/warning/no-text notes, source-image/result/retry/no-text confirmation actions for failed and needs-review rows, job status badges through injected helpers from the OCR facade, and a delegated lazy hover/focus image preview that reuses one body-level preview element, clamps inside the browser viewport, chooses the better left/right side near the cursor, and cleans up on pointerout, scroll, resize, visibility change, or row re-render. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `css/dashboard-ocr-execution.css` |
| `js/dashboard-ocr-batches.js` | Dashboard OCR batch renderer and countdown helper. Renders persisted batch rows, progress bars, Retry/Refresh buttons, and dynamic structured scheduler ETA status chips such as `Menunggu 35s` or `Memproses`, while exposing countdown label updates back to the OCR facade timer. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `css/dashboard-ocr-execution.css` |
| `js/dashboard-ocr-worker.js` | Dashboard OCR combined-quota renderer. Renders active-provider combined quota used/remaining/reset state, account-specific provider counts, optional safety-cap context, reset countdown text, and tooltip copy from the OCR facade timer without putting that UI formatting back into the coordinator. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `css/dashboard-ocr-execution.css` |
| `js/dashboard-ocr-results.js` | Dashboard OCR result renderer. Renders the grouped OCR enrichment queue with shared source/listing/review pill actions, compact franchise-grouped OCR result cards with stable per-asset anchors, per-page prev/next controls, OCR text previews, source image links with shared hover-preview attributes and no competing text tooltip, listing links, and review links through injected label/page-state helpers from the OCR facade. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, `css/dashboard-ocr-results.css`, `js/dashboard-ocr-jobs.js`, `js/dashboard-utils.js` |
| `js/dashboard-ocr.js` | Admin OCR coordinator/facade. Controls OCR subtabs including the dedicated Review OCR subtab, credential autosave submit cycles, provider enablement, dry-run/enqueue/retry/no-text actions, OCR enrichment bundle creation from grouped results, server-backed scheduler batch creation when a scheduler is active, first-chunk dashboard priming for new/retried scheduler batches, resuming failed/paused scheduler batches before creating new ones, browser continuous-run OCR fallback with server lease acquire/release when no scheduler is active, server-side job/result searches including exact asset lookup from Eksekusi Job result buttons, active filter/page-size/franchise-page state, polling/timer lifecycle, foreground refresh after hidden tabs become active again, and delegates state creation plus provider/worker/job/batch/countdown/result rendering and shared job/result/OCR Review image hover-preview binding/cleanup to focused OCR modules. Persisted batch rows remain inspectable/retryable and are now the primary reliable path for long OCR runs. | `js/dashboard-admin.js`, `js/dashboard-ocr-schedulers.js`, `js/dashboard-ocr-state.js`, `js/dashboard-ocr-providers.js`, `js/dashboard-ocr-worker.js`, `js/dashboard-ocr-jobs.js`, `js/dashboard-ocr-batches.js`, `js/dashboard-ocr-results.js`, `functions/dashboard-data.js`, `src/pages/dashboard/index.astro`, `src/lib/ocr-provider-metadata.js`, `js/dashboard-utils.js`, OCR CSS modules |
| `js/dashboard-admin.js` | Client controller facade for `/dashboard`. Shows a loading skeleton while Clerk/session/dashboard authorization is processing, boots Clerk auth, renders a short per-tab `sessionStorage` cache after Clerk confirms the same active user/session, refreshes `/dashboard-data` live with `cache: "no-store"`, controls tabs/metrics/status/debug, maps documentation deep links such as `#google-contacts-setup` to the Operations tab before scrolling, and delegates Premium, Review, Operations, and OCR configuration workflows to focused client modules. Cache is cleared and the protected shell relocked on missing/expired authorization. | `src/pages/dashboard/index.astro`, `functions/dashboard-data.js`, `css/dashboard.css`, `js/auth-clerk.js`, `js/shared-tooltip.js`, `js/dashboard-utils.js`, `js/dashboard-premium-operations.js`, `js/dashboard-review.js`, `js/dashboard-operations.js`, `js/dashboard-ocr-schedulers.js`, `js/dashboard-ocr-state.js`, `js/dashboard-ocr-providers.js`, `js/dashboard-ocr-worker.js`, `js/dashboard-ocr-jobs.js`, `js/dashboard-ocr-batches.js`, `js/dashboard-ocr-results.js`, `js/dashboard-ocr.js` |
| `js/premium-page.js` | Public `/premium` analytics helper. Records deduped page views and CTA clicks through `/premium-event` with no user-identifying browser metadata. | `src/pages/premium/index.astro`, `functions/premium-event.js` |
| `src/pages/dashboard/index.astro` | Static protected `/dashboard` route assembler with deferred same-URL Clerk login, auth-loading skeleton, metrics, Outreach/Quality/Review/Operations/OCR tabs, shared CSS/script load order, masked auth debug, and OCR provider metadata injection. Review, Operations, and OCR panel markup now live in focused Astro components so the route stays small while preserving the same browser data hooks and authorization model. | `src/components/dashboard/DashboardReviewPanel.astro`, `src/components/dashboard/DashboardOperationsPanel.astro`, `src/components/dashboard/DashboardOcrPanel.astro`, `js/dashboard-admin.js`, `js/dashboard-utils.js`, `js/dashboard-premium-operations.js`, `js/dashboard-review.js`, `js/dashboard-operations.js`, `js/dashboard-ocr-schedulers.js`, `js/dashboard-ocr-state.js`, `js/dashboard-ocr-providers.js`, `js/dashboard-ocr-worker.js`, `js/dashboard-ocr-jobs.js`, `js/dashboard-ocr-batches.js`, `js/dashboard-ocr-results.js`, `js/dashboard-ocr.js`, `src/lib/ocr-provider-metadata.js`, `functions/dashboard-data.js`, `functions/_google-contacts.js`, dashboard CSS modules, Clerk, D1, Google People API |
| `src/components/dashboard/DashboardReviewPanel.astro` | Static dashboard Review panel markup. Owns the Review tab HTML for direct listing edits, generic pending edit review, claim review, and admin Area Listing controls while leaving all runtime behavior in `js/dashboard-review.js`. | `src/pages/dashboard/index.astro`, `js/dashboard-review.js`, `css/dashboard-review.css`, `functions/dashboard-data.js` |
| `src/components/dashboard/DashboardOperationsPanel.astro` | Static dashboard Operations panel markup. Owns publish queue, network publication, lead summary, Premium operations/payment/settings, system health, traffic guardrail, dashboard integration guide placement, and staff edit-policy containers while leaving rendering/actions in operations and Premium browser modules. | `src/pages/dashboard/index.astro`, `src/components/dashboard/DashboardIntegrationGuide.astro`, `js/dashboard-operations.js`, `js/dashboard-premium-operations.js`, `css/dashboard-operations.css`, `css/dashboard-premium.css`, `functions/dashboard-data.js` |
| `src/components/dashboard/DashboardIntegrationGuide.astro` | Static dashboard documentation component for third-party setup anchors used by warnings: Google Contacts, OCR provider, OCR scheduler, Premium email delivery, and publish automation. | `src/components/dashboard/DashboardOperationsPanel.astro`, `docs/architecture/DASHBOARD_INTEGRATION_GUIDE.md`, `js/dashboard-operations.js` |
| `src/components/dashboard/DashboardOcrPanel.astro` | Static dashboard OCR panel markup. Owns OCR guide-card navigation plus Pengaturan, Eksekusi Job, Hasil OCR, and Review OCR containers while leaving provider/job/batch/result behavior in the split OCR browser modules. | `src/pages/dashboard/index.astro`, `js/dashboard-ocr.js`, OCR browser modules, OCR CSS modules, `functions/dashboard-data.js` |
| `src/pages/profil/index.astro` | Static protected user profile shell at `/profil` with Franchisee.id favicon/apple-touch icon. Loads custom Clerk runtime, navbar auth state, Font Awesome, base/profile domain stylesheets including the Outfit display font, profile helper modules, `js/profile-page.js`, and the high-intent Premium promo ribbon; anonymous visitors are redirected client-side to `/login/?next=/profil/`. | `js/profile-page.js`, `js/profile-ui-utils.js`, `js/profile-premium.js`, `js/profile-opportunities.js`, `js/profile-account.js`, `js/profile-roles.js`, `js/profile-leads.js`, `js/profile-analytics.js`, `js/profile-franchisee.js`, `js/profile-franchisor.js`, `js/site-promo-bar.js`, `functions/profile-data.js`, `js/auth-clerk-debug.js`, `js/auth-clerk-ui.js`, `js/auth-clerk-core.js`, `js/auth-clerk.js`, `js/auth-navbar.js`, `css/auth-clerk.css`, `css/profile.css`, `css/profile-premium.css`, `css/profile-analytics.css`, `css/profile-franchisor.css` |
| `src/pages/premium/index.astro` | Public premium membership sales page at `/premium`. Uses benefit-led copy for the free-brand-to-Premium ladder: start with a free brand page, then upgrade when the brand is ready to look more trustworthy, be easier to find, and receive more actionable prospect interest. Includes add-on editorial/social promotion pricing and direct CTAs to login/register/profile membership without exposing infrastructure terms; loads `js/premium-page.js` for coarse funnel tracking and `js/site-promo-bar.js` for active admin-managed campaign ribbons with the per-day view cap. | `css/premium.css`, `js/premium-page.js`, `js/site-promo-bar.js`, `/profil/?tab=membership` |
| `src/pages/sso-callback/index.astro` | Hidden static Clerk OAuth callback route. Loads debug, UI, core, and facade auth modules, lets `Auth.init()` run `clerk.handleRedirectCallback()`, redirects to the saved pending destination only after a session is active, and otherwise shows masked callback debug with an icon-only copy button. This page should be listed in Clerk allowed redirect/application URLs but is not a separate login surface. | `js/auth-clerk-debug.js`, `js/auth-clerk-ui.js`, `js/auth-clerk-core.js`, `js/auth-clerk.js`, Clerk Google OAuth, `/login`, `/register`, `/dashboard` |
| `public/_redirects` | Cloudflare Pages redirect rules. Redirects legacy duplicate directory URLs (`/direktori-franchise`, `/rekomendasi`, `/populer`, `/abjad`) to canonical `/peluang-usaha` states and legacy category URLs/aliases to static `/peluang-usaha/kategori/[slug]` landing pages. | Astro public asset copy, Cloudflare Pages |
| `migrations/0016_franchise_location_metadata.sql` | Adds generated-row metadata and indexes to existing `franchise_locations` so inferred location rows can be refreshed safely and later distinguished from owner/admin-managed rows. Applied remotely on 2026-07-04. | D1 `franchise_locations`, D1 `locations`, `scripts/sync-franchise-locations.ts` |
| `migrations/0017_franchise_origin_target_market.sql` | Adds optional `franchises.brand_country` and `franchises.target_market` fields so domestic and international franchisors can state brand origin and intended market explicitly. Applied remotely on 2026-07-05. | D1 `franchises`, `/daftar`, `/profil`, `/dashboard`, D1 static pages |
| `migrations/0018_backfill_franchise_origin_target_market.sql` | Backfills legacy `brand_country` / `target_market` values from clear contact country signals. Applied remotely on 2026-07-05; final distribution after correcting local-number precedence was 192 Indonesia/Indonesia, 1 Taiwan/Indonesia, and 4 blank rows with no clear signal. | D1 `franchises`, `franchisor_profiles`, `franchise_contacts` |

## Supporting Files

| File/Directory | Role |
| --- | --- |
| `templates/peluang-usaha-tpl.html` | Large static template with `<!-- DYNAMIC_FRANCHISE_LISTING -->` placeholder. Header/footer directory links point directly to canonical `/peluang-usaha` query URLs. |
| `templates/detail-franchise-tpl.html` | Large static detail template with placeholders such as `{BRAND_NAME}`, `{SLUG}`, `{DETAIL_QUICK_FACTS}`, `{INFO_SECTION_ACTIONS}`, `{INTERNATIONAL_FACTS}`, `{DYNAMIC_TABS_CONTENT}`, and `<!-- DYNAMIC_DISCLAIMER_BOX -->`. The visible detail heading is promoted to H1 in the information section while the old hero/title block is hidden by generated detail CSS. Header/footer/category links point directly to canonical `/peluang-usaha` query URLs, and the legacy fee label is `Biaya Kemitraan Awal`. |
| `data/country-metadata.json` | Canonical supported-country metadata for country name, ISO code, flag, dial code, aliases, WhatsApp digit range, international mobile pattern, and display grouping. |
| `docs/architecture/INTERNATIONAL_FRANCHISOR_POLICY.md` | Product/data policy for keeping Indonesian franchisor onboarding low-friction while supporting overseas brands seeking Indonesian partners. Documents Indonesia/Indonesia defaults, phone-signal backfill, collapsed form fields, and non-Indonesia public flag display. |
| `daftar/index.html` | Static initial profile/listing completion page that loads `css/form-franchise.css`, all modular form scripts, shared tooltip, and the custom Clerk/navbar runtime. Treat as integration context and preserve every field. Anonymous visitors are redirected to `/login?next=...`; completed users are redirected to `/profil/` or a safe `next` destination after form submit; known Clerk/D1 identity fields are read-only. Franchisor listing completion keeps brand origin and target-market fields collapsed for Indonesian brands and opens/prefills them when a non-Indonesia contact country code is selected. |
| `login/index.html` | Legacy static login page shell. The old WPForms block is replaced at runtime by `js/auth-clerk.js` with custom Clerk `Masuk`/`Buat Akun` UI. |
| `register/index.html` | Compatibility redirect page; canonical public registration is `/login?mode=register`. Cloudflare `_redirects` also redirects `/register` and `/register/`. |
| `css/form-franchise.css` | Form CSS aggregator. Preserve import order. |
| `css/form-franchise/*.css` | Modular form styling for utilities, layout/tabs/steps, core fields, alerts/autocomplete, packages, and claim search. |
| `json/unclaimed-brands.json` | Generated sanitized claim-search data. Legacy builder can write this, but the current D1-backed path regenerates it from published unclaimed D1 rows. Frontend uses this before API fallback. |
| `json/d1-franchise-static-data.json` | D1 snapshot consumed by Astro static routes. Generated by `scripts/build-d1-franchise-pages.ts` before Astro build so the static route layer has a stable, validated input. After the proposal migration, migrated proposal lists point to first-party `https://assets.franchisee.id/franchises/{slug}/proposal/...` URLs. |
| `json/d1-generated-pages-manifest.json` | D1 public-page generation manifest. Tracks hashes and D1-owned page paths so bridge reruns skip unchanged pages and prune only pages previously generated by `scripts/build-d1-franchise-pages.ts`. |
| `json/country-metadata.json` | Public copy of supported-country metadata loaded by `/daftar` so country-code dropdowns, origin inference, and WhatsApp digit ranges stay aligned. |
| `json/country-codes.json` | Backward-compatible country-code option list retained for older code paths; current `/daftar` loads `json/country-metadata.json`. |
| `csv/franchisors.csv` | Local fallback/source snapshot for `FRANCHISOR`. |
| `csv/franchisee.csv` | Local fallback/source snapshot for `FRANCHISEE`. |
| `csv/unclaimed.csv` | Local fallback/source snapshot for `UNCLAIMED`. |
| `astro.config.mjs` | Astro static build config for Cloudflare Pages-compatible output. Uses `build.format: "preserve"` so listing index output stays nested while detail source files can build flat `.html` files. |
| `src/env.d.ts` | Astro TypeScript client reference. |
| `tsconfig.json` | Strict TypeScript config for migration scripts and Astro support code. Includes `scripts/**/*.ts`, `src/**/*.ts`, and `src/**/*.d.ts`. |
| `package.json` | Declares pnpm scripts for D1 CSV import, D1 snapshot/bridge generation, Astro static builds, legacy static copying, focused regression checks, metadata checks, and publish queue polling: `import:csv:*`, `build:d1:franchises:*`, `astro:sync`, `build`, `build:astro`, `copy:legacy-static`, `country:check`, `profile:check`, `contact:check`, `google-contacts:check`, `detail-assets:check`, `proposal-download:check`, `ocr:check`, `publish:d1:poll`, `dev:astro`, and `preview:astro`. `build:d1:franchises` now avoids writing root bridge HTML; `build:d1:franchises:bridge` is the explicit legacy bridge writer. `build` is a conventional alias for Cloudflare Pages and runs the Astro pipeline plus legacy static copy. |
| `.github/workflows/d1-static-publish.yaml` | 30-minute/manual D1 publish poller. Runs `scripts/d1-static-publish-poller.mjs`, calls the Cloudflare Pages Deploy Hook only when D1 is dirty and guardrails allow publishing, and supports direct `wrangler pages deploy dist` fallback without committing generated output. |
| `.github/workflows/premium-email-worker.yaml` | 30-minute/manual Premium email cron. Calls protected `/premium-email-worker` with `PREMIUM_EMAIL_WORKER_SECRET` so queued payment emails and renewal reminders can be processed after deployment. |
| `.github/workflows/head.yaml` | Legacy automation that injects `.head` content into every HTML file. Use cautiously because it touches many static exports. |
| `.github/workflows/sitemap-readme.yaml` | Manual workflow for sitemap/readme generation from HTML files. |
| `docs/README.md` | Central documentation index and source-of-truth policy. |
| `SUGGESTION.md` | Assistant-owned proactive recommendation backlog for product, UX, reliability, performance, security, data quality, operations, migration safety, and developer workflow improvements. |
| `docs/architecture/TECH_STACK_DECISIONS.md` | Canonical migration stack decisions: TypeScript, Zod, D1 SQL migrations, role model, Clerk/D1 responsibility split, and Drizzle adoption timing. |
| `docs/architecture/D1_STATIC_PUBLISH_STRATEGY.md` | Compares twice-daily D1 static publishing with GitHub Actions polling, Cloudflare Deploy Hook builds, and GitHub direct deploy fallback. Current target is 30-minute polling after a D1 dirty queue exists, without committing generated output from the poller. |
| `docs/architecture/AUTH_ONBOARDING_NAV_PLAN.md` | Progress tracker and implementation plan for public account entry: role-first registration, post-auth `/daftar` completion, public role add-on/correction flow, Clerk-prefilled form fields, animated auth/form toggles, and logged-in navbar account state. |
| `docs/architecture/PROFILE_PAGE_PLAN.md` | Progress tracker and implementation plan for protected `/profil`: account identity, franchisee/franchisor profile ownership, franchisee recommendation value surfaces, owned listing management, claim status, `/daftar` read-only identity fields, and owner-facing profile APIs. |
| `DASHBOARD.md` | Admin/staff dashboard plan covering overview, listing operations, unclaimed outreach, claim review, data quality, publishing, leads, system health, roles, and MVP sequencing. Third-party setup details live in `docs/architecture/DASHBOARD_INTEGRATION_GUIDE.md`. |
| `docs/architecture/DASHBOARD_INTEGRATION_GUIDE.md` | Focused `/dashboard` third-party setup guide for Google Contacts, OCR provider/scheduler, Premium email delivery, and publish automation. Mirrors the stable in-app dashboard anchors used by warning links. |
| `migrations/0001_initial_network_schema.sql` | First D1 schema migration for shared network sites, users/roles, profiles, franchises, claims, assets, leads, site publications, subscriptions/entitlements, imports, and audit events. |
| `migrations/0002_add_franchisor_social_links.sql` | Adds optional Facebook, TikTok, YouTube, and LinkedIn URL columns to `franchisor_profiles`; website and Instagram already existed. |
| `migrations/0003_site_publish_queue.sql` | Adds `site_rebuild_requests` and `site_publish_state` for D1-to-static publish queueing, per-site guardrails, and publish mode tracking. Applied remotely to `franchise_db` on 2026-06-18. |
| `migrations/0004_dashboard_operations.sql` | Adds dashboard operation tables for unclaimed outreach events, staff auto-approval rules, and listing edit suggestions. Applied remotely to `franchise_db` on 2026-06-22 after setting `CLOUDFLARE_ACCOUNT_ID` for Wrangler account context. |
| `migrations/0005_email_role_grants.sql` | Adds `email_role_grants` for pre-authorizing roles by normalized email before a real Clerk user id exists. Applied remotely to `franchise_db` on 2026-06-24; `admin@alampintar.org` and `email@franchisor.id` have active pending `admin` grants for Google/email bootstrap. |
| `migrations/0006_saved_opportunities.sql` | Adds `franchise_saved_opportunities` so logged-in franchisee saved listings persist across devices. Applied remotely to `franchise_db` on 2026-06-28. |
| `migrations/0007_contacts_quality_dashboard.sql` | Adds `franchise_contacts` and `franchise_quality_checks` for normalized contact storage and persistent dashboard quality checks. Applied remotely to `franchise_db` on 2026-06-28 and verified after migration. |
| `migrations/0008_analytics_telemetry_multisite.sql` | Adds `franchise_product_events` for privacy-safe listing interaction analytics and `operation_events` for dashboard health telemetry. Applied remotely to `franchise_db` on 2026-06-28 and verified after migration. |
| `migrations/0009_premium_membership.sql` | Adds `premium_orders`, `premium_payment_confirmations`, and `franchise_subscriptions` for the manual premium membership payment workflow. Applied remotely to `franchise_db` on 2026-06-28 and verified after migration. |
| `migrations/0012_premium_email_worker_guardrails.sql` | Adds notification email provider/attempt metadata, renewal reminder idempotency, due-email indexes, and Premium pending-order/source-order guardrails. Applied remotely to `franchise_db` on 2026-06-30 and verified after migration. |
| `migrations/0013_premium_lifecycle_settings_reports.sql` | Adds Premium settings, grace notifications, annual reports, HTML email bodies, and order discount fields. Applied remotely to `franchise_db` on 2026-06-30 and partially verified after migration. |
| `migrations/0010_premium_operations.sql` | Adds `payment_methods`, `premium_funnel_events`, and `premium_notifications` for premium payment settings, conversion tracking, and owner/admin notifications. Applied remotely to `franchise_db` on 2026-06-29 and verified after migration. |
| `migrations/0014_payment_method_qris_image.sql` | Adds optional `qris_image_url` and `qris_image_alt` columns to `payment_methods` so admin-managed manual payment instructions can include a QRIS image. Applied remotely to `franchise_db` on 2026-07-04 and verified after migration. |
| `migrations/0015_premium_promo_settings.sql` | Adds default Premium promo/event ribbon settings under `premium_settings`, including active flag, discount percent, event label, bonus text, CTA, and date window. Applied remotely to `franchise_db` on 2026-07-04 and verified sequentially after migration. |
| `migrations/0011_notification_email_queue.sql` | Adds `notification_email_queue` so important account/payment emails can be queued before a delivery provider is connected. Applied remotely to `franchise_db` on 2026-06-30 and verified after migration. |
| `wrangler.toml` | Active Wrangler config for Cloudflare Pages output `dist`, shared D1 binding `franchise_db` using database UUID `812cd8ac-edd0-45d9-981f-c9a15358317b`, and R2 binding `FRANCHISE_ASSETS` for owner-uploaded listing media. Production uploaded assets are exposed at `https://assets.franchisee.id` through the R2 custom domain on bucket `franchise-assets`, with Pages secret `FRANCHISE_ASSETS_PUBLIC_BASE_URL` set to that base URL. It intentionally omits `account_id` because Pages config validation rejects that key. Remote commands should run through `npx cfman wrangler --account franchise-network ...`. |
| `wrangler.example.toml` | Non-active Wrangler example showing Pages output `dist`, the `franchise_db` binding, and migrations directory; copy to `wrangler.toml` only after adding the real D1 UUID. |
| `.node-version` | Pins local/Cloudflare build runtime intent to Node `20.19.4`, matching the Astro 5.x dependency set. |
| `.context/wrangler-local-d1-test.toml` | Local-only Wrangler config used to validate D1 migrations without production credentials. Do not use as deployment config. |
| `.context/d1-import-franchise-data.sql` | Generated import SQL output. Ignored by git because it is reproducible from `/csv` and `scripts/import-csv-to-d1.ts`. |

## Main Data Flows

### 1. Directory Build Flow
1. `pnpm run build` runs the D1-backed Astro/static pipeline.
2. `scripts/build-d1-franchise-pages.ts` refreshes the D1 franchise snapshot and claim-search JSON. Legacy root bridge HTML is not written during normal builds.
3. Astro generates canonical `/peluang-usaha` directory/detail routes, modal-range directories, category landing pages, comparison, and buyer-tool pages from the D1 snapshot.
4. `scripts/copy-legacy-static.mjs` copies remaining legacy static assets/pages without overwriting Astro output.
5. Public-page-affecting D1 writes enqueue `site_rebuild_requests`.
6. `.github/workflows/d1-static-publish.yaml` polls the queue and triggers Cloudflare Pages when D1 is dirty.

### 2. Franchisee Registration Flow
1. `/daftar/index.html` loads `franchiseeForm` and `js/form-08-franchisee-steps.js`.
2. Step 1 collects personal/contact data; Step 2 collects interests and budget.
3. `js/form-utils.js` validates and formats visible fields.
4. `js/form-06-submit-validation.js` posts JSON to `/form-submit` with `form_type: "FRANCHISEE"`.
5. `js/auth-clerk-core.js` supplies a Clerk session bearer token through `window.FranchiseAuth`; unauthenticated submits are blocked.
6. `functions/form-submit.js` verifies Clerk, maps the user into D1, requires the D1 `franchisee` role (or `admin`), validates the payload with Zod, and delegates duplicate/profile/legacy/audit writes to `functions/_form-submit-franchisee.js`.

### 3. Franchisor Listing Flow
1. `/daftar/index.html` loads `franchiseListingForm` and the modular `js/form-01-*` through `js/form-07-*` stack.
2. Step navigation and required validation are controlled by `js/form-03-navigation-steps.js`.
3. Field formatting and detailed validation live in `js/form-utils.js`.
4. BEP/minimum investment logic lives in `js/form-04-calculation-city.js`.
5. Country-code and WhatsApp normalization live in `js/form-05-country-whatsapp.js`.
6. Aggressive autosave stores draft field values in `localStorage` through `js/form-01-state-helpers.js` and `js/form-06-submit-validation.js`.
7. Submit posts to `/form-submit` with `form_type: "FRANCHISOR"`, including optional `brand_country` and `target_market` when supplied.
8. `js/auth-clerk-core.js` supplies a Clerk session bearer token through `window.FranchiseAuth`; unauthenticated submits are blocked.
9. `functions/form-submit.js` verifies Clerk, maps the user into D1, requires the D1 `franchisor` role (or `admin`), validates with Zod, and delegates listing/package/publication/legacy/audit/rebuild writes to `functions/_form-submit-franchisor.js`.

### 4. Claim Brand Flow
1. Detail pages for unclaimed brands link to `/daftar?claim={slug}` through `build-details.js`.
2. `js/form-07-init.js` opens the claim tab when the `claim` query exists.
3. `js/form-02-claim-workflow.js` loads `json/unclaimed-brands.json`; if unavailable, it calls `/get-franchises?tab=UNCLAIMED&purpose=claim-search`.
4. Claim search rows are sanitized and deduped in `build-listing.js`, `functions/get-franchises.js`, and `form-01-state-helpers.js`.
5. Selecting a brand switches to the franchisor form, fills brand/category/minimum capital, stores `unclaimed_id`, and persists claim mode in `localStorage`.
6. Submit changes `form_type` to `claim`; `functions/form-submit.js` requires the D1 `franchisor` role, then `functions/_form-submit-franchisor.js` updates the D1 unclaimed franchise into a free franchisor-owned listing, records a `franchise_claims` row with `claimant_user_id`, and changes the source sheet to `FRANCHISOR` so it no longer appears in unclaimed claim search.

### 5. Clerk Login/Register And D1 User Mapping Flow
1. `/login/` loads the legacy static page shell; `js/auth-clerk-debug.js` initializes masked diagnostics, `js/auth-clerk-core.js` initializes Clerk/session helpers, and `js/auth-clerk.js` replaces `#wpforms-725` with a custom login/register UI.
2. `/register/` redirects to `/login?mode=register`; registration is the `Buat Akun` tab in the `/login` auth UI.
3. `/dashboard/` mounts the same auth runtime as a login-only internal admin/staff surface with no public registration role selector.
4. Browser code fetches `/auth-config`, sets the resolved publishable key on Clerk's script-load global/attributes, loads locally copied ClerkJS from `/clerk/clerk.browser.js` with pinned CDN fallbacks, and signs users in/up with Clerk client APIs.
5. Email/password and Google OAuth are both supported. Google OAuth uses `/sso-callback/` as the Clerk `redirectUrl`, then returns to the saved `redirectUrlComplete` such as `/dashboard` or role-specific `/daftar/?role=...&continue=1`.
6. `/login?mode=forgot-password` sends a Clerk reset code and switches to an in-page password reset panel; `/login?mode=forgot-email` keeps recovery guidance and Google sign-in inside the same login surface.
7. Public registration requires `Daftar sebagai` before email/password or Google SSO starts; the selected `franchisee`/`franchisor` role is stored across OAuth and synced through `/auth-sync` after Clerk session activation.
8. The custom browser bootstrap handles Clerk OAuth callbacks on `/sso-callback/` before token checks, even when the callback has no visible Clerk URL parameters, then clears stale callback parameters or navigates to the saved post-login URL.
9. After sign-up or sign-in, `/auth-sync` verifies the Clerk session, upserts D1 `users` by Clerk id or the same verified primary email, applies active `email_role_grants`, inserts self-assignable `franchisee`/`franchisor` roles when requested, and pushes the D1 role snapshot into Clerk metadata.
10. `/daftar` requires Clerk login before completion, opens the requested role tab when `?role=` is present, locks email/name/PIC fields from Clerk/D1 identity, and redirects completed users to `/profil/`.
11. Public navbar links are normalized by `js/auth-navbar.js`; logged-out `Daftar Mitra` points to protected `/daftar/`, anonymous users are redirected to `/login?next=...` with an explanatory message, and logged-in users see Font Awesome account/logout icons, name, D1 role badge, `/profil/` account link, and immediate red icon-only logout.
12. UI hints use the shared custom tooltip runtime (`data-fr-tooltip`) instead of browser `title` hints; legacy interactive titles loaded on shared-tooltip pages are upgraded at runtime.
13. Clerk Dashboard webhooks call `/clerk-webhook` for `user.created`, `user.updated`, and `user.deleted`; the endpoint verifies signatures and syncs D1 `users`.
14. D1 role changes initiated by the app use `/user-role`, which mutates D1 and then updates Clerk metadata. Manual SQL changes require `/sync-clerk-metadata` because D1 has no outbound webhook trigger.
15. `/form-submit` uses `functions/_clerk-auth.js` to verify the Clerk token, check D1 roles, sync Clerk metadata from current D1 roles, and attach D1 user ownership to profiles/listings/claims/audit rows.

### 5a. Protected Profile Flow
1. `/profil/` is a static Astro shell that loads the custom Clerk runtime and redirects anonymous users to `/login/?next=/profil/`.
2. `js/profile-page.js` fetches `/profile-data` with the Clerk bearer token, renders a benefit-led next-best-action panel above the tabs, and renders side tabs by D1-authoritative role: franchisee users see the franchisee section, franchisor users see franchisor/listing/leads/claims sections, and admin/staff users see both.
3. If a public user is missing `franchisee` or `franchisor`, `/profil` shows an additive CTA, confirms the change, posts `add_public_role` to `/profile-data`, and redirects to the matching `/daftar` form.
4. Franchisee users see `Peluang Saya`, where `profile-data` recommends published franchises by category/budget, `franchise_saved_opportunities` stores saved listings across devices, and `create_franchise_inquiry` writes one-click interest requests into `franchise_leads`.
5. Franchisor users see `Membership`, where they can create a yearly premium transfer order for an owned listing, renew within 30 days before expiry, receive a unique transfer amount and managed payment instructions, review readiness checks, upload proof of payment, submit payment confirmation, see Premium notifications, and wait for admin review.
6. Franchisor users can use the `Leads` tab to review incoming `franchise_leads` for owned listings, open email/WhatsApp contact shortcuts, and update lead status.
7. Franchisor users can edit Area Listing rows from the listing panel. Saves replace owner-managed `franchise_locations` rows, keep generated rows as fallback, and queue static rebuild requests.
8. Owner media uploads from the listing panel write to R2 through `/profile-upload`, create `franchise_assets`, update the listing media URL field, and queue static rebuild requests.
9. `/profile-data` maps Clerk to D1 through `functions/_clerk-auth.js`, returns D1-authoritative roles and ownership rows, and exposes `/dashboard/` only when the user has `admin` or `staff`.
10. Account name, email, and password are edited one field at a time. Name/email saves update Clerk first, then D1 `users` plus profile identity fields, and password changes use ClerkJS so Google-only users can add password login while password users can change it.
11. Franchisee/franchisor profile edits update existing D1 profile rows and write audit events; first-time completion still belongs to `/daftar/`.
12. Owner listing edits update whitelisted public fields directly in D1, including brand origin and target-market context, are limited to one edit per listing per 6 hours, write audit events, and enqueue `site_rebuild_requests` instead of triggering an immediate build.

### 6. CSV To D1 Import Flow
1. `pnpm run import:csv:dry` parses and validates the three CSV snapshots without writing SQL.
2. `scripts/import-csv-to-d1.ts` uses quote-aware CSV parsing and shared TypeScript Zod row schemas before generating database writes.
3. `UNCLAIMED` rows pass through `isLikelyClaimBrandRow` sanitation to avoid importing URL/phone/address/legal-entity/contact-label noise as brand names.
4. Stable SHA1-derived ids make reruns idempotent for franchises, profiles, packages, publications, and legacy source mappings.
5. `pnpm run import:csv:sql` writes `.context/d1-import-franchise-data.sql`.
6. `pnpm run import:csv:remote` applies the generated SQL to remote D1 through `npx cfman wrangler --account franchise-network`.
7. Imported franchises are published to `site_franchisee_id` via `franchise_site_publications` so later network domains can subscribe/publish without duplicating canonical franchise rows.

### 7. D1 Public Franchise Page Generation Flow
1. `pnpm run build:d1:franchises:dry` queries remote D1 and renders pages in memory to show write/skip/prune counts.
2. `pnpm run build:d1:franchises` reads the `franchise-network` token from the local cfman token store or Cloudflare token env, selects published `site_franchisee_id` rows from `franchise_db`, and writes `json/d1-franchise-static-data.json` so Astro can build static routes from the same D1 source without querying D1 inside route files. The snapshot includes public phone, office address, brand origin, target-market, and progressive canonical listing fields.
3. The script regenerates `json/unclaimed-brands.json` for the claim-search frontend.
4. `pnpm run build:d1:franchises:dry` still renders `/peluang-usaha/index.html` and `/peluang-usaha/{slug}.html` bridge pages in memory from the WordPress-exported templates to validate the bridge renderer without writing files.
5. `pnpm run build:d1:franchises:bridge` is the explicit legacy bridge writer. It writes root `/peluang-usaha/index.html`, flat `/peluang-usaha/{slug}.html`, and `json/d1-generated-pages-manifest.json`.
6. In bridge-write mode, `json/d1-generated-pages-manifest.json` stores content hashes and D1-owned paths; unchanged pages are skipped on rerun.
7. Bridge stale cleanup only removes pages that exist in the previous manifest and still contain the D1-generated marker, including old marker-owned `/peluang-usaha/{slug}/index.html` files when switching to flat `.html` output. Legacy/example pages not owned by the manifest are intentionally left alone.
8. `pnpm run migrate:proposal-r2` is the resumable operational bridge for legacy proposal images. It preserves the franchise/brand mapping, uploads images to R2 under each slug, stores original image URLs in `franchise_assets.legacy_url`, rewrites `franchises.proposal_url` to first-party public URLs, and queues rebuild requests.
9. Proposal image downloads use `/proposal-download` rather than browser canvas. The Function fetches allowlisted proposal image hosts server-side, builds a watermarked PDF at request time, and leaves R2 originals unchanged.

### 7a. D1 Change To Static Publish Flow
1. D1 updates do not automatically trigger Cloudflare Pages builds.
2. A public-page-affecting D1 edit requires a rebuild/deploy (`pnpm run build:astro` in the Pages build) before static HTML changes are visible.
3. `migrations/0003_site_publish_queue.sql` adds `site_rebuild_requests` and `site_publish_state` for D1 dirty tracking.
4. `functions/_site-publish-queue.js` provides `siteRebuildStatements()`; `/form-submit` uses it for franchisor listing submit, claim submit, dev unclaimed creation, and dev test-data cleanup.
5. `.github/workflows/d1-static-publish.yaml` runs every 30 minutes at minutes 7 and 37. It exits when D1 is clean.
6. When D1 is dirty and guardrails allow it, the workflow calls the Cloudflare Pages Deploy Hook. Cloudflare then runs the configured Pages build, including `pnpm run build:astro`.
7. The poller marks requests as `queued` after a successful deploy-hook call. Queued requests older than `stale_queued_after_minutes` are moved back to `failed_retryable` for retry.
8. GitHub direct `dist/` deploy with Wrangler is the fallback/upgrade mode if Cloudflare Pages build quota becomes constrained.
9. The poller must not commit generated output back to `main`, because the connected Cloudflare Pages Git integration can turn that commit into another build.
10. Manual `workflow_dispatch` can force a publish, but normal franchisor edits should wait for the next poll window.

### 7b. Admin/Staff Dashboard Flow
1. `/dashboard` is a static Astro route and should be treated as an internal operations surface for Franchisee.id only.
2. The page loads `js/auth-clerk-debug.js`, `js/auth-clerk-ui.js`, `js/auth-clerk-core.js`, `js/auth-clerk.js`, shared tooltip support, dashboard utility modules, Premium Operations module, Review module, and `js/dashboard-admin.js`; the login-only staff/admin form is deferred and only mounted on the same `/dashboard` URL after the controller confirms login is needed.
3. `js/dashboard-admin.js` calls `/dashboard-data` with a Clerk bearer token and presents Outreach, Data Quality, Review, Operations, and OCR tabs. When the same Clerk user/session has a fresh per-tab cache, the dashboard renders that cached payload immediately and then refreshes live data in the background. Focused modules own Premium, review, operations, and OCR credential workflows.
4. `functions/dashboard-data.js` verifies Clerk on every request and requires D1 role `staff`, therefore allowing elevated `admin`. GET refreshes use an already-synced D1 fast path to avoid repeated Clerk user fetches/metadata writes; first sync, inactive users, or missing roles fall back to the full Clerk-to-D1 sync path. POST actions keep the full sync path before mutations.
5. `_dashboard-queries.js` reads overview metrics, unclaimed outreach rows, outreach summary counts, pending claims, pending edit suggestions, pending premium confirmations with receipt/readiness metadata, Premium operations summaries, editable listing snapshots, persisted data-quality warnings with computed fallback, lead counts, system-health probes, and publish queue state from D1.
6. Admin approval of a premium payment creates or renews the subscription, creates/publishes publication rows for `site_franchisee_id`, `site_franchise_id`, `site_franchisor_id`, and `site_waralaba_id` if needed, records Premium events/notifications, queues owner email notifications, then queues rebuild requests per site.
7. Payment method settings are admin-managed from dashboard and stored in `payment_methods`; profile payment instructions use the managed data with fallback BCA details and can show an admin-uploaded QRIS image.
8. WhatsApp outreach uses staff personal WhatsApp through generated `wa.me` links; no WhatsApp API is involved.
9. Staff must manually confirm a sent WhatsApp message before `_dashboard-actions.js` logs `listing_outreach_events` and an `audit_events` row.
8. Staff edit suggestions are created through guided field rows in the dashboard UI, stored as structured diffs in `listing_edit_suggestions`, and shown to admins as old/new field-level changes.
9. Active `staff_auto_approval_rules` let trusted staff edits apply immediately while still recording the suggestion and audit trail.
10. Claim approvals update `franchise_claims`, attach `owner_user_id`/`franchisor_profile_id` when present, move unclaimed listings to free claimed state, write audit events, and queue static rebuilds.
11. The Data Quality tab can refresh checks, which upserts high-confidence normalized contacts into `franchise_contacts`, upserts current open issues into `franchise_quality_checks`, resolves no-longer-active checks, and records an audit event.
12. OCR provider credentials are saved as encrypted D1 text envelopes using the external Cloudflare Pages secret `OCR_KEY`; dashboard reads only expose `has_api_key` / `has_api_secret` booleans.

### 8. Astro D1 Static Route Flow
1. `pnpm run build:astro` first runs `pnpm run astro:sync`, which refreshes `json/d1-franchise-static-data.json` and `json/unclaimed-brands.json` without writing root bridge HTML.
2. `src/lib/franchise-static.ts` validates the snapshot with the shared D1 static row schema and exposes shared listing/detail renderers while delegating text, contact, ranking, category, Premium, and generated asset behavior to focused helper modules; generated output also normalizes legacy public brand text to Franchisee.id and old support mailbox references to `email@franchisee.id`, and shows brand-origin/target-market facts only for non-Indonesia origins.
3. `src/pages/peluang-usaha/index.astro` renders canonical `/peluang-usaha/index.html`, uses a CSS-only fallback when cover/logo images are missing, includes client-side controls for `q`, `sort`, `kategori`, `status`, and `view=kategori`, and now includes a franchisor CTA that starts with free brand creation before Premium education.
4. `src/pages/peluang-usaha/[slug].astro` uses `getStaticPaths` to generate one flat `.html` detail page per D1 row because Astro is configured with `build.format: "preserve"`. Detail output owns its tab script, CSS-only missing-logo placeholder, readable breadcrumb/category chips, parsed public contact block, and a matching owner CTA below the detail tabs.
5. Legacy directory/archive URLs such as `/direktori-franchise`, `/rekomendasi`, `/populer`, `/abjad`, `/kategori`, `/category`, and top-level category slugs are redirect-only compatibility paths to `/peluang-usaha` query-param states.
6. `scripts/copy-legacy-static.mjs` copies the legacy static export into `dist` without overwriting Astro-generated files, so old CSS/JS/images/static pages remain available.
7. The copy step skips legacy root `/peluang-usaha` and duplicate archive/category folders, and rewrites copied HTML links to canonical `/peluang-usaha` query URLs.
8. Generated and copied legacy HTML is sanitized to remove unused WordPress runtime calls for `analyticswp`, emoji loading, LatePoint front scripts, and `/wp-admin/admin-ajax.php`.
9. Cloudflare Pages build-time D1 reads require `CLOUDFLARE_API_TOKEN`; optional `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_D1_DATABASE_ID` override the current project defaults.

## Business Rules To Preserve
- Never remove or rename form fields without explicit user request and schema updates.
- Keep claim-search sanitization aligned across builder, API fallback, and frontend sanitizer.
- Keep form runtime modular in flat prefixed files; do not revive monolithic `js/form-franchise.js`.
- Keep JSON assets in `/json` and CSV assets in `/csv`.
- Keep static-first public franchise pages for SEO unless a migration plan explicitly replaces generation.
- Keep `/peluang-usaha` as the canonical public directory/search/filter route. Do not create new indexable duplicate archives for recommendation, popularity, alphabetical, category, or legacy directory views unless the canonical route policy is explicitly changed.
- For D1-generated public pages, prune only files tracked in `json/d1-generated-pages-manifest.json` and marked with `d1-generated:franchisee.id`.
- Unclaimed listings may show imported public contact, address, and social data when D1 has it. Display it as public/unclaimed data with a claim CTA; do not require claim ownership before showing existing public phone/address details.
- Phone display from legacy import data is presentation-normalized in `src/lib/franchise-contact.ts`: preserve labels such as Marketing, WA/WhatsApp, Kantor, Call Center, and Owner; distinguish WhatsApp-capable Indonesian/regional mobile numbers, Indonesian landlines, and call-center short numbers where possible; render `tel:` links for numbers; add WhatsApp claim-notification links for unclaimed WhatsApp-capable numbers. Current regional mobile support covers SEA countries plus nearby Asian markets such as China, Hong Kong, Macau, Taiwan, Japan, South Korea, India, Bangladesh, Pakistan, Sri Lanka, and Nepal. The dashboard quality refresh also records high-confidence structured contacts in `franchise_contacts`, while public rendering still reads the existing snapshot fields until the static generator is intentionally moved to the normalized table.
- International franchisor support is explicit listing metadata with low-friction Indonesian defaults: use `franchises.brand_country` and `franchises.target_market` for public trust/admin review; new Indonesian submissions default to Indonesia/Indonesia, phone-country signals can backfill legacy rows, and public country facts/badges are shown only for non-Indonesia origins.
- Imported descriptions are presentation-normalized in `src/lib/franchise-static.ts` when they are mostly uppercase. Keep raw D1 text intact until a reviewed data-cleaning/admin workflow is added.
- Keep `CHANGELOG.md` updated for every file create/update/delete.
- For new migration code, prefer TypeScript plus Zod validation at runtime boundaries.
- D1 table changes must be made through committed SQL migrations.
- Server-side authorization must read D1 roles/permissions; Clerk only supplies identity/session context.
- Clerk metadata must be treated as a cached D1 role snapshot for UI/routing only. Do not authorize server actions from Clerk metadata.
- Pre-first-login admin/staff access should use `email_role_grants` instead of fake `users` rows. The real D1 user must be created from a Clerk user id during auth sync.
- Manual D1 role SQL must be followed by `/sync-clerk-metadata`; normal role changes should go through `/user-role`.
- `/form-submit` must remain D1-only. Do not restore Google Sheets write helpers unless the user explicitly asks for archive/export tooling.

## Known Implementation Gaps
- Email delivery is queued in `notification_email_queue`, but no outbound email provider is connected yet.
- Google Sheets auth and parsing logic is duplicated in builders and functions.
- `functions/form-submit.js` now attaches Clerk-owned D1 users and role checks, but it still needs listing revision workflows, approval state transitions, and asset ownership.
- Login/register now exist as custom Clerk surfaces, but deployed Clerk environment variables and dashboard settings still need production verification.
- D1 write behavior still needs deployed Pages verification against the real binding after frontend submission testing.
- GitHub Actions publish polling needs repository secrets before it can run: `CLOUDFLARE_API_TOKEN` and `PAGES_DEPLOY_HOOK_FRANCHISEE_ID`; optional vars are `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_D1_DATABASE_ID`, and `PAGES_PROJECT_NAME`.
