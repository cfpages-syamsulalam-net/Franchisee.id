---
import { loadFranchiseStaticRows, renderCapitalIndexPage } from "../../../lib/franchise-static";

export const prerender = true;

const html = renderCapitalIndexPage(loadFranchiseStaticRows());
---
<Fragment set:html={html} />
