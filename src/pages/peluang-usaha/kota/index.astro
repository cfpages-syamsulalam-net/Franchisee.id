---
import { loadFranchiseStaticRows, renderCityIndexPage } from "../../../lib/franchise-static";

export const prerender = true;

const html = renderCityIndexPage(loadFranchiseStaticRows());
---
<Fragment set:html={html} />
