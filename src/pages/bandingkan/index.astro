---
import { renderComparisonPage } from "../../lib/franchise-buyer-tools";
import { loadFranchiseStaticRows } from "../../lib/franchise-static";

export const prerender = true;

const html = renderComparisonPage(loadFranchiseStaticRows());
---
<Fragment set:html={html} />
