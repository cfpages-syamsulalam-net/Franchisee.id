---
import { renderBuyerToolsPage } from "../../lib/franchise-buyer-tools";
import { loadFranchiseStaticRows } from "../../lib/franchise-static";

export const prerender = true;

const html = renderBuyerToolsPage(loadFranchiseStaticRows());
---
<Fragment set:html={html} />
