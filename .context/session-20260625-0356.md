# Session 2026-06-25 03:56

## Focus
Planned the next public auth/onboarding/navbar UX pass before implementation.

## User Request
- After public franchisee/franchisor login, replace navbar `Login` and `Daftar` with user name and role/status.
- Reconcile `/login` register tab with the fuller `/daftar` multi-step form.
- Make `/login` registration choose `Daftar sebagai` before Google SSO.
- Make `/login` labels and inputs inline like `/daftar`.
- Use animated segmented/toggle styling for `/login` and `/daftar`.
- Suggest a better visible name for `/daftar` based on content.
- Create a Markdown plan and use it as progress tracker.

## Work Done
- Reviewed form preservation, form schema, Clerk setup, technical inventory, auth runtime, auth CSS, and form tab/navigation modules.
- Added `docs/architecture/AUTH_ONBOARDING_NAV_PLAN.md` as the implementation tracker.
- Updated `docs/README.md`, `docs/architecture/TECH_STACK_DECISIONS.md`, and `CODEBASE.md` so the new tracker is discoverable.

## Key Decisions In Plan
- Keep `/daftar` URL, but present it as `Lengkapi Profil` after account creation.
- Treat `/login` registration as lightweight Clerk identity creation.
- Require public role selection before both email/password registration and Google SSO.
- Redirect new public accounts to role-specific `/daftar` completion.
- Add a small public navbar auth controller rather than hard-coding user state into static HTML.
