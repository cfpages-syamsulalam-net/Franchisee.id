# Session 2026-07-04 00:10 (Asia/Jakarta)

## Summary
- Completed the riskier static renderer split that was still open in `AUDIT.md`.
- Extracted contact rendering/parsing into `src/lib/franchise-contact.ts`, ranking into `src/lib/franchise-ranking.ts`, and category route helpers into `src/lib/franchise-category.ts`.
- Kept `src/lib/franchise-static.ts` as the route-facing facade and reduced it to 499 lines.
- Added `scripts/migrate-blogspot-proposals-to-r2.mjs` plus `pnpm run migrate:proposal-r2`.
- Migrated legacy Blogspot/Blogger proposal images to R2 while preserving mapping to each franchise brand.
- Removed the ignored local proposal image cache after R2 upload; the image cache was about 79.1 MB and the full migration work folder was about 80.8 MB before deletion.
- Added admin-managed QRIS image support for Premium manual payment instructions.

## Proposal R2 Migration
- Source: `json/d1-franchise-static-data.json` `proposal_url` values.
- Migrated: 34 franchises / 491 proposal images.
- R2 object pattern: `franchises/{slug}/proposal/{pageNo}-{hash}.jpg`.
- Public base: `https://assets.franchisee.id`.
- D1 updates applied remotely:
  - Upserted `franchise_assets` rows with `asset_type='proposal'`, `legacy_url`, R2 key, public URL, mime/size, display order, and franchise id.
  - Updated migrated `franchises.proposal_url` values to JSON arrays of R2 public URLs.
  - Queued `site_rebuild_requests` and touched `site_publish_state`.
- Large generated migration work files are ignored under `.context/proposal-r2-migration/`.

## Validation
- `pnpm exec tsc --noEmit --pretty false`: passed.
- `pnpm run build`: passed after the D1/R2 migration and refreshed the static snapshot.
- Remote D1 verification: `proposal_assets=491`, `migrated_franchises=34`.
- Public R2 sample HEAD checks returned HTTP 200 for migrated proposal images.
- Remote D1 migration `0014_payment_method_qris_image.sql`: applied and verified on `franchise_db`.

## Notes
- Payment automation was intentionally skipped. Current Premium payment path remains manual bank transfer with unique amount plus optional admin QRIS image and proof upload.
- Current proposal PDF decision: keep browser PDF generation from first-party R2 images; do not store duplicate PDFs unless production usage later shows performance or sharing problems.
- Implementation method: proposal images are stored once in R2; browser JS loads those images, draws A4 pages to canvas, builds a local PDF Blob, and triggers download without uploading the generated PDF anywhere.
