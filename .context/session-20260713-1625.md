# Session Snapshot - 2026-07-13 16:25 Asia/Jakarta

## Focus
The user asked why `Jumlah staff minimum` was suggested and said the proof excerpt must bold/highlight the exact text used as the basis.

## Implemented
- Added `functions/_proposal-evidence.js`.
  - Sanitizes OCR/proposal text.
  - Returns `{ excerpt, basis }` for field-aware evidence.
  - Handles money fields, staff count, area, royalty percent, and royalty basis.
  - Avoids treating weak standalone numeric matches as proof for fields like `min_staff_count`.
- `functions/_ocr-enrichment-review.js`
  - New OCR bundle evidence now stores `basis` beside `excerpt`.
- `functions/_dashboard-review-evidence.js`
  - Read-time evidence backfill for older proposal/OCR suggestions also stores `basis`.
- `js/dashboard-review.js`
  - `Dasar dokumen` excerpts now highlight the exact basis with `<mark>`.
  - If no field-specific basis is found, Review OCR shows: `Basis spesifik belum ditemukan di excerpt ini. Cek gambar sebelum approve.`
- `css/dashboard-review.css`
  - Added subtle highlight styling for evidence basis and warning styling for weak evidence.

## Validation
- `pnpm run proposal:check` passed.
- `pnpm run dashboard:ocr:check` passed.
- `pnpm run ocr:runner:check` passed.
- `pnpm exec tsc --noEmit --pretty false` passed.

## Notes
- Existing pending suggestions with old evidence but no stored `basis` are refreshed at dashboard read time when matching proposal/OCR knowledge rows exist.
- If no field-specific basis can be found, the UI warns and staff should check the image proof before approving.
