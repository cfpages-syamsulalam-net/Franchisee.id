# Session 2026-07-09 01:03 (Asia/Jakarta)

## Focus
Fix OCR batch behavior when an OCR provider hits rate/quota limits, especially OCR.Space `E553: Rate limit exceeded`, and make Hasil OCR franchise cards display inline as two cards per row when there is enough browser width.

## Changes
- Added migration `migrations/0024_ocr_batch_pause_statuses.sql` so `ocr_batch_runs.status` can store `paused_rate_limit` and `paused_quota`.
- Updated `functions/_ocr-job-runner.js` so provider rate-limit/quota errors:
  - mark provider cooldown/exhaustion,
  - return the current job to `pending`,
  - release already-claimed but unprocessed jobs back to `pending`,
  - refresh the batch as paused instead of failing every remaining page.
- Updated `functions/_ocr-batch-runs.js` to support paused batch progress and to exclude providers still in cooldown from runnable-provider checks.
- Updated `functions/ocr-worker.js` so scheduler redispatch stops when a batch is paused for provider rate limit/quota.
- Updated `js/dashboard-ocr.js` so paused batch statuses have clear labels and cooldown providers do not keep OCR execution buttons enabled.
- Fixed expired cooldown handling in `_ocr-batch-runs.js` and `dashboard-ocr.js` so a provider with `health_status = 'cooldown'` becomes runnable again after `cooldown_until` passes.
- Disabled batch Retry buttons when no provider is currently runnable, avoiding confusing `/dashboard-data` 400 responses from the dashboard.
- Updated `css/dashboard-ocr.css` so Hasil OCR cards use a responsive grid, showing two cards per row when width allows.
- Updated required docs: `CHANGELOG.md`, `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, `docs/architecture/OCR_BATCH_SCHEDULING.md`, and `SUGGESTION.md`.
- Applied remote D1 migration `0024_ocr_batch_pause_statuses.sql` to `franchise_db` via `npx cfman wrangler --account franchise-network d1 execute franchise_db --remote --file migrations/0024_ocr_batch_pause_statuses.sql`.
- Verified remote `ocr_batch_runs` schema now accepts `paused_rate_limit` and `paused_quota`.
- Updated `AGENTS.md` so future necessary SQL migrations are applied to active remote D1 before final reporting unless the user declines or execution is blocked.
- Reset remote D1 `ocr_space` from expired cooldown to `ready`; `last_error` remains visible for troubleshooting.

## Operational note
After deploy, existing batches that failed due to OCR.Space E553 should be retried only after provider cooldown or after enabling another OCR provider. New E553 hits should pause the batch instead of converting every page in the franchise to `failed`.

## Follow-up: Dashboard Login/Cache
- Removed the redundant hidden dashboard auth form mount by adding `data-auth-defer="true"` to the `/dashboard` auth root and making `js/auth-clerk.js` skip deferred roots until `js/dashboard-admin.js` explicitly force-mounts login.
- Added a short `/dashboard` `sessionStorage` cache in `js/dashboard-admin.js` for successful dashboard payloads keyed to the active Clerk user id and active session. The cache renders after Clerk init, then `/dashboard-data` refreshes live.
- Missing/expired authorization clears the dashboard cache and relocks the protected dashboard shell.
- Added follow-up suggestion 77 for a measured backend `/dashboard-data` auth performance pass after the client-side cache behavior is verified in production.

## Follow-up: OCR Batch Countdown
- Added a local countdown chip to `js/dashboard-ocr.js`/`css/dashboard-ocr.css` for scheduler messages like `Trigger QStash terjadwal (10s)`.
- Countdown uses the D1 UTC `updated_at` timestamp plus parsed scheduler delay, updates every second, and remains secondary to the normal `/dashboard-data` polling refresh.

## Follow-up: Suggestions 76 and 77
- Suggestion 76: Audited `TECHNICAL_INVENTORY.md`; `_ocr-job-runner.js` and `ocr-worker.js` now have single canonical OCR runtime blocks, so the backlog item was marked done without another large doc rewrite.
- Suggestion 77: Added `requireD1UserFast()` in `functions/_clerk-auth.js` and routed dashboard GET through it in `functions/dashboard-data.js`.
- Dashboard GET still verifies the Clerk bearer token and enforces D1 `staff`/`admin` roles, but for active already-synced users it avoids Clerk user fetches and Clerk metadata writes.
- Dashboard GET falls back to full sync for missing/inactive users or role mismatches; dashboard POST actions still use full `requireD1User()` sync before mutations.
