# Outreach row icon correction, 2026-09-24 02:00 WIB

The user still saw Simpan kontak, No WA, Stage, New, Open, and published in `/dashboard#outreach` after the first legend deployment. The remaining sources were the header save action, next-action badge, status badge, and metadata chips in `js/dashboard-outreach.js`.

The correction reuses shared dashboard action rendering and `data-fr-tooltip`, moves indicator labels into accessible tooltip text, and expands the legend above the worklist. The editable status selector keeps readable option text. `scripts/check-dashboard-outreach-icons.cjs` renders a sample row and checks the reported labels. The focused check, `node --check js/dashboard-outreach.js`, and `pnpm exec astro build` passed. Live JavaScript had a four-hour browser cache limit, so the dashboard now references a versioned script URL to deliver the new indicators immediately. No further suggestion for this session after checking the pipeline metadata source, row consumers, shared tooltip load, and cache behavior. Follow up with Git commit and push, remote parity, and live deployment verification.
