# Session Snapshot - 2026-07-12 21:32 Asia/Jakarta

## Focus
Clarified the dashboard review split after the user noticed Pending Edit Review and Review OCR looked like the same OCR source. Also improved Review diff readability for money/numeric values and short text casing.

## Implemented
- `js/dashboard-review.js`
  - `proposal_extraction` and `ocr_enrichment_bundle` are now treated as document-derived suggestions and rendered in Review OCR.
  - Pending Edit Review now excludes document-derived suggestions and is reserved for manual/staff edits.
  - Brand cells render a clear actor/source line:
    - manual/staff rows show `Diusulkan: ...`
    - document-derived rows show `Sumber: OCR terstruktur` or `Sumber: Ekstraksi proposal`
  - `_idr` fields display as `Rp` with Indonesian thousands separators.
  - `_percent` fields display with `%`.
  - Other numeric fields display with Indonesian separators.
  - Short classification fields such as category, outlet type, city origin, brand country, and target market are title-cased for display.
- `css/dashboard-review.css`
  - Brand/source cells are forced into a vertical grid so the email/source line always appears below the franchise name.
  - Manual review badges use a distinct blue tone from OCR/proposal badges.
- `functions/_ocr-enrichment-review.js`
  - New OCR bundle short text candidates are normalized to title case before creating the pending suggestion.
  - Example: `RUKO, booth` becomes `Ruko, Booth`.
- Regression checks now assert proposal/OCR routing, money formatting fragments, manual source badge styling, and backend OCR candidate case normalization.

## Validation
- `pnpm run dashboard:ocr:check` passed.
- `pnpm run ocr:runner:check` passed.

## Notes
- Existing pending OCR/proposal suggestions can still have raw stored values, but the Review UI normalizes display casing and numeric formatting.
- New grouped OCR bundle suggestions will store normalized short classification values.
