# Session 2026-07-05 18:03 (Asia/Jakarta)

## Focus
Fixed dashboard Data Quality false-positive `suspicious_contact` checks caused by weak imported contact parsing.

## Changes
- Strengthened `functions/_dashboard-utils.js`:
  - Added `parsePhoneContacts()` for generic structured contacts.
  - Kept `parseWhatsAppContacts()` as a WhatsApp-capable filter for outreach.
  - Recognizes Indonesian mobile multi-number strings, Taiwan mobile numbers such as `886972940831`, Indonesian landlines, and call-center short numbers.
  - Avoids treating middle groups such as `0937` inside `0878-0937-1485` or `0556` inside `0813 2386 0556` as separate contact starts.
- Updated `functions/_contact-normalization.js` so Data Quality refresh upserts structured phone contacts, while WhatsApp source rows stay WhatsApp-capable only.
- Mirrored parser behavior in `src/lib/franchise-contact.ts` so generated public detail pages render the same contact edge cases.
- Added `scripts/check-contact-parser.mjs` and `pnpm run contact:check` for regression coverage.
- Generated `.context/contact-quality-cleanup-20260705-remote.sql` and applied it to remote D1.

## Remote D1 Cleanup
- Database: `franchise_db`
- Upserted structured contacts: 65
- Resolved false-positive open `suspicious_contact` checks: 36
- Verification query after cleanup: open `suspicious_contact` count for `site_franchisee_id` is `0`.
- Spot checks confirmed:
  - Keding: `+886 972 940 831`
  - dr. laundry: `0851 8455 0162`, `0812 8441 5836`
  - Geprek Bensu: `Call Center: 1500 612`
  - JNE Express: `(021) 2927 8888`

## Notes
- The remote cleanup resolves the current dashboard data. Deploying the code change is still needed so future Data Quality refreshes do not recreate the old false positives.
