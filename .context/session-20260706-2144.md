# Session 2026-07-06 21:44 (Asia/Jakarta)

## Focus

Generated franchise detail listing page tab/hero consolidation.

## Completed

- Planned the follow-up UX decisions in `docs/ux/LISTING_DETAIL_UX_AUDIT.md`.
- Hid the legacy hero image/title block and removed the inline hero background-image binding from `templates/detail-franchise-tpl.html`.
- Promoted the visible information-section brand heading to the page H1.
- Moved the logo/social/fact summary into the `Profil` tab.
- Moved the description into a dedicated `Detail` tab.
- Added a shared `Investasi` tab from existing D1 fields.
- Kept optional `Support`, Premium media, `Brosur`, FAQ, and `Kontak` tabs in the same connected shell.
- Extracted shared tab composition into `src/lib/franchise-detail-tabs.ts` so Astro and the D1 bridge do not duplicate tab markup/investment logic.
- Restyled the tab shell in `src/lib/franchise-detail-assets.ts` so the active tab connects visually to full-width tab content.
- Switched comparison/net-profit icons to older Font Awesome-compatible classes.
- Updated `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, and `CHANGELOG.md`.

## Validation status

Passed:

- `pnpm exec tsc --noEmit --pretty false`
- `pnpm run build:astro`
  - D1 bridge fetched 197 listings and wrote 197 detail pages.
  - Astro built 276 pages.
- Line-count check after touching files over 500 lines:
  - `templates/detail-franchise-tpl.html`: 1269
  - `src/lib/franchise-detail-assets.ts`: 1276
  - `src/lib/franchise-static.ts`: 686
  - `src/lib/franchise-static-assets.ts`: 600
  - `TECHNICAL_INVENTORY.md`: 824
  - `CODEBASE.md`: 368
  - `CHANGELOG.md`: 2052
- Generated HTML spot-checks confirmed:
  - `dist/peluang-usaha/keding.html`, `peluang-usaha/keding.html`, and `dist/peluang-usaha/abo-meatshop.html` each contain exactly one `<h1`.
  - `Keding` output includes the promoted detail H1, connected tab shell CSS, `Profil`, `Detail`, `Investasi`, `Brosur`, and `Kontak` tabs, profile panel inside tabs, investment cards, and `fa-balance-scale` compare icons.
  - `Abo Meatshop` output includes `Brosur Abo Meatshop`, proposal image metadata, and browser PDF controls.
  - No `fa-scale-balanced`, `fa-arrow-trend-up`, `fa-circle-info`, or hero `background-image: url(...)` matches in checked source/generated files.
- `git diff --check -- templates/detail-franchise-tpl.html src/lib/franchise-detail-tabs.ts src/lib/franchise-detail-assets.ts src/lib/franchise-static.ts src/lib/franchise-static-assets.ts src/lib/franchise-detail-summary.ts src/lib/franchise-premium-detail.ts scripts/d1-page-renderer.ts docs/ux/LISTING_DETAIL_UX_AUDIT.md CODEBASE.md TECHNICAL_INVENTORY.md CHANGELOG.md .context/session-20260706-2144.md`
  - No whitespace errors; Git reported only LF/CRLF normalization warnings on Windows.
