# Session 2026-06-24 23:23

## Focus
- Investigated `/dashboard` auth/debug output and the mismatch between 197 total listings, 196 unclaimed listings, and 75 visible Unclaimed Outreach rows.

## Findings
- Auth flow looked healthy in the provided browser log: Clerk loaded, user/session existed, a bearer token was resolved, and `/dashboard-data` returned HTTP 200.
- The outreach mismatch came from the read model, not auth. `getUnclaimedOutreachQueue()` filtered to published unclaimed listings with non-empty `f.phone` and also had a hard `LIMIT 75`.
- The static D1 snapshot in `json/d1-franchise-static-data.json` still has 197 total rows and 196 unclaimed rows, matching the dashboard overview.

## Changes
- Raised the outreach queue cap to 250 rows in `functions/_dashboard-queries.js`.
- Added `getUnclaimedOutreachSummary()` with published unclaimed, contact-ready, missing-phone, and queue-limit counts.
- Added `outreach_summary` to `functions/dashboard-data.js` GET responses.
- Updated `js/dashboard-admin.js` to show visible outreach rows against contact-ready and published-unclaimed totals instead of a raw row count.
- Updated `CHANGELOG.md`, `CODEBASE.md`, and `TECHNICAL_INVENTORY.md`.

## Verification
- `node --check js/dashboard-admin.js` passed.
- `pnpm run build` passed. The build rewrote generated public franchise pages as part of the normal D1 sync; those generated files were restored afterward to keep this patch focused.
- `pnpm exec wrangler pages functions build functions --outdir <temp>` passed and compiled the Pages Functions successfully.

## Next Checks
- After deploy, verify the dashboard shows a badge like `N dari M kontak siap / P unclaimed published`.
