# Session 2026-07-05 23:27 (Asia/Jakarta)

## Goal
Make origin/target-market support low-friction for Indonesian franchisors while still supporting overseas franchisors seeking Indonesian partners.

## Implemented
- Added `docs/architecture/INTERNATIONAL_FRANCHISOR_POLICY.md`.
- Expanded supported country-code options across `/daftar` static fallbacks, `json/country-codes.json`, and form JS defaults.
- Collapsed `brand_country` / `target_market` under “Brand dari luar Indonesia?” in the franchisor listing form.
- Defaulted Indonesian submissions to `brand_country=Indonesia` and `target_market=Indonesia`.
- Open/prefill origin fields when a non-Indonesia contact country code is selected.
- Expanded client WhatsApp validation plus dashboard/public contact parsing to supported SEA and nearby Asian mobile numbers.
- Public generated pages now show flag-labeled origin/target facts only for non-Indonesia origins.
- Added migration `0018_backfill_franchise_origin_target_market.sql` and applied the remote D1 backfill.

## Remote D1 Result
- Backfill first updated 193 rows, then 32 rows were corrected after verification showed local Indonesian numbers could match loose non-Indonesia substrings.
- Final distribution:
  - 192 rows: `Indonesia` / `Indonesia`
  - 1 row: `Taiwan` / `Indonesia` (`Keding`, phone `886972940831`)
  - 4 rows remain blank because no clear contact-country signal exists.

## Validation
- `pnpm run contact:check`: passed, 26 cases.
- `pnpm run profile:check`: passed.
- `pnpm exec tsc --noEmit --pretty false`: passed.
- `pnpm run build:astro`: passed, 276 pages built.
- Spot check: `dist/peluang-usaha/keding.html` renders `Asal Brand` as `🇹🇼 Taiwan`; Indonesian example `AHA Right Brain` remains `Indonesia` in the snapshot and does not get a public non-Indonesia fact block.

## Follow-up
- Suggestion 60 tracks centralizing country metadata so country names, dial codes, aliases, and flags do not drift across form, backend, parser, and renderer layers.
