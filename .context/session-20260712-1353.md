# Session Snapshot - 2026-07-12 13:53 Asia/Jakarta

## Focus
Continued OCR Review UX work after the dedicated Review OCR section was added. The user reported the Review OCR icon was missing and asked for approvable OCR suggestions to show a trustworthy basis: OCR text or brochure image, preferably through the shared hover-preview component.

## Implemented
- Replaced unavailable `fa-list-check` icons in the dashboard shell with `fa-clipboard-list`.
- New `ocr_enrichment_bundle` suggestions now store source evidence under `old_value.__ocr_evidence`.
  - Evidence includes field label/value, source count, conflict count, OCR excerpt, page number, and brochure/source image URL when available.
  - Excerpts are passed through the shared proposal source-text sanitizer before they are stored in the suggestion metadata.
  - `suggested_value` remains the normal canonical field payload, so the existing admin approval sanitizer/application path is unchanged.
- `js/dashboard-review.js` now renders OCR evidence per changed field:
  - OCR excerpt appears inline below the old/new diff.
  - Source page/count/conflict metadata appears beside it.
  - A `Gambar dasar` pill uses `data-ocr-image-preview-url` and `data-ocr-image-preview-alt`.
- `js/dashboard-admin.js` passes the OCR Review table to the OCR module.
- `js/dashboard-ocr.js` binds the existing delegated OCR image-preview component to the Review OCR table.
- `css/dashboard-review.css` adds compact responsive styles for the evidence rows.
- `scripts/check-dashboard-ocr-client.mjs` now asserts the OCR Review preview binding and evidence UI fragments.

## Validation
- `pnpm run dashboard:ocr:check` passed.
- A focused marker scan found no remaining `fa-list-check` usage and no `data-fr-tooltip` conflict on OCR image-preview actions.
- `js/dashboard-review.js` line count checked at 568 lines; `AUDIT.md` records the future split plan.

## Documentation Updated
- `CHANGELOG.md`
- `CODEBASE.md`
- `TECHNICAL_INVENTORY.md`
- `AUDIT.md`
- `SUGGESTION.md`
- `docs/architecture/OCR_LISTING_ENRICHMENT_PLAN.md`

## Notes
- Existing pending OCR bundle suggestions created before this change may not have `old_value.__ocr_evidence`; new bundles will.
- No D1 migration was needed.
