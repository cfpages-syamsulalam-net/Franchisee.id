# Session Context - 2026-07-18 02:35 Asia/Jakarta

## Request
Complete the category permalink migration after suggestion 111: redirect old `/peluang-usaha?kategori=...` URLs to canonical slug routes, replace every old internal link, and strengthen `AGENTS.md` so logically necessary follow-through is not omitted from future suggestions.

## Decisions
- Cloudflare Pages `_redirects` cannot match source query parameters, so an exact `functions/peluang-usaha/index.js` route owns the HTTP 301 behavior.
- The handler removes only `kategori`, preserves search/sort/status or other unrelated query state, and falls through to the Astro static asset when no legacy category exists.
- `src/shared/franchise-category-route.mjs` is the single executable alias/slug/path contract across Cloudflare and Astro; `.d.mts` supplies TypeScript types.
- Generated browser code keeps a `window.location.replace()` fallback for local/static contexts, but production SEO migration relies on the server 301.
- Canonical-path aliases such as `/peluang-usaha/kategori/fnb` also redirect to their consolidated destination.

## Implementation
- Updated Astro helpers, generated browser behavior, legacy builders, detail template, redirect rules, and focused checks.
- Mechanically migrated 198 tracked `peluang-usaha/*.html` files; 795 old category-query links were removed and a final runtime scan returned zero matches.
- Added suggestion 112 as Done and strengthened `AGENTS.md` with dependency-aware suggestion and canonical-route migration completion rules.
- Added suggestion 113 for post-deploy one-hop 301 and Search Console monitoring; this remains operationally pending because local builds cannot prove production edge routing or crawler recrawl state.
- Updated SEO plan, UX audit, main audit, codebase map, technical inventory, and changelog.

## Validation
- `pnpm run directory:check`: Passed for 197 listings and 14 category routes, including redirect, fallthrough, browser fallback, producer, and tracked-snapshot checks.
- `pnpm exec tsc --noEmit --pretty false`: Passed.
- `pnpm run astro:check`: Passed with 0 errors and the same five pre-existing hints.
- `pnpm exec wrangler pages functions build functions ...`: Compiled the complete Worker successfully; temporary output was removed.
- `pnpm exec astro build`: Built 273 pages successfully.
- Generated output scan: zero HTML files retained the old query-category URL; canonical links, fallback script, alias map, canonical food page, and absence of the duplicate FnB page were confirmed.
- Build-only `dist`, `.astro`, and Wrangler temporary output were removed after inspection. Final `git diff --check` passed with line-ending notices only; the runtime old-link scan returned zero matches, the large detail template retained its 1,276-line count, and all 198 snapshot diffs have equal additions/removals with no truncation signal.
