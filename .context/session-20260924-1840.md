# Public franchise export restriction

- Starting commit: `e938a5f` on `main`; prior brand-match change already deployed.
- `/get-franchises` selected D1 `f.*` and spread `raw_payload` into anonymous results. `source=sheets` had no published-listing constraint and failed with an external auth error on production.
- Changed the endpoint to explicit D1 published-listing fields, denied Sheets reads, preserved unclaimed claim-search mapping, removed unused auth/profile helpers; the static generator no longer selects raw payload, its schema strips that field, and the tracked snapshot was rebuilt without it. The GitHub repository is public; older commits still contain raw applicant fields, now tracked under suggestion 119.
- Focused privacy check and Astro build passed locally. Commit, push, remote parity and live request checks pending at snapshot time.
- Data follow-up: suggestion 119, address historical public Git blobs. Next code gate: suggestion 117, hold new-brand submissions until independently reviewed. Then run controlled persona testing using disposable identities and listings as documented in `docs/product/NEXT_STEPS.md`.
