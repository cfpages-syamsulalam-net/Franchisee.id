# Session 2026-06-19 21:53 Asia/Jakarta

## Focus
Expanded the D1-backed Astro public directory so legacy navigation routes work from the D1 snapshot instead of stale static exports.

## Implemented
- Added D1-backed Astro routes for `/rekomendasi`, `/populer`, `/abjad`, `/kategori`, `/kategori/[slug]`, `/category/[slug]`, and top-level category slugs.
- Extended `src/lib/franchise-static.ts` with route-specific listing metadata, recommendation/popular/abjad ordering, category summaries, category alias generation, and category cards.
- Added CSS-only franchise/category placeholders for cards that have no D1 cover/logo URL, avoiding missing WooCommerce placeholder image 404s.
- Kept `/peluang-usaha/[slug]` detail output flat as `.html`; category archive routes also build as flat `.html` files under Astro `build.format: "preserve"`.

## Verification
- `pnpm run build` completed and generated 259 Astro pages before copying legacy static assets into `dist`.
- Representative generated outputs existed: `dist/rekomendasi/index.html`, `dist/populer/index.html`, `dist/abjad/index.html`, `dist/kategori/index.html`, `dist/kategori/makanan-minuman.html`, and `dist/peluang-usaha/index.html`.
- `dist/peluang-usaha/index.html` contained `.franchise-css-placeholder` markup and no longer depended on a missing WooCommerce placeholder image for empty image slots.

## Notes
- `/populer` currently uses a deterministic listing-quality proxy. True popularity should later use D1 metrics such as views, saves, leads, payments, admin boosts, or network distribution data.
- Category aliases currently include `makanan-minuman-fb`, `perhotelan-travel`, `properti-furniture`, `teknologi-digital`, and `jasa-layanan`.
