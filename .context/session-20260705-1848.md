# Session 2026-07-05 18:48 (Asia/Jakarta)

## Focus
Expanded Franchisee.id contact/onboarding support for regional franchisors, resolved Keding's stale `missing_contact` Data Quality warning, and implemented explicit listing metadata for brand origin and target market.

## Findings
- Keding had an active structured contact row:
  - `phone`: `+886 972 940 831`
  - normalized: `886972940831`
- The remaining Data Quality issue was a stale persisted `missing_contact` warning from the earlier scan, not a missing structured contact.

## Changes
- Resolved stale remote D1 `missing_contact` warnings that already have active structured contact rows.
- Expanded contact parsing in `functions/_dashboard-utils.js` and `src/lib/franchise-contact.ts` for:
  - Indonesia `+62`
  - Malaysia `+60`
  - Singapore `+65`
  - China `+86`
  - Hong Kong `+852`
  - Taiwan `+886`
  - Vietnam `+84`
- Added China, Hong Kong, Taiwan, and Vietnam to `json/country-codes.json` and the form fallback country-code list.
- Made form WhatsApp validation country-aware so 8-digit Singapore/Hong Kong mobile numbers and exact regional local lengths are accepted.
- Expanded `pnpm run contact:check` regression coverage for regional mobile formats.
- Added and remotely applied `migrations/0017_franchise_origin_target_market.sql` for `franchises.brand_country` and `franchises.target_market`.
- Added optional `brand_country` and `target_market` fields to the franchisor `/daftar` form, form-submit schemas/binds, owner `/profil` listing editor, dashboard guided edit fields, profile read model/patch schema, D1 static snapshot query/schema, and public card/detail fact rendering.

## Remote Verification
- Keding `missing_contact`: `resolved`
- Franchisee.id open `missing_contact` count: `0`
- Remote D1 migration `0017_franchise_origin_target_market.sql`: applied successfully.

## Follow-up
- Suggestion 58 is implemented. Existing imported listings can now be enriched with brand origin and target market through owner/admin listing edits.
