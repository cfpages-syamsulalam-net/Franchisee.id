# Session Snapshot - 2026-07-13 17:43 Asia/Jakarta

## Focus
The user asked for Outreach to bulk-save franchisor names/numbers into the currently linked Google account before WhatsApp outreach, and asked to clean public `/daftar` copy that sounded like internal/developer guidance.

## Implemented
- Added `functions/_google-contacts.js`.
  - Rebuilds the current unclaimed outreach queue server-side.
  - Converts up to 200 ready WhatsApp contacts into Google People API batch-create payloads.
  - Retrieves the staff member's Google OAuth access token from Clerk.
  - Searches existing Google Contacts first and skips duplicate phone numbers.
  - Calls `people:batchCreateContacts`.
  - Returns setup-required errors if Google Contacts scope/token/People API access is missing.
- Added dashboard action `save_outreach_google_contacts`.
  - Schema lives in `functions/_dashboard-schemas.js`.
  - Router lives in `functions/dashboard-data.js`.
- Added Outreach UI button.
  - `src/pages/dashboard/index.astro` has an Outreach header action container.
  - `js/dashboard-admin.js` passes it into operations.
  - `js/dashboard-operations.js` renders the button with shared `renderPillActionButton`.
  - `css/dashboard.css` adds pill primary/busy/done states and header action layout.
- Added `scripts/check-google-contacts.ts` and `google-contacts:check`.
- Rewrote `/daftar` brand-origin helper copy to be public-facing.
- Strengthened `AGENTS.md` public-copy rule and scan checklist.

## Manual Setup Needed Before Production Use
- Enable Google People API for the Google OAuth project used by Clerk.
- Add `https://www.googleapis.com/auth/contacts` to the Clerk Google social connection.
- Staff must login again with Google after the scope is added.
- If setup is missing, the dashboard action returns an actionable setup-required error.

## Duplicate Prevention
- `SUGGESTION.md` ID 88 is implemented.
- The action sends a People API `searchContacts` warmup request, searches each phone number, normalizes `+62` and local `08...` formats, and skips contacts already present in the staff member's Google Contacts.
