# Session Snapshot - 2026-07-05 16:19 (Asia/Jakarta)

## Focus
- Fixed `/dashboard` failing after valid login with `D1_ERROR: too many SQL variables at offset 511: SQLITE_ERROR`.

## Diagnosis
- Auth was working: Clerk session existed and D1 returned the user with `admin` role.
- The failure happened in dashboard GET composition, most likely while loading editable listing structured locations.
- `getEditableListings()` can load 250 listing rows, then `getStructuredLocationsForListings()` previously built one `IN (?, ...250)` query, which can exceed D1/SQLite bind-variable limits.

## Changes Made
- `functions/_dashboard-queries.js`
  - Added chunked structured-location reads for editable dashboard listings.
- `functions/_profile-franchisor-actions.js`
  - Added chunked owned-listing location and publication reads to prevent the same bind-limit class of bug in `/profil`.
- `functions/_profile-premium.js`
  - Added chunked owned-listing Premium membership reads for orders, subscriptions, and readiness.
- `CODEBASE.md` and `TECHNICAL_INVENTORY.md`
  - Documented chunked D1 ID-query behavior.
- `SUGGESTION.md`
  - Added suggestion 55 for a high-volume dashboard read-model smoke test.
- `CHANGELOG.md`
  - Recorded this bugfix session.

## Validation
- Planned:
  - line counts for touched backend/docs files
  - `pnpm run profile:check`
  - `pnpm exec tsc --noEmit --pretty false`
  - `pnpm exec astro build`
  - `git diff --check`

