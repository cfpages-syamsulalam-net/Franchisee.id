# Session 2026-07-12 02:48 (Asia/Jakarta)

## Focus
Replay structured enrichment over OCR proposal knowledge now that the larger OCR result set is in remote D1, while filtering known source-noise watermarks from stored OCR text and dashboard previews.

## Changes
- Expanded `functions/_proposal-knowledge.js` candidate extraction for area, staff, setup, investment/fee/working-capital, contract, BEP range, monthly omzet/profit, HPP, royalty basis/period, target market, and support signals.
- Added `sanitizeProposalSourceText()` and used it before proposal knowledge storage, extraction, replay, and OCR result preview output.
- Added `scripts/enrich-ocr-structured-data.ts` plus `pnpm run ocr:enrich:structured` for replayable remote D1 enrichment.
- Generated `.context/ocr-structured-enrichment-fetch.sql` and `.context/ocr-structured-enrichment.sql`.
- Applied the replay to remote D1 `franchise_db`; structured candidate rows increased from 56 to 164 of 479 extracted OCR rows, and the filtered source marker verified at 0 remaining rows.

## Validation
- `pnpm run proposal:check`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm run ocr:enrich:structured -- --apply-remote`
- `pnpm run ocr:enrich:structured` verified `changed=0` after remote apply.

## Follow-Up
- Suggestion 87 records a grouped per-franchise OCR enrichment review queue so admins can approve merged candidates without flooding the existing page-level suggestion list.
