# Session 2026-07-12 05:50 (Asia/Jakarta)

## Focus
Separated OCR review from generic listing review and improved the pending review table layout.

## Changes
- Moved the generic Brand/Diff/Reason/Aksi pending edit review table out of the direct-edit form panel into its own full-width `Pending Edit Review` panel.
- Added a fourth OCR guide card, `Review OCR`, with a dedicated full-width pending OCR bundle table.
- Updated `js/dashboard-review.js` so `ocr_enrichment_bundle` rows render only in OCR Review, while generic edit suggestions stay in the Review tab.
- Improved diff rendering with icon-led field rows and old-to-new value blocks so long values are easier to scan.
- Added shared dashboard text-pill action helpers in `js/dashboard-utils.js` and `.dash-pill-action` styles in `css/dashboard.css`.
- Changed OCR enrichment queue source/listing/`Buat Review` actions to use shared pill helpers.
- Changed OCR bundle creation to open the OCR tab and `Review OCR` subtab after reload.
- Added an `AGENTS.md` rule requiring shared dashboard pill action helpers for similar text-pill actions.

## Validation Surface
- `pnpm run dashboard:ocr:check` now syntax-checks dashboard utils/review modules in addition to OCR modules and asserts the OCR Review subtab, shared pill helpers, and OCR bundle split fragments.

## Follow-Up Notes
- `src/pages/dashboard/index.astro` is 962 lines; before another dashboard layout feature, extract dashboard review/OCR panels into Astro components.
- `js/dashboard-ocr.js` is 1,429 lines; before another OCR control workflow, extract action handlers into a dedicated helper.
- `js/dashboard-review.js` is 587 lines; split review table/diff rendering if another review/location workflow is added.
