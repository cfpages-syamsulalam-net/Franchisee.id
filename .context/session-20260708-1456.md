# Session 20260708-1456

## Scope
- Continued OCR dashboard UX polish after provider toggle/retry implementation.
- Audited `/dashboard` operator UX for OCR job rows and auth loading state.

## Implementation Notes
- Added a dashboard auth loading skeleton shown while Clerk/session/dashboard access is being checked.
- Kept the login form hidden until auth flow confirms there is no usable session, the session expired, or an auth error needs manual login.
- Changed OCR failed-row action from queue-only retry into direct per-row OCR execution:
  - resets the failed job to `pending`;
  - immediately calls the bounded OCR runner for that job;
  - keeps OCR attempt history.
- Kept batch failed retry as queue-only because it can affect many rows; the UI now labels it as "Antre ulang gagal" with tooltip instructions.
- Reworked recent OCR job rows into icon-led cards: status chip, page/provider/attempt metadata, error icon, and compact action chips for listing/result/direct OCR.
- Refactored dashboard styling by keeping `css/dashboard.css` as base shell/layout styles and extracting auth loading styles to `css/dashboard-auth.css` plus OCR-specific styles to `css/dashboard-ocr.css`.

## Validation Direction
- Use browser `node --check` only for non-module dashboard clients.
- Use OCR repo checks and Wrangler Functions build for Cloudflare Functions ESM.
