# Session Snapshot - 2026-07-11 06:39 (Asia/Jakarta)

## Focus
- Calibrated OCR provider quota behavior so large OCR runs use the combined free quota of active providers instead of the old hidden 100-job worker cap.
- Preserved individual provider quota/rate guards so a provider whose own cap is reached is skipped even when other active providers still have quota.
- Added an AGENTS.md rule to auto-detect long touched/related files each implementation session and record refactor findings/plans in AUDIT.md.

## Key changes
- Added source-linked OCR provider limit metadata in `src/lib/ocr-provider-metadata.js`.
- Exposed provider limit details through `functions/_ocr-provider-config.js`.
- Added `migrations/0029_ocr_provider_actual_limits.sql` to calibrate D1 provider limits/rate guards without touching credentials/enabled state.
- Changed `functions/_ocr-job-runner.js` to compute active-provider combined quota and let resettable exhausted providers return after reset.
- Changed `functions/ocr-worker.js` to use combined provider quota and keep `OCR_WORKER_DAILY_CAP` as an optional safety cap only.
- Implemented suggestion 85 by extracting combined quota, provider quota prep, reset-aware provider re-entry, and quota increment statements into `functions/_ocr-quota-policy.js`; `_ocr-job-runner.js` is now about 1,135 lines.
- Updated dashboard OCR provider/worker renderers to show per-provider remaining quota, source-linked limit notes, combined active capacity, account-specific provider counts, and safety-cap context.
- Added OCR runner regression coverage for combined quota summing of active known-limit providers and separate account-specific provider reporting.
- Updated OCR strategy/scheduling docs, code maps, audit tracker, suggestion backlog, and changelog.

## Follow-up noted
- The quota/cap extraction from `_ocr-job-runner.js` is complete. Future OCR runner refactors should only be added to AUDIT.md if a new responsibility grows inside the runner again.

## Validation so far
- `pnpm run ocr:check` passed.
- `pnpm run dashboard:ocr:check` passed.
- `pnpm run ocr:runner:check` passed.
- `pnpm exec tsc --noEmit --pretty false` passed.
- Remote D1 `franchise_db` migration `0029_ocr_provider_actual_limits.sql` was applied successfully and verified for OCR.Space, Google Vision, Mindee, and PDF.co quota fields.
