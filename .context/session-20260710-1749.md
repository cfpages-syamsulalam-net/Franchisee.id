# Session 2026-07-10 17:49 (Asia/Jakarta)

## Focus
- Make dashboard OCR execution reliable when the browser tab/window is inactive.
- Improve OCR batch status copy/countdown so admins see whether the scheduler is waiting or processing.
- Use multiple active OCR providers for bounded concurrent throughput while preserving provider fallback and rate-limit pause behavior.

## Changes
- `Jalankan OCR` now starts a persisted scheduler-backed OCR batch when an active scheduler exists. This is the reliable path and continues server-side even if the admin changes tab/window.
- Browser continuous OCR remains as a fallback only when no scheduler is active, with explicit UI copy that the tab should stay open/active.
- Dashboard OCR state now refreshes when a hidden/background tab becomes active again so progress does not look stuck after returning to the tab.
- Batch rows render dynamic status chips such as `Menunggu 35d` and `Memproses`, with descriptive background-processing copy instead of overdue-warning text.
- `_ocr-job-runner.js` now processes bounded concurrent waves when multiple OCR providers are active by rotating each job's first-choice provider. Existing fallback, text-too-short handling, quota guards, and rate-limit pause behavior are preserved.

## Validation notes
- Use repository checks for Functions/TypeScript. `node --check` remains valid only for browser non-module files in this repo.
