# Session Context - 2026-06-08 05:45 (Asia/Jakarta)

## User Goal
Audit the Franchisee.id codebase, document project-owned logic, and create a migration tracker for moving the current static/Google Sheets-backed site toward an interactive Cloudflare stack using D1, R2, and Clerk.

## Work Completed
- Created `CODEBASE.md` as a living map of relevant runtime, builder, function, data, workflow, and template files.
- Created `AUDIT.md` as the technology migration audit and progress tracker.
- Updated `AGENTS.md` with a rule to keep `CODEBASE.md` current and added `CODEBASE.md`/`AUDIT.md` to the root instruction index.
- Logged the documentation updates in `CHANGELOG.md`.

## Key Findings
- Current backend is Cloudflare Pages Functions wrapping Google Sheets reads/writes.
- Static builders generate `/peluang-usaha/`, detail pages, sitemap, and `json/unclaimed-brands.json`.
- Form runtime is modular in `js/form-01-*` through `js/form-09-*`, with shared helpers in `js/form-utils.js`.
- Claim search sanitization is duplicated across builder, function, and frontend modules and must remain aligned.
- `js/build-sitemap.js` still has naive CSV fallback parsing, unlike `js/build-listing.js`.
- Recommended target is Astro on Cloudflare with D1, R2, Clerk, and existing CSS preserved; Next.js remains an alternate if dashboard complexity demands it.

## Worktree Note
Before this session's edits, there were already uncommitted changes in `.github/workflows/generate-pages.yaml`, `AGENTS.md`, `KNOWLEDGE.md`, `QWEN.md`, `package.json`, and `pnpm-lock.yaml`. This session only made targeted additions and did not revert unrelated work.

