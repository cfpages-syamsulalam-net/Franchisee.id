# Session Snapshot - 2026-06-18 18:48 Asia/Jakarta

## Implemented D1-To-Static Publish Queue

- Added `migrations/0003_site_publish_queue.sql`.
- Remote D1 `franchise_db` migration was applied successfully with `npx cfman wrangler --account franchise-network d1 migrations apply franchise_db --remote`.
- Verified `site_publish_state` has `site_franchisee_id`, `publish_mode='cloudflare_deploy_hook'`, `daily_publish_limit=12`, and `min_publish_interval_minutes=30`.

## Runtime Queue Wiring

- Added `functions/_site-publish-queue.js`.
- Updated `functions/form-submit.js` to enqueue rebuild requests for:
  - Franchisor listing submit.
  - Claim submit.
  - Dev unclaimed listing creation.
  - Dev test-data clearing.
- Franchisee lead submissions do not enqueue rebuild requests because they do not affect public static listing pages.

## GitHub Actions Poller

- Added `scripts/d1-static-publish-poller.mjs`.
- Added `pnpm run publish:d1:poll`.
- Added `.github/workflows/d1-static-publish.yaml`.
- Workflow schedule: minutes 7 and 37 every hour.
- Default publish mode: poll D1, call Cloudflare Pages Deploy Hook only when dirty and guardrails allow it.
- Fallback mode: if `site_publish_state.publish_mode='github_direct_deploy'`, build Astro in GitHub and deploy `dist/` with Wrangler without committing generated output.

## Required GitHub Setup

Before the poller works in GitHub, add repository secrets:

- `CLOUDFLARE_API_TOKEN`: token with D1 query/write access for `franchise_db`; also used by Wrangler in direct deploy fallback.
- `PAGES_DEPLOY_HOOK_FRANCHISEE_ID`: full Cloudflare Pages Deploy Hook URL for `franchisee.id`.

Optional repository variables:

- `CLOUDFLARE_ACCOUNT_ID`
- `CLOUDFLARE_D1_DATABASE_ID`
- `PAGES_PROJECT_NAME`

Defaults are currently embedded for this project/account/database.

## Verification

- `node --check scripts/d1-static-publish-poller.mjs` passed.
- `Get-Content functions\_site-publish-queue.js | node --input-type=module --check` passed.
- `Get-Content functions\form-submit.js | node --input-type=module --check` passed.
- `pnpm exec tsc --noEmit --pretty false` passed.
- `git diff --check` passed with only existing CRLF warnings.

## Remaining Work

- Add GitHub repository secrets and push workflow.
- Verify first dirty D1 edit creates one queued request and triggers exactly one Cloudflare Pages build.
- Add an admin-only manual publish endpoint or dashboard action later.
- Wire future listing edit/delete/admin publication endpoints to `siteRebuildStatements()`.
