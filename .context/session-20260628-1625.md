# Session 2026-06-28 16:25

## Summary
- Updated the public `/peluang-usaha` saved-opportunity control so directory cards show an icon-only bookmark on the top-left of each image.
- Kept the detail page save action visible, but changed blocked save feedback to show a direct CTA when franchisee interest data is incomplete.
- Added API next-action metadata for missing franchisee profile states in `functions/profile-data.js`.
- Added a permanent `AGENTS.md` rule that warning/error/empty-state/blocked-action messages must include clear next steps and direct CTAs whenever the next action is known.
- Added `AUDIT.md` UX actionability audit/tracker and marked this pass complete while leaving wider public copy cleanup open.

## Validation
- `node --check js\opportunity-save.js`
- `Get-Content functions\profile-data.js | node --input-type=module --check`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm run build`

## Notes
- No local dev server was started.
- File line counts after touching large files:
  - `src/lib/franchise-static.ts`: 1078
  - `functions/profile-data.js`: 1164
  - `AUDIT.md`: 312
