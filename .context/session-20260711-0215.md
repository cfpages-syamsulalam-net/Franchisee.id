# Session 2026-07-11 02:15 (Asia/Jakarta)

## Focus
- Tweak brochure/proposal image navigation overlay behavior on generated franchise detail pages.

## Changes
- Proposal image navigation now tracks a side-specific pointer state:
  - `is-pointer-prev`
  - `is-pointer-next`
- Only the hovered/moved side shows the transparent gradient and prev/next label.
- Hovering or focusing the actual prev/next button pins that side so the gradient stays visible while the pointer is on the control.
- The global yellow button hover/tap flash is neutralized for `.fr-proposal-hit`.

## Validation target
- `pnpm run detail-assets:check`
- `pnpm exec tsc --noEmit --pretty false`
- `git diff --check`
