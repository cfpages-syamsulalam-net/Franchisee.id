# Session 2026-07-11 00:10 (Asia/Jakarta)

## Focus
- Audit the current `/daftar` franchisor form against existing form schema/preservation docs and backend D1 mapping.
- Plan priority canonical field additions and a progressive multi-step franchisor form that stays low-friction.

## Findings
- The active `/daftar/index.html` franchisor form is 5 steps but currently much slimmer than `FORM_SCHEMA.md` and `FORM_PRESERVATION_MANDATE.md`.
- Visible active franchisor fields now:
  - Step 1: `brand_name`, `company_name`, `category`, `year_established`, `brand_country`, `target_market`, `haki_status`, `haki_number`, `nib_number`.
  - Step 2: `min_capital`, `royalty_percent`.
  - Step 3: `full_desc`.
  - Step 4: `logo_url`.
  - Step 5: contact/social fields.
- Fields documented as preserved but not currently visible include `outlet_type`, `location_requirement`, `rent_cost`, `fee_license`, `fee_capex`, `fee_construction`, `total_investment_value`, `net_profit_percent`, `royalty_basis`, `short_desc`, `support_system`, `cover_url`, `gallery_urls`, `video_url`, and `proposal_url`.
- Backend submission still accepts passthrough payloads and already maps many of those field names into D1 via `franchiseBindValues()`, so UI restoration should be additive.

## Plan created
- Added `docs/forms/FRANCHISOR_PROGRESSIVE_FORM_PLAN.md`.
- Recommended keeping 5 steps but changing them to:
  1. Identitas
  2. Model & Modal
  3. Proyeksi
  4. Profil & Dukungan
  5. Media & Kontak
- Baseline fields stay visible; optional follow-up groups reveal only after related baseline engagement.
- Priority optional canonical fields to add first:
  - `min_area_sqm`
  - `min_staff_count`
  - `setup_duration_days`
  - `working_capital_idr`
  - `additional_cost_notes`
  - `estimated_bep_min_months`
  - `estimated_bep_max_months`
  - `omzet_monthly_min_idr`
  - `omzet_monthly_max_idr`
  - `net_profit_monthly_min_idr`
  - `net_profit_monthly_max_idr`
- Before adding those fields to the visible UI, restore missing existing backend-supported fields into progressive sections.

## Documentation updates
- Linked the new plan from `docs/README.md`.
- Referenced it from `docs/architecture/OCR_LISTING_ENRICHMENT_PLAN.md`.
- Updated `AUDIT.md` with the form mismatch and canonical/progressive direction.
- Added suggestion 83 in `SUGGESTION.md`.
- Logged changes in `CHANGELOG.md`.

## Validation notes
- This turn only planned/documented the form and field strategy. No migration or runtime form code was implemented yet.
