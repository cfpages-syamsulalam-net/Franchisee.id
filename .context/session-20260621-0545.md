# Session Snapshot - 2026-06-21 05:45 Asia/Jakarta

## Focus
- Cosmetic and SEO cleanup for D1-backed franchise listing and detail pages.
- Canonical category URL policy: Indonesian `/kategori` is canonical; old `/category` exists only as a redirect.

## Changes Made
- Removed the black `HEADER PLACEHOLDER` bar from the franchise detail template.
- Removed fake WordPress author/date metadata (`By admin / October 10, 2025`) from franchise listing details because D1 does not yet expose trustworthy submission/publication timestamps.
- Replaced visual WooCommerce placeholder image fallbacks with CSS-only placeholders in D1/Astro listing output and legacy helper scripts.
- Improved directory cards with clickable franchise names, compact status badges, instant CSS tooltips, and fact chips for modal, BEP, and year founded when data exists.
- Styled detail breadcrumbs so links remain readable on the legacy grey/yellow backgrounds.
- Normalized category links from `/category/...` to `/kategori/...`.
- Added `public/_redirects` so Cloudflare Pages redirects `/category/*` and `/category` to `/kategori/*`.
- Removed Astro `/category/[slug]` route generation and updated the legacy static copy step to skip old root `category/` files.
- Cleaned detail metadata: route-specific title/description, `og:type=website`, generated breadcrumb JSON-LD, and removed stale article-style metadata from generated detail pages.

## Validation
- `pnpm run build` passed.
- Build output confirmed `dist/category` is absent and `dist/_redirects` exists.
- Spot-checked `dist/peluang-usaha/index.html` for clickable names, status badges, fact chips, and CSS placeholders.
- Spot-checked `dist/peluang-usaha/nec.html` for cleaned title/description, `og:type=website`, `/kategori` breadcrumbs, and generated breadcrumb JSON-LD.
