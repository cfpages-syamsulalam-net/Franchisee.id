# Session Context - 2026-04-04 12:15 (Asia/Jakarta)

## Request Summary
User asked to restore pre-submit WhatsApp behavior on `/daftar/index.html` and recover lost legacy UX from historical `/pendaftaran/index.html`, then requested data-file organization improvements.

## What Was Restored
- Restored WhatsApp country-code dropdowns (`country_code`) in both form areas:
  - Franchisee tab WhatsApp field.
  - Franchisor Step 5 (WhatsApp PIC) field.
- Restored pre-submit WhatsApp normalization so payload sent to `/form-submit` uses international format:
  - Defaults to `+62` when no explicit country code is available.
  - Supports existing `+` and `00` prefixed input.
- Restored live validation hooks (`blur`/`input`/`change`) so green valid-state feedback appears consistently.
- Fixed valid-state checkmark spacing in CSS (`padding-right`) so the green check icon renders visibly.

## Data/Structure Refactor Agreed in Session
- Centralized JSON assets into `/json` and CSV assets into `/csv`.
- Moved generated/static claim-search dataset to `/json/unclaimed-brands.json`.
- Added `/json/country-codes.json` as a configuration source for WhatsApp country dropdown options.
- Updated builders and frontend paths accordingly:
  - `js/build-listing.js`
  - `js/build-sitemap.js`
  - `js/form-franchise.js`
- Removed legacy root CSV locations and empty legacy `data/` directory.

## Historical Lineage Context Preserved
- Confirmed that `/daftar/index.html` is a renamed continuation of `/pendaftaran/index.html`.
- Added lineage reminders in docs and inline comment to reduce future regressions during restoration/refactor work.

## Documentation Synced During Session
- `AGENTS.md`
- `CHANGELOG.md`
- `FORM_SCHEMA.md`
- `KNOWLEDGE.md`
- `PRD.md`
- `TECHNICAL_INVENTORY.md`
- `QWEN.md`
- `js/symbols_inventory.md`
- `js/technical_comparison.md`

## Current Convention Snapshot
- JSON assets: `/json/*`
- CSV assets: `/csv/*`
- Claim autocomplete static source: `/json/unclaimed-brands.json`
- Country-code options source: `/json/country-codes.json`
- Session context snapshots: `/.context/*.md`
