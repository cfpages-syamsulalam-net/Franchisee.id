# Session 2026-06-25 00:43

## Focus
- Updated `/peluang-usaha` listing cards so unclaimed brands use the same information-page CTA as claimed listings.
- Adjusted status badge tooltip stacking so hover text is not clipped by legacy card containers.

## Changes
- `src/lib/franchise-static.ts`: Directory card buttons now always render `Info Franchise`; unclaimed claim prompts remain on detail pages through disclaimer/contact/sticky CTA output.
- `src/lib/franchise-static-assets.ts`: Directory CSS now overrides legacy grid/card/content overflow and raises active badge/tooltip stacking.
- `scripts/build-d1-franchise-pages.ts`: D1 bridge card CTA text aligned with Astro renderer.
- `js/build-listing.js`: Legacy builder card CTA text aligned with the current renderer.
- `CHANGELOG.md`, `CODEBASE.md`, and `TECHNICAL_INVENTORY.md` updated.

## Verification
- `node --check js/build-listing.js` passed.
- `pnpm exec tsc --noEmit --pretty false` passed.
- `rg -n "Klaim Brand|KLAIM BRAND" src/lib scripts/build-d1-franchise-pages.ts js/build-listing.js -S` returned no source matches.
- `pnpm run build` passed. The rendered `dist/peluang-usaha/index.html` contains `Info Franchise` and the tooltip overflow CSS. Tracked generated bridge output was restored afterward to keep the source patch focused.
