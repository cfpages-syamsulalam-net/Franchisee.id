# Session 2026-07-06 23:27 (Asia/Jakarta)

## Goal
Enrich public franchise listings from existing form/proposal data, improve detail tab/brochure/contact UX, and add non-destructive brochure watermarking.

## Implemented
- Expanded the D1 static query to carry the full existing listing fact contract, including outlet type, location requirement, cost components, contract, omzet, profit, and royalty data.
- Simplified Profil/tab borders and changed active/inactive tabs to color-based states.
- Added brochure previous/next controls, page count, arrow-key navigation, compatible icons, and bottom-right download-time Franchisee.id watermarking for image-based generated PDFs. R2 originals remain unchanged.
- Replaced hard-coded Alam floating detail contacts with parsed listing WhatsApp/phone and PIC/brand labels.
- Added UnPDF-based selectable-text extraction for owner-uploaded proposal PDFs. Source text/candidates are stored in `franchise_asset_knowledge`; only missing facts create pending admin-reviewed listing suggestions.
- Added and remotely applied migration `0019_proposal_knowledge.sql`. Verified existing 0014-0018 schema/data, reconciled their missing migration-ledger rows, then applied/verified 0019.
- Added `pnpm run proposal:check` and documented the image-only OCR backfill as suggestion 62.

## Validation
- `pnpm run proposal:check`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm exec wrangler pages functions build functions --outdir .context/functions-bundle-check --minify` (temporary output removed; minified bundle was 2,366,818 bytes)
- Generated detail fixture assertion for outlet/location facts, brochure controls/watermark metadata, and dynamic contact floats
- `pnpm run build:d1:franchises:dry -- --limit 1`
- Remote D1 verification confirmed `franchise_asset_knowledge` and migration ledger entries 14-19

## Follow-up
- Production browser QA after push: desktop/mobile tab colors, brochure navigation, watermark appearance, dynamic contact floats, and one real proposal upload.
- Suggestion 62 tracks resumable OCR for image-only/scanned proposal archives.
