# Session Snapshot - 2026-07-17 00:28 (Asia/Jakarta)

## Focus
- Implemented the state-transition audit P0 fixes and the P1 Premium lifecycle test item requested after `docs/architecture/STATE_TRANSITION_AUDIT.md`.

## Implemented
- Added explicit JSON 405 handlers for protected app write/upload routes that previously risked empty Cloudflare method responses:
  - `functions/form-submit.js`
  - `functions/profile-data.js`
  - `functions/profile-upload.js`
  - `functions/premium-receipt-upload.js`
  - `functions/payment-method-upload.js`
- Added `scripts/check-pages-functions-methods.ts` and `pnpm run functions:methods:check` covering 10 route-method contracts.
- Hardened dashboard-only Google Contacts OAuth:
  - New OAuth starts clean old consumed/expired state rows for the staff/admin user.
  - Denied callbacks consume/audit the OAuth state.
  - Expired callbacks consume/audit the OAuth state.
  - `pnpm run google-contacts:check` asserts these cleanup paths.
- Hardened Premium email queue delivery:
  - Due rows are claimed with `locked_at = CURRENT_TIMESTAMP` before delivery.
  - Concurrent worker races skip rows claimed by another run.
  - Locks older than 15 minutes become retryable.
  - `loadDueEmails()` is exported for focused coverage.
- Added `scripts/check-premium-lifecycle.ts` and `pnpm run premium:lifecycle:check`, covering email lock behavior plus source assertions for order expiry reuse, duplicate payment confirmation, renewal-window, approval/source-order, grace downgrade, publication hiding, and rebuild queue contracts.
- Closed the remaining open state-transition audit rows:
  - Publish queue requests now coalesce active matching rows and recalculate `pending_count`.
  - Outreach rows expose `stage_changed_at` separately from preserved milestone timestamps.
  - OCR stale `running` jobs return to `pending`; overdue failed batches no longer set `completed_at`.
  - Google Contacts bulk save now returns per-contact `created`/`duplicate`/`unconfirmed`/`skipped` results and only marks confirmed rows as `saved_contact`.
  - Claim transition matrix, OCR provider health policy, field dictionary, autosave stale-claim guard, and R2/D1 migration runbook are now documented and covered by `pnpm run state-transitions:check`.

## Documentation Updated
- `docs/architecture/STATE_TRANSITION_AUDIT.md`: Marked ST-01, ST-02, ST-10, and ST-11 done for this pass.
- `SUGGESTION.md`: Marked suggestions 97, 108, and 110 done.
- `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, and `CHANGELOG.md`: Documented new scripts and runtime contracts.
- `docs/architecture/STATE_TRANSITION_AUDIT.md`: Marked all inventory rows done for this pass after closing ST-03 through ST-14 open items.
- `docs/data/FRANCHISE_FIELD_DICTIONARY.md`, `docs/forms/CLAIM_TRANSITION_MATRIX.md`, and `docs/architecture/R2_D1_MIGRATION_RUNBOOK.md`: Added new transition/data runbooks.
- `docs/forms/AUTO_SAVE.md`, `docs/architecture/OCR_PROVIDER_STRATEGY.md`, and `docs/README.md`: Documented stale-draft, provider-health, and docs-index changes.

## Validation
- `pnpm run functions:methods:check`
- `pnpm run google-contacts:check`
- `pnpm run premium:lifecycle:check`
- `pnpm run state-transitions:check`
- `pnpm run ocr:runner:check`
- `pnpm exec tsc --noEmit --pretty false`
- `git diff --check` reported only LF-to-CRLF warnings, no whitespace errors.

## 2026-07-17 10:22 Update - Suggestion 104/105 And Directory SEO Plan
- Implemented suggestion 104:
  - Added `docs/ux/UI_REFERENCE_MAP.md`.
  - Updated `AGENTS.md` so public, legal/compliance, directory, profile, dashboard, and review UI work must choose a matching existing Franchisee.id visual reference before styling.
- Implemented suggestion 105:
  - Added `docs/product/SCOPE_TRIAGE.md`.
  - Updated `AGENTS.md` so compound requests are classified as P0/P1/P2 using the triage doc.
- Captured the user's `/peluang-usaha` SEO/UX direction in `docs/seo/TOPICAL_AUTHORITY_AND_DIRECTORY_SEO_PLAN.md`:
  - Current issues: ugly/low-density cards, duplicate search, listing rows pushed too far below first viewport, vague franchisor CTA, and category filter query URLs being weak for SEO.
  - Desired future implementation: compact cards, one primary search/filter surface, direct owner CTA copy ("Punya peluang usaha franchise? Tampilkan brand franchise Anda gratis di Franchisee.id."), buyer/franchisor conversion paths, and indexable `/peluang-usaha/kategori/[slug]` category pages with dynamic metadata/body copy.
  - Broader SEO direction: build topical authority from user personas and intent clusters for franchise buyers, franchisors, unsure entrepreneurs, location-driven buyers, and growth-ready owners.
- Updated `AGENTS.md` permalink policy:
  - `/peluang-usaha` remains the general directory hub.
  - `/peluang-usaha/kategori/[slug]` is now the canonical indexable category landing route.
  - `/peluang-usaha?kategori=...` should be treated as temporary/filter state, not the primary SEO category path.
- Updated `SUGGESTION.md`:
  - Suggestions 104 and 105 are Done.
  - Suggestion 111 tracks the future `/peluang-usaha` directory/category SEO implementation.
- Updated `docs/README.md`, `CODEBASE.md`, `AUDIT.md`, and `CHANGELOG.md` for the new docs and planning direction.

## 2026-07-17 12:24 Update - Suggestion 111 Directory SEO/UX
- Implemented the planned `/peluang-usaha` directory revamp:
  - Removed the duplicate legacy search and long explanatory block from generated directory pages.
  - Compacted hero, controls, cards, media, facts, and responsive mobile layout so results start earlier.
  - Kept one primary directory search and moved category guidance plus buyer/owner conversion CTAs after results.
  - Updated owner copy to: "Punya peluang usaha franchise? Tampilkan brand franchise Anda gratis di Franchisee.id."
- Implemented canonical category behavior:
  - Category selection navigates to `/peluang-usaha/kategori/[slug]`.
  - Only `q`, `sort`, and `status` remain temporary query state.
  - `FnB` and old food/travel/property/technology aliases consolidate to canonical destinations and no longer generate duplicate pages.
  - `jasa-layanan` remains an intentional aggregate hub.
  - Sparse categories and `lainnya` remain reachable with `noindex, follow`.
- Added typed category-specific title/description/subheading/decision content/internal links using actual snapshot counts.
- Replaced legacy WordPress Article/admin JSON-LD with CollectionPage, ItemList, and BreadcrumbList schema.
- Added `docs/seo/CATEGORY_CONTENT_BACKLOG.md` with 35 P2 article ideas and editorial status/intent/funnel/CTA/evidence fields.
- Split directory document/types/assets/styles/client modules so `franchise-static.ts` and the asset facade do not absorb all new responsibilities.
- Validation completed:
  - `pnpm exec tsc --noEmit --pretty false`
  - `pnpm run directory:check` (197 listings, 14 category routes)
  - `pnpm run astro:check` (0 errors; existing hints only)
  - `pnpm exec astro build` (273 pages)
  - Built HTML confirmed one primary directory search, both directory style blocks, CollectionPage schema, dynamic Makanan & Minuman count/title, sparse-category noindex, and absent alias output pages.
  - Removed `dist/` and `.astro/` after validation.
