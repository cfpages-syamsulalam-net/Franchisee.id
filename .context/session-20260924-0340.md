# Dashboard UI/UX audit, 2026-09-24 03:40 WIB

The user asked for a full dashboard UI/UX audit after sharing a signed-in Outreach screenshot. The audit covers ten tabs in `docs/ux/DASHBOARD_UI_UX_AUDIT_2026-09-24.md`. Direct screenshot evidence exists for Outreach; other tabs were reviewed from source because the authenticated browser session is unavailable here.

The main correction makes Outreach worklist rows inline on desktop, keeps category beneath the franchise name, gives icons circular 28 px frames and stable positions, hides Burned reason except after Burned selection, and requires a chosen reason before saving. The server schema rejects Burned without a reason. The count and legend are shorter, shared header/focus styles are corrected, OCR navigation is compact, and Publikasi copy is shorter. Worklist layout lives in `css/dashboard-outreach-worklist.css`.

Focused client and schema checks plus Astro build passed. A local headless browser fixture was inspected at 1264, 900, and 500 px and exposed a note-input overflow, which was fixed. Remaining steps: final diff review, commit/push, remote parity, live asset check, and signed-in visual follow-up when a staff session is available.
