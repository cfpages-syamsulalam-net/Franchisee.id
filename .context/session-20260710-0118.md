# Session 2026-07-10 01:18 Asia/Jakarta

## Focus
- Improved `/dashboard` OCR execution UX so status counts are clickable, job rows are paginated server-side, and rows are compactly grouped by franchise.
- Completed the focused `js/dashboard-ocr.js` refactor into smaller OCR dashboard browser modules.

## Runtime Changes
- Added `search_ocr_jobs` dashboard action:
  - Supports statuses: `all`, `unqueued`, `pending`, `running`, `succeeded`, `needs_review`, `failed`.
  - Supports `limit` and `offset` pagination.
  - `unqueued` lists active proposal image assets that do not yet have an OCR job.
- `dashboard-ocr.js` now:
  - Renders clickable status chips for Belum antre/Pending/Running/Sukses/Perlu cek/Gagal.
  - Fetches paginated job rows from the server instead of filtering only the latest 12 rows.
  - Groups visible job cards by franchise.
  - Keeps the active job filter after retry/no-text/enqueue/run mutations.
  - Makes batch Refresh show a timestamped completion message, even when server-side progress has not changed.
  - Acts as the coordinator/facade for OCR actions, autosave, polling, and active filter state.
- Added focused OCR browser modules:
  - `js/dashboard-ocr-state.js`: initial coordinator state.
  - `js/dashboard-ocr-providers.js`: provider and scheduler rendering/forms.
  - `js/dashboard-ocr-jobs.js`: status chips, grouped job cards, job pagination.
  - `js/dashboard-ocr-batches.js`: batch rows, progress bars, countdown labels.
  - `js/dashboard-ocr-results.js`: compact grouped result cards and per-franchise page navigation.
- `dashboard-admin.js` fetches `/dashboard-data` with `cache: "no-store"` to avoid stale-feeling manual refreshes.

## Refactor Status
- `AUDIT.md` now marks the dashboard OCR client split done.
- `src/pages/dashboard/index.astro` remains a later refactor candidate; planned direction is a route-level assembler plus dashboard components, starting with the OCR panel when it changes again.

## Validation
- Validation should be rerun after the final refactor wiring:
  - `pnpm run dashboard:ocr:check`
  - `pnpm run ocr:runner:check`
  - broader repo checks as needed before handoff.
