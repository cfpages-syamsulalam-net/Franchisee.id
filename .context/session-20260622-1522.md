# Session 2026-06-22 15:22 (Asia/Jakarta)

## Dashboard Completion

- `/dashboard` remains a static Astro shell and `/dashboard-data` remains the server-side authorization boundary.
- Dashboard access requires D1 role `staff`; `admin` is allowed through the existing elevated-role hierarchy in `_clerk-auth.js`.
- Staff can submit listing edit suggestions through a structured JSON diff form.
- Admin can approve/reject pending edit suggestions from the dashboard.
- Approved edit diffs write accepted values field-by-field to whitelisted `franchises` columns, write audit events, and enqueue static rebuild requests through `siteRebuildStatements()`.
- Active `staff_auto_approval_rules` let trusted staff edits apply immediately while still recording the suggestion and audit trail.
- Admin can approve/reject pending `franchise_claims` from the dashboard.
- Approved claims attach owner/profile data when available, move unclaimed listings to free claimed state, write audit events, and enqueue static rebuild requests.
- Dashboard now also shows lead status counts, recent leads, and read-only system health/publish state.

## Verification Notes

- `functions/dashboard-data.js` syntax check passed via `node --input-type=module --check`.
- `pnpm exec tsc --noEmit --pretty false` passed before the full build.
- `pnpm run build` passed after fixing the Astro textarea JSON parsing issue.
- Generated public D1 artifacts from the build were restored because they were not part of the dashboard change.
