# Session Snapshot - 2026-07-08 18:04 (Asia/Jakarta)

## Focus
OCR batch scheduling research for running up to 100 OCR jobs with delay/rate limiting after one dashboard click.

## Implemented
- Added `docs/architecture/OCR_BATCH_SCHEDULING.md` with a persisted-batch-run architecture and ranked scheduler/cron provider research.
- Linked the new scheduling document from `docs/README.md`.
- Added a pointer from `docs/architecture/OCR_PROVIDER_STRATEGY.md`.
- Added suggestion 72 for persisted OCR batch runs before increasing the dashboard batch target to 100.

## Recommendation
- Do not run 100 OCR jobs in one long dashboard request.
- Add D1 `ocr_batch_runs`, a `start_ocr_batch_run` dashboard action, and worker `batch_id` draining.
- Keep GitHub Actions as baseline/fallback; use Cloudflare Workers Cron or cron-job.org for one-minute progress; use Upstash QStash if delayed queue semantics become important.

## Source Categories Checked
- Cloudflare Workers Cron/limits.
- GitHub Actions schedule/billing.
- cron-job.org, Upstash QStash, Inngest, Trigger.dev, Google Cloud Scheduler, UptimeRobot, Netlify Scheduled Functions, and Vercel Cron Jobs.
