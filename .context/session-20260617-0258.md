# Session Snapshot - 2026-06-17 02:58 Asia/Jakarta

## Focus
Implemented the first Clerk authentication layer and connected D1 writes to Clerk-owned D1 users and D1 role checks.

## Added
- `@clerk/backend` and `@clerk/clerk-js`.
- `functions/_clerk-auth.js`: verifies Clerk bearer tokens, fetches Clerk users, upserts D1 `users`, assigns allowed roles, and checks D1 roles.
- `functions/auth-config.js`: returns `CLERK_PUBLISHABLE_KEY` to static browser pages.
- `functions/auth-sync.js`: maps active Clerk users into D1 and self-assigns only `franchisee` or `franchisor`.
- `js/auth-clerk.js`: custom ClerkJS login/register/email-code verification UI and `window.FranchiseAuth` token helper.
- `css/auth-clerk.css`: custom auth styles using existing site variables.
- `register/index.html`: dedicated custom account registration page.
- `docs/architecture/CLERK_SETUP.md`: Clerk Dashboard and Cloudflare Pages env setup instructions.

## Changed
- `/form-submit` now requires Clerk session bearer tokens and D1 roles.
- Franchisee writes require `franchisee`, franchisor/claim writes require `franchisor`, and dev test-data writes require `staff` or `admin`.
- D1 writes now populate `franchisee_profiles.user_id`, `franchisor_profiles.user_id`, `franchises.owner_user_id`, `franchise_claims.claimant_user_id`, and `audit_events.actor_user_id`.
- `/daftar` submit JS attaches Clerk auth headers through `window.FranchiseAuth`.
- `/login` keeps its legacy shell but replaces the old WPForms block at runtime with custom Clerk UI.

## Required Manual Setup
Set these Cloudflare Pages env vars before deployed testing:
- `CLERK_PUBLISHABLE_KEY`
- `CLERK_SECRET_KEY`
- Optional: `CLERK_AUTHORIZED_PARTIES`

In Clerk Dashboard, enable email/password auth and email-code verification, and add production/preview allowed origins.

## Next Recommended Work
1. Run deployed Pages verification for `/auth-config`, `/login`, `/register`, `/auth-sync`, and `/form-submit`.
2. Manually assign first `admin` or `staff` D1 role after creating a Clerk user.
3. Build protected `/franchisee` and `/franchisor` landing pages.
4. Add Clerk webhook handling later for user lifecycle cleanup/status sync.
