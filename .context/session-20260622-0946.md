# Session Snapshot - 2026-06-22 09:46 Asia/Jakarta

## User Direction

- Implement `DASHBOARD.md` and use it as the dashboard progress tracker.
- Use `/dashboard` for Franchisee.id dashboard.
- Staff can suggest edits only; admin approval is required unless admin grants staff auto-approval.
- WhatsApp outreach should use staff personal WhatsApp through generated `wa.me` links, no API integration.
- Dashboard scope is Franchisee.id first; centralized multi-site dashboard is deferred.
- Replace remaining old directory links such as `/rekomendasi`, `/populer`, `/abjad`, `/kategori`, `/category`, and `/direktori-franchise` with canonical `/peluang-usaha` query URLs.

## Implemented

- Added `src/pages/dashboard/index.astro` static dashboard shell.
- Added `functions/dashboard-data.js` protected dashboard API requiring D1 `staff` or elevated `admin`.
- Added `migrations/0004_dashboard_operations.sql` for outreach logs, staff auto-approval rules, and listing edit suggestions.
- Updated `DASHBOARD.md` with product decisions and progress tracker.
- Confirmed source/legacy HTML outside generated output has no remaining old directory route links; `scripts/copy-legacy-static.mjs` still rewrites copied legacy HTML links during build.
- Validated all migrations locally with `npx wrangler d1 migrations apply franchise_db --local`.
- Open dashboard decisions updated: suggestion reviews should use structured JSON diffs before approved field-by-field D1 writes; trusted staff auto-approval may cover all fields; outreach logs only after manual staff confirmation; outreach priority uses data completeness only for now.

## Pending

- Apply migration `0004_dashboard_operations.sql` to remote D1.
- Remote apply succeeded after setting `CLOUDFLARE_ACCOUNT_ID=0ba63b7f0096bc267a93fe5c80b1f571`: `0004_dashboard_operations.sql` is applied to remote `franchise_db`.
- Add staff edit suggestion UI/API.
- Add admin approve/reject actions for edit suggestions and claim reviews.
- Add richer system health and lead/commercial metrics.
