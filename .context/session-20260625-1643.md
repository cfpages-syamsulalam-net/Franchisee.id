# Session Snapshot - 2026-06-25 16:43 Asia/Jakarta

## Focus
Implemented the shared tooltip convention and added project rules for non-technical public copy.

## Changes
- Added `css/shared-tooltip.css` and `js/shared-tooltip.js`.
- Loaded shared tooltip assets on `/profil`, `/dashboard`, `/sso-callback`, `/login`, `/daftar`, and franchise directory/detail templates.
- Converted active UI hints from browser `title` attributes to `data-fr-tooltip` or plain accessible labels.
- Moved public franchise status badge hints from nested CSS-only tooltip markup to the shared `data-fr-tooltip` runtime.
- Updated `AGENTS.md` so future UI hints use the shared tooltip and public copy avoids infrastructure/internal wording.
- Updated `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, and `CHANGELOG.md`.

## Validation Notes
- Form preservation mandate and form UX/validation docs were checked before editing `/daftar/index.html`; only shared CSS/JS includes and hint attributes were changed.
- No local dev server should be started unless explicitly requested; validate with syntax, type, and build checks.
