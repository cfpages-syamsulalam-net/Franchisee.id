# Session Snapshot - 2026-07-06 10:55 (Asia/Jakarta)

## Focus
- Investigated Cloudflare Pages Free-plan Worker request pressure before traffic scaling.
- Removed requested Maxim GIF references.
- Audited and improved generated franchise listing detail UX compactness.

## Cloudflare findings
- Official Cloudflare docs confirm Pages Functions requests count as Workers requests.
- Workers Free / Pages Functions Free quota is 100,000 requests per day and resets at midnight UTC.
- Static asset requests are free/unlimited when they do not invoke Functions.
- Main repo-side public request drivers found:
  - `js/product-events.js` auto-sent `/product-event` for every listing detail page view.
  - `js/site-promo-bar.js` fetched `/premium-promo` anywhere the ribbon script was injected and could send `/premium-event` on ribbon render.
  - `js/premium-page.js` sent `/premium-event` page-view/CTA events.

## Implemented
- Added sampling/dedupe/daily-budget guardrails to `js/product-events.js`.
- Added 30-minute localStorage promo cache and 24-hour promo-event dedupe to `js/site-promo-bar.js`.
- Added `/premium` event dedupe to `js/premium-page.js`.
- Increased `/premium-promo` cache headers to `public, max-age=900, stale-while-revalidate=3600`.
- Added `docs/ux/LISTING_DETAIL_UX_AUDIT.md` with findings, Cloudflare docs references, UX issues, and next-step tracker.
- Removed the requested `franchise-peluang-usaha-maxim-transportasi-online-main-2025.gif` from:
  - `templates/detail-franchise-tpl.html`
  - legacy nested pages `peluang-usaha/english-study-centre/index.html`, `peluang-usaha/kfc/index.html`, and `peluang-usaha/pisang-molen-m-a/index.html`
  - regenerated D1 flat listing pages.
- Reduced detail page vertical friction:
  - subtler breadcrumb styling,
  - capped large detail images,
  - tighter spacing around image/tabs/promo lead/action blocks,
  - compact icon-only save/compare detail buttons,
  - removed duplicate inline claim links and kept floating claim CTA as the single claim action.
- Synchronized D1 bridge and legacy builder disclaimer copy so generated source pages do not reintroduce inline claim CTAs.
- Added `SUGGESTION.md` item 61 for route-level Cloudflare usage review after deploy.

## Validation
- `node --check` passed for `js/product-events.js`, `js/site-promo-bar.js`, and `js/premium-page.js`.
- `pnpm exec tsc --noEmit --pretty false` passed.
- `pnpm run country:check` passed.
- `pnpm run contact:check` passed.
- `pnpm run profile:check` passed.
- `pnpm run build:astro` passed after the final renderer changes.
- `git grep` found no tracked references to the requested Maxim GIF.
- `git grep` found no tracked old inline claim CTA phrases (`Kirim info klaim`, `Klaim listing ini`, `klaim halaman ini`).

## Follow-up
- After deploy, review Cloudflare route-level Function metrics for 24 hours to confirm `/product-event`, `/premium-promo`, `/premium-event`, and auth endpoints stay below the daily Free-plan budget.
- Manual production QA remains needed for mobile first-screen visual tuning of detail pages.
