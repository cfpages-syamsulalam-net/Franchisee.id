# Session Snapshot - 2026-06-25 06:36 (Asia/Jakarta)

## Focus
Cleaned public-facing copy so normal users do not see infrastructure or implementation terms.

## Changes
- Reworded `/profil` copy:
  - Removed references to storage/auth infrastructure and publish queues.
  - Added direct CTA buttons from the summary notice to the relevant `/daftar` form when data is incomplete.
  - Renamed visible profile sections to ordinary language such as `Minat Usaha` and `Data Brand`.
- Reworded `/daftar` read-only identity helper text to say the fields follow the user's account.
- Reworded auth and Google callback fallback messages to avoid implementation names.
- Reworded backend messages that can surface in UI responses for profile, auth sync, form submit, and franchise reads.

## Verification
- `node --check` passed for changed browser scripts.
- ESM syntax checks passed for changed Pages Functions.
- `pnpm exec tsc --noEmit --pretty false` passed.
- `pnpm run build` passed.
- No local dev server was started.
