# Session Snapshot - 2026-06-25 06:23 (Asia/Jakarta)

## Focus
Updated repository working rules after the user clarified that manual UI checks happen directly on production `https://franchisee.id`, not through local dev servers.

## Changes
- Stopped the local Astro dev server started during the `/profil` implementation verification.
- Updated `AGENTS.md`:
  - Do not start local dev or preview servers by default.
  - Use local build/type/static checks for validation.
  - Manual browser QA happens on production after the user pushes code.
  - Start a local server only when explicitly requested.
- Recorded the rule change in `CHANGELOG.md`.
