# Session: new-brand review gate

- Thread: `01a0cf22-1731-7e41-a6d1-8f5258d23b45`.
- Intent: continue Franchisee.id toward full persona testing; user expects commit, push and production parity.
- New franchisor applications now create a private pending listing, draft publication and admin review record. Admin approval requires independently checked evidence, assigns ownership, publishes and queues a rebuild. Rejection archives the unpublished application and releases the name for a fresh application. Applicant identity and claimed NIB/HAKI details are only returned to admins.
- Migration: `0037_new_brand_review.sql` and `0038_rejected_brand_resubmission.sql` applied to active remote `franchise_db`; remote schema guards and unique pending-brand index verified, migration ledger rows 37 and 38 recorded. Earlier rows 34–36 were already executed manually but their bookkeeping is still separate.
- Verified locally: SQL guard check, review action check, brand match and claim regressions, brand-match UI check, Astro build. Signed-in approval and rejection still require disposable identities and a disposable listing.
- Historical raw-payload audit: reachable commits have keys without populated NIB/HAKI values; populated PIC/email/phone values duplicate top-level fields. History rewrite for the raw payload alone is not justified by that finding.
- Publication: implementation commit `86c6f456360b3f57f23f3081c65ec300559f8582` reached `origin/main` and active Cloudflare Pages production deployment `9e041a54-45b9-48dd-bf83-af8e129a061f`. Live dashboard markup and action script include new review UI; public brand match returns an empty result for an unmatched synthetic name; unsigned `/dashboard-data` returns 401. Controlled signed-in review, claim and rebuild walkthrough remains. Next code gate after that is trust-sensitive post-approval contact changes (suggestion 120).