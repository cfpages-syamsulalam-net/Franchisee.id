# Session Snapshot - 2026-06-17 14:47 Asia/Jakarta

## Focus
- Changed D1/Astro public franchise detail physical output from folder indexes to flat `.html` files.
- Public links remain extensionless because Cloudflare Pages can serve `/peluang-usaha/[slug]` from `/peluang-usaha/[slug].html`.

## Code Changes
- `astro.config.mjs`
  - Uses `build.format: "preserve"` and `trailingSlash: "never"`.
- `src/pages/peluang-usaha/[slug].astro`
  - Keeps `getStaticPaths()` from the D1 snapshot and renders each detail page with `src/lib/franchise-static.ts`.
  - With `build.format: "preserve"`, this builds flat `/peluang-usaha/[slug].html` output.
- `scripts/build-d1-franchise-pages.ts`
  - Writes D1 bridge detail pages to `peluang-usaha/[slug].html`.
  - Keeps listing output at `peluang-usaha/index.html`.
  - Safe-prunes old manifest-owned `peluang-usaha/[slug]/index.html` files only when they contain the `d1-generated:franchisee.id` marker.
- `src/lib/franchise-static.ts`
  - Uses extensionless JSON-LD detail URLs.

## Rules To Preserve
- Use extensionless hrefs such as `/peluang-usaha/kopi-coba` in generated cards and UI.
- Use flat physical files such as `peluang-usaha/kopi-coba.html` for generated detail pages.
- Do not delete legacy/example `/peluang-usaha` folders unless they are tracked in `json/d1-generated-pages-manifest.json` and still contain the generated marker.
