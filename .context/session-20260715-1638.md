# Session 2026-07-15 16:38 Asia/Jakarta

## Focus
Implemented a sales outreach Kanban board in `/dashboard` so staff can move franchisor/listing contacts through a durable pipeline instead of reviewing a narrow table.

## Key Changes
- Added shared pipeline contract in `src/lib/outreach-pipeline.js`.
- Added D1 migration `migrations/0030_listing_outreach_statuses.sql` for current listing/site outreach stage.
- Added backend helper `functions/_outreach-status.js` for status upserts and compatible outreach event-history mapping.
- Added `update_outreach_status` dashboard action validation/routing.
- Updated `getUnclaimedOutreachQueue()` to include unclaimed rows, existing pipeline rows, and active subscription rows with current status/subscription context.
- Replaced the Outreach table in `src/pages/dashboard/index.astro` with Kanban board containers.
- Updated `js/dashboard-operations.js` to delegate Outreach rendering/actions while keeping publication, Premium payment review, lead, and health panels.
- Extracted Outreach browser behavior into `js/dashboard-outreach.js`; `js/dashboard-operations.js` now delegates Outreach and stays below the long-file threshold.
- Updated `css/dashboard-operations.css` with board/card/status/drag styling.
- Updated `AUDIT.md` to record that `functions/_dashboard-queries.js` remains the remaining touched long-file risk and should be split after D1 storage/migration cleanup.

## Pipeline Stages
`Uncontacted`, `Saved Contact`, `Contacted`, `Responded`, `Qualified`, `Claim Started`, `Claimed`, `Subscribed`, `Renewal Risk`, `Burned`.

## Validation Status
- `node --check js\dashboard-outreach.js`: passed.
- `node --check js\dashboard-operations.js`: passed.
- `node --check js\dashboard-admin.js`: passed.
- `pnpm exec tsc --noEmit --pretty false`: passed.
- `pnpm run dashboard:ocr:check`: passed.
- `pnpm run google-contacts:check`: passed.
- `pnpm run astro:check`: passed with existing hints only.
- `pnpm run build:astro`: passed; generated `dist` was removed afterward.
- Remote D1: Standard `wrangler d1 migrations apply` was blocked by older pending `0024_ocr_batch_pause_statuses.sql` failing with `Exceeded maximum DB size`. Applied `0030_listing_outreach_statuses.sql` directly through `wrangler d1 execute` and verified the table plus indexes exist.

## Follow-Up
- Remote D1 storage cleanup and migration-ledger reconciliation were completed on 2026-07-16; future `wrangler d1 migrations apply` should no longer try to replay migrations `0024` through `0032`.
- After D1 cleanup, split outreach reads from `functions/_dashboard-queries.js` into a focused dashboard outreach query module.

## 2026-07-15 17:21 Update
- Added a Sales System Operating Plan to `DASHBOARD.md` so the Outreach board can become a measurable staff workflow instead of only a Kanban status view.
- The plan defines sales outcomes, a stage-by-stage playbook, staff dashboard instructions, follow-up cadence, measurement data, and the next implementation tracker.
- Added `SUGGESTION.md` recommendation 95 for the measurable sales work system.

## 2026-07-15 21:20 Update
- Implemented the measurable sales workflow from the plan: Outreach cards now carry next actions, SLA follow-up dates, overdue/mine/unassigned filters, notes, burned reasons, effective claim/subscription status, and conversion metrics.
- Added `migrations/0032_sales_outreach_workflow_fields.sql` and applied it directly to remote D1. `listing_outreach_statuses` now has `next_follow_up_at` and `burned_reason`.
- Moved OCR/proposal long-text ownership toward R2 with `functions/_ocr-text-store.js`, a temporary dashboard backfill action, and `migrations/0031_ocr_text_r2_storage.sql`. New OCR/proposal writes store full text in R2 when `FRANCHISE_ASSETS` is available; D1 keeps preview/length/object-key metadata.
- Applied `0031_ocr_text_r2_storage.sql` directly to remote D1 and verified the new columns. Existing legacy rows still need migration: remote counts at verification time were 479 `franchise_asset_knowledge.source_text` rows and 478 `ocr_content_cache.text` rows.
- Added temporary admin dashboard maintenance actions for OCR text backfill and schema-gated `d1_migrations` reconciliation through `0032`.
- Initial local historical R2 migration failed because the ambient Cloudflare token could not access the target account. The later 2026-07-16 update found the saved `cfman` token path and completed the backfill.

## 2026-07-16 01:04 Update
- Corrected the Cloudflare auth procedure: the ambient shell token is unauthorized for this account, but the saved `cfman` `franchise-network` token works when read into the child process environment with `CLOUDFLARE_ACCOUNT_ID=0ba63b7f0096bc267a93fe5c80b1f571`.
- Updated `AGENTS.md` to make that two-token behavior explicit and to forbid exposing one-time storage backfills as normal dashboard controls after the steady-state write path is fixed.
- Updated `scripts/migrate-ocr-text-to-r2.mjs` to call `pnpm exec wrangler` directly with the saved `cfman` token and account id instead of relying on the broken `cfman wrangler` launch path.
- Ran the OCR text migration locally against remote D1/R2: `franchise_asset_knowledge.source_text` remaining rows = 0, `ocr_content_cache.text` remaining rows = 0, R2-backed knowledge rows = 479, and R2-backed cache rows = 478.
- Removed the dashboard OCR text migration button/action after the backfill completed. Future OCR/proposal text writes are R2-first through `_ocr-text-store.js`; the CLI remains only as an operator replay helper for another environment.
- Cloudflare D1 rejected in-place `VACUUM` and `PRAGMA incremental_vacuum`, but pruning old high-volume `operation_events` telemetry before 2026-07-10 reduced remote D1 reported size from about 501 MB to about 211 MB.
- Verified live schema markers for migrations `0024` through `0032`, inserted the missing `d1_migrations` ledger rows, and confirmed `wrangler d1 migrations list franchise_db --remote` reports no pending migrations.

## 2026-07-16 06:01 Update
- Fixed the `/dashboard` staff/admin login error where an empty HTTP 405 response could be passed to `response.json()` and surface as `Unexpected end of JSON input`.
- `js/auth-clerk-core.js` and `js/dashboard-admin.js` now read response text before JSON parsing and return actionable fallback messages for empty/non-JSON responses.
- `functions/auth-sync.js` and `functions/dashboard-data.js` now return JSON 405 responses for unsupported methods so auth/dashboard clients receive a parseable body.

## 2026-07-16 06:35 Update
- Implemented suggestion 96 with `js/fetch-json.js`, a shared `window.FranchiseFetch` browser helper for text-first JSON parsing.
- Loaded the helper before auth/profile/dashboard/form/public-save scripts on Astro routes, legacy login/register/daftar pages, and public franchise templates.
- Converted browser JSON parsing in auth, dashboard, profile, form, promo, opportunity-save, and proposal-download flows; `rg` over `js/` and `src/lib/franchise-detail-scripts.ts` now shows no direct `response.json()` browser calls.

## 2026-07-16 06:55 Update
- Hardened `/auth-sync` after production still returned an empty HTTP 405 for POST: `functions/auth-sync.js` now uses a single `onRequest` dispatcher that handles POST itself and returns JSON 405 for all other methods.
- Validated the Cloudflare Pages Functions bundle with `pnpm exec wrangler pages functions build --outdir .context\functions-build-check`, then removed the generated build-check output.

## 2026-07-16 07:20 Update
- Split `/dashboard` sales workflow into separate Outreach and Pipeline tabs. Outreach now renders the actionable worklist for save-contact/WhatsApp/status-note work, while Pipeline renders the Kanban board and stage/conversion summary.
- Both tabs still use the same `outreach_queue` rows and `update_outreach_status` action, so moving a brand from either surface reloads the shared dashboard state.
- Expanded the shared Outreach/Pipeline query to include non-archived registered/owned franchisor listings with usable contact data, not only published unclaimed listings, so new franchisors can enter the sales workflow.
- Added suggestion 98 and an `AUDIT.md` refactor note to extract the growing Outreach/Pipeline read model from `functions/_dashboard-queries.js` before the next sales dashboard feature.

## 2026-07-16 07:35 Update
- Implemented suggestion 98 immediately after review: added `functions/_dashboard-outreach-queries.js` for Outreach/Pipeline queue and summary reads, moved the related sales-status helpers there, and left `_dashboard-queries.js` as a compatibility re-export for `/dashboard-data`.
- `_dashboard-queries.js` line count dropped to 681; `_dashboard-outreach-queries.js` is 316 lines.
- Updated `AGENTS.md` to explicitly forbid leaving obvious behavior-preserving extraction work as a backlog item when a touched file is already over roughly 500 lines and the current change clarified that responsibility.

## 2026-07-16 07:55 Update
- Made `/dashboard` top tabs horizontally scroll instead of wrapping, so the growing tab set stays one compact navigation row.
- Reworked Outreach and Pipeline cards to be denser: status badges, icon-only shared-tooltip metadata chips, one-line note inputs, tooltip-backed next-action/reason context, and compact Pipeline column headers.
- Extracted Outreach/Pipeline styles from `css/dashboard-operations.css` into `css/dashboard-outreach.css`; operations CSS now owns only general operations/publication/check-row styling.
- Added `css/dashboard-outreach.css` to the dashboard OCR client assertion source list and documented the new CSS/module ownership in `DASHBOARD.md`, `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, `AUDIT.md`, and `SUGGESTION.md`.

## 2026-07-16 08:20 Update
- Fixed the Google Contacts stale-scope failure path: when `/dashboard-data` returns a 409 for missing/old Google Contacts OAuth scope, Outreach now shows a persistent inline alert instead of only the top status/console network entry.
- The alert explains that Clerk scope configuration may already be updated but the staff session still has the old Google OAuth grant, and it provides `Logout & login Google ulang` plus the Google Contacts setup guide link.
- `_google-contacts.js` now marks Google token/scope/People API permission conflicts as `reauth_required` and uses logout/login-Google copy for stale-session cases.

## 2026-07-16 09:55 Update
- Implemented the new Google Contacts implementation shape after removing `https://www.googleapis.com/auth/contacts` from the Clerk Google connection.
- Main Clerk login stays basic-scope only for all users; public users never receive Contacts permission.
- Added migration `0033_staff_google_contacts_oauth.sql` with `staff_google_connections` and `staff_google_oauth_states`.
- Added `functions/_google-contacts-oauth.js`, `functions/google-contacts-start.js`, and `functions/google-contacts-callback.js` for dashboard-only staff/admin Google OAuth after D1 role authorization.
- `_google-contacts.js` now reads/refreshes the encrypted dashboard Contacts token from D1 instead of asking Clerk for an OAuth access token.
- Added `js/dashboard-google-contacts.js` and wired Outreach to show `Hubungkan Google Contacts`, connected-account pills, and setup/connect/reauth notices.
- Updated `/dashboard-data` to include `google_contacts` connection state and documented the decision in `DASHBOARD.md`, `docs/architecture/DASHBOARD_INTEGRATION_GUIDE.md`, `TECH_STACK_DECISIONS.md`, `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, `AUDIT.md`, and `SUGGESTION.md`.
- Implemented suggestion 99 in the same pass: `/dashboard-data` now supports `disconnect_google_contacts`, Google Contacts state includes token health/last error/timestamps, and Outreach renders reconnect/disconnect icon controls plus a compact health pill.
- Applied `0033_staff_google_contacts_oauth.sql` to remote D1 with the saved `cfman` `franchise-network` token after the ambient token failed with Cloudflare authentication error `10000`; inserted the `d1_migrations` ledger row for id 33 and verified `wrangler d1 migrations list franchise_db --remote` returns no pending migrations.

## 2026-07-16 15:11 Update
- Added public Google verification-ready legal routes: `/privacy-policy` and `/terms-of-service`.
- Added shared `src/components/LegalPage.astro` so both legal pages use one simple, responsive layout and reciprocal Privacy/Terms/Contact navigation.
- Privacy Policy now explicitly documents staff/admin-only Google Contacts usage, duplicate-contact checking, disconnect choice, and Google API Services User Data Policy Limited Use compliance.
- Updated `docs/architecture/DASHBOARD_INTEGRATION_GUIDE.md` with exact Google Auth Platform Branding values: homepage `https://franchisee.id/`, privacy policy `https://franchisee.id/privacy-policy`, terms URL `https://franchisee.id/terms-of-service`, support email `email@franchisee.id`, and authorized domain `franchisee.id`.

## 2026-07-16 15:21 Update
- Implemented suggestion 100 immediately: generated franchise templates now link `/privacy-policy` and `/terms-of-service` from the footer `Informasi` list.
- `scripts/copy-legacy-static.mjs` now injects a compact legal footer row into copied legacy HTML pages when the source page does not already include those links, avoiding a mass source HTML rewrite while making production pages normally discoverable after build.
- Updated `AGENTS.md` to make same-session discoverability/linking work mandatory for newly added public pages, policy pages, integration guides, warnings, and actions when the normal user/admin path is obvious.

## 2026-07-16 15:46 Update
- Reworked `src/components/LegalPage.astro` from a plain standalone legal document shell into a Franchisee.id-themed page with branded header, dark hero, document card, and footer.
- Changed `scripts/copy-legacy-static.mjs` legal-link injection to render as a high-contrast black/yellow Franchisee.id strip so Privacy Policy and Terms of Service links stay readable on copied legacy footer backgrounds.
