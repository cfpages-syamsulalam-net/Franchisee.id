# Session Snapshot - 2026-06-13 01:46 (Asia/Jakarta)

## Focus
Documented the concrete migration decisions requested by the user:
- TypeScript by default for new app/backend/importer/schema work.
- Zod runtime validation at all trust boundaries.
- Source-controlled SQL migrations for D1.
- D1-authoritative role model for `franchisee`, `franchisor`, `admin`, and `staff`.
- Practical Google Sheets to D1 cutover path.
- Drizzle recommendation and adoption timing.

## Decisions
- Keep Astro on Cloudflare as the default framework direction.
- Keep existing CSS as the styling contract.
- Keep Clerk for identity/auth, but store authoritative roles and permissions in D1.
- Use Clerk metadata/session claims only as small UI hints because Clerk metadata/session custom claims have size and freshness limitations.
- Use SQL migrations first for transparent schema control; add Drizzle when TypeScript route handlers and dashboards start querying D1 regularly.

## Documentation Updated
- `TECH_STACK_DECISIONS.md` created as the stack decision source.
- `AGENTS.md` updated so future agents follow TypeScript, Zod, SQL migration, D1-role, and proactive recommendation rules.
- `AUDIT.md`, `CODEBASE.md`, `PRD.md`, `GEMINI.md`, `KNOWLEDGE.md`, and `QWEN.md` aligned with the new technical decisions.
- `CHANGELOG.md` updated for the session.

## Next Coding Work
1. Add `wrangler` config with D1 binding name `DB`.
2. Create the first SQL migration.
3. Add TypeScript/Zod schemas for imported CSV rows and API payloads.
4. Build an idempotent CSV-to-D1 importer.
5. Replace D1 read paths before replacing write paths.
