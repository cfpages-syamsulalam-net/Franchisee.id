# Existing-brand registration choice — 2026-09-24 17:52 UTC

Task: Syamsul asked for an early existing-brand notice. Published unclaimed listings offer a claim when no claim is pending; managed listings link to the existing page; PIC and phone show only for a published, admin-approved owner claim. New duplicate creation still fails safely at submit.

Source: `functions/brand-match.js`, `functions/_form-submit-utils.js`, `functions/_form-submit-franchisor.js`, `/daftar` form HTML, CSS and modular JS. Production D1 expression index in migration 0036 applied and its query plan confirmed. Found and fixed an ID mismatch: unclaimed JSON uses `franchises.id`, prior claim lookup used `legacy_row_id`; both IDs now resolve only with an exact ownerless unclaimed brand match. No original named form field was removed.

Verification: `pnpm exec tsx scripts/check-brand-match.ts`, `scripts/check-brand-match-ui.ts`, `scripts/check-claim-workflow.ts`; browser-script `node --check`; field-preservation comparison; `pnpm run build:astro`; all passed before publication. Next: commit/push intended GitHub branch, verify remote parity and Cloudflare deployment, then public endpoint and route smoke checks. A controlled signed-in claim/managed-brand visual test needs disposable accounts and listing.
