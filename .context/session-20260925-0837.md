# 25 September 2026: suggestion follow-through

Task: implement feasible open `SUGGESTION.md` items, publish through GitHub and check production.

D1: migration 0039 applied to `franchise_db` and confirmed `unique_pending_owner_review` index and `guard_owner_edit_review_decision` trigger. Existing owner proposals counted zero before applying migration. Published owner listing, profile and media edits now queue immutable private proposals; admin approval requires typed notes. A controlled signed in, disposable listing test is still needed.

Local: `pnpm run astro:check` passed with zero errors and five existing hints; `pnpm run build:astro` passed; sitemap fixture passed; owner review TS and SQL checks passed; 217 listing dashboard check passed. Rebuild sitemap after excluding 404 and login, then rerun final build and checks.

Next: review complete Git boundary and remaining checklist items, update suggestion statuses, commit and push `origin/main`, verify deployed SHA and production sitemap, old category redirects, unauthenticated guard. Do not claim protected owner approval verified without test accounts.
Production follow-through: commit c8a3a1b pushed to origin/main and appeared as Cloudflare Pages production deployment aec9e7f0-645b-4e1c-a80b-0f17cc6efd69. Live sitemap returned 200 with 276 canonical URLs and excludes /404 and /login. /category/fnb/ and /category/anak-balita/ each returned one 301 to live canonical page. Anonymous GET /dashboard-data returned 401. Signed in review journey still untested.
