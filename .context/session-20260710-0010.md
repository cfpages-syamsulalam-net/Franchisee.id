# Session 2026-07-10 00:10 (Asia/Jakarta)

## Focus
Improve persisted OCR batch retry behavior and make overdue scheduler waits visible in `/dashboard`.

## Changes
- Updated `functions/_ocr-batch-runs.js` so retrying a non-completed OCR batch:
  - keeps `succeeded` jobs untouched,
  - returns retryable `failed` jobs to `pending`,
  - returns stale `running` jobs to `pending`,
  - leaves existing `pending` jobs pending,
  - refreshes batch progress counts before scheduling the next trigger.
- Updated `js/dashboard-ocr.js` so the retry success message reports failed/running jobs returned to the queue.
- Updated `js/dashboard-ocr.js` so scheduler countdown chips remain visible after the scheduled delay has elapsed, with copy telling admins to use Refresh or Retry if status does not change.
- Updated `CHANGELOG.md`, `CODEBASE.md`, `TECHNICAL_INVENTORY.md`, and `SUGGESTION.md`.

## Operational note
Existing batches such as `ocrbatch_19dcb7bdadaa465f` that already have succeeded pages should be safe to retry after deploy. Retry should skip succeeded pages and continue only the still-pending or reset retryable pages in that same batch.
